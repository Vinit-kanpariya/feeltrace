// crawler/src/processor.ts (stub — replaced in 02-05-PLAN.md)
// This is a placeholder for the real job processor implementation.
// The real processJob function is added in 02-05-PLAN.md when the
// full Playwright crawl + signal extraction pipeline is wired up.

export async function processJob(jobId: string, url: string): Promise<void> {
  console.log(`[processor-stub] Would process jobId=${jobId} url=${url}`)
}
