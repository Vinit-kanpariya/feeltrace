---
phase: 04-results-dashboard
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - vitest.config.mts
  - src/types/narrative.test.ts
  - src/lib/graph-utils.test.ts
  - src/components/ShareButton.test.tsx
  - src/components/JobStatusBadge.test.tsx
  - src/components/NarrativeSection.test.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04]

must_haves:
  truths:
    - "vitest.config.mts include pattern covers .test.tsx files in addition to .test.ts"
    - "All 5 test stub files exist and are RED (failing with import-not-found or missing-module errors)"
    - "npm run test:run exits non-zero (tests are discovered but fail)"
  artifacts:
    - path: "vitest.config.mts"
      provides: "Test discovery for .test.{ts,tsx}"
      contains: "src/**/*.test.{ts,tsx}"
    - path: "src/types/narrative.test.ts"
      provides: "Contract tests for SEVERITY_LABELS and CATEGORY_LABELS"
    - path: "src/lib/graph-utils.test.ts"
      provides: "Contract tests for meetsCredibilityThreshold and buildGraphData"
    - path: "src/components/ShareButton.test.tsx"
      provides: "Contract tests for clipboard copy and 2s revert"
    - path: "src/components/JobStatusBadge.test.tsx"
      provides: "Contract tests for router.push on complete status"
    - path: "src/components/NarrativeSection.test.tsx"
      provides: "Contract tests for all 4 sub-sections rendering"
  key_links:
    - from: "vitest.config.mts"
      to: "src/components/*.test.tsx"
      via: "include glob pattern"
      pattern: "test\\.\\{ts,tsx\\}"
---

<objective>
Fix the Vitest include pattern and write all 5 RED test stubs that document the Phase 4 behavioral contracts before implementation begins.

Purpose: Establish the Nyquist verification baseline. These stubs will fail on import until their source modules exist — that is correct behavior at Wave 0. Subsequent waves turn them GREEN by implementing the modules.

Output: Updated vitest.config.mts + 5 failing test files that specify exact expected behaviors.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/STL-LT-068/Desktop/feeltrace/.planning/PROJECT.md
@C:/Users/STL-LT-068/Desktop/feeltrace/.planning/ROADMAP.md
@C:/Users/STL-LT-068/Desktop/feeltrace/.planning/STATE.md
@C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix vitest.config.mts include pattern for .tsx test files</name>
  <files>vitest.config.mts</files>
  <read_first>
    - vitest.config.mts — read the current include array before editing; must see the existing glob strings
  </read_first>
  <action>
    Update the include array in vitest.config.mts. The current value is:
      ['src/**/*.test.ts', 'crawler/src/**/*.test.ts']
    Change the first entry to cover both .ts and .tsx:
      ['src/**/*.test.{ts,tsx}', 'crawler/src/**/*.test.ts']
    The crawler entry stays .test.ts only — the crawler has no React component tests.
    Do not change any other config property (plugins, environment, etc.).
  </action>
  <verify>
    <automated>grep -c "test\.\{ts,tsx\}" vitest.config.mts</automated>
  </verify>
  <done>vitest.config.mts include array contains 'src/**/*.test.{ts,tsx}' (not just .ts)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED test stubs for all Phase 4 behavioral contracts</name>
  <files>
    src/types/narrative.test.ts,
    src/lib/graph-utils.test.ts,
    src/components/ShareButton.test.tsx,
    src/components/JobStatusBadge.test.tsx,
    src/components/NarrativeSection.test.tsx
  </files>
  <read_first>
    - src/types/job.ts — established types file pattern to replicate in test imports
    - C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-VALIDATION.md — exact behavior contracts per test file
    - C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-RESEARCH.md — Pattern 4 (meetsCredibilityThreshold), code examples for NarrativeResult shape
  </read_first>
  <behavior>
    src/types/narrative.test.ts (covers DASH-01):
    - SEVERITY_LABELS[1] === 'Low'
    - SEVERITY_LABELS[2] === 'Medium'
    - SEVERITY_LABELS[3] === 'High'
    - SEVERITY_LABELS[4] === 'Critical'
    - CATEGORY_LABELS['perceived-perf'] === 'Perceived Performance'
    - CATEGORY_LABELS['technical-perf'] === 'Technical Performance'
    - CATEGORY_LABELS['accessibility'] === 'Accessibility'

    src/lib/graph-utils.test.ts (covers DASH-03):
    - meetsCredibilityThreshold([]) returns false
    - meetsCredibilityThreshold([{ confidence: 'high' }]) returns false (only 1 edge, needs >= 2)
    - meetsCredibilityThreshold([{ confidence: 'low' }, { confidence: 'low' }]) returns false (no high edge)
    - meetsCredibilityThreshold([{ confidence: 'high' }, { confidence: 'low' }]) returns true (2 edges, 1 high)
    - buildGraphData with 2 issues + 1 edge: produces 2 nodes (cause at x=0, effect at x=320) + 1 rfEdge
    - buildGraphData edge label equals the mechanism field from the CausalEdge

    src/components/ShareButton.test.tsx (covers DASH-04):
    - navigator.clipboard.writeText is called when button is clicked
    - Button text shows 'Link copied' immediately after click
    - Button text reverts to 'Copy share link' after 2000ms

    src/components/JobStatusBadge.test.tsx (covers D-03):
    - router.push('/results/{jobId}') is called when status response has status='complete'
    - Error message is displayed when status response has status='failed' with an error_message
    - The <pre> JSON dump is NOT rendered (result state removed per D-03)

    src/components/NarrativeSection.test.tsx (covers DASH-02):
    - Renders the text from narrative.summary
    - Renders the text from narrative.perceivedPerformance
    - Renders the text from narrative.technicalPerformance
    - Renders each string in narrative.recommendations array
    - Sub-label 'Overview' is present in rendered output
    - Sub-label 'How it feels' is present in rendered output
    - Sub-label 'What the data says' is present in rendered output
    - Sub-label 'Recommended actions' is present in rendered output

    All 5 files must import from their respective source modules (which do not exist yet), so
    they will fail at the import/module-not-found stage. This is expected — these are RED stubs.
    Use vi.mock for browser APIs (clipboard, useRouter, fetch) in the component test files.
    Use @testing-library/react render + screen.getByText for component assertions.
  </behavior>
  <action>
    Create each test file importing from its not-yet-existing module so the test suite is RED.

    For src/types/narrative.test.ts: import { SEVERITY_LABELS, CATEGORY_LABELS } from './narrative'
    and write describe/it blocks covering the 7 label assertions above.

    For src/lib/graph-utils.test.ts: import { meetsCredibilityThreshold, buildGraphData } from './graph-utils'
    and write describe/it blocks covering the 6 threshold/layout assertions above.
    Use minimal CausalEdge-shaped objects: { id, fromIssueId, toIssueId, confidence, mechanism } — do not
    import from prisma generated types (test files cannot have DB dependencies).

    For src/components/ShareButton.test.tsx: import { ShareButton } from './ShareButton'
    At the top of the file, mock useRouter (vi.mock('next/navigation', () => ({ useRouter: () => ({}) }))).
    Mock navigator.clipboard: Object.assign(navigator, { clipboard: { writeText: vi.fn() } }).
    Use vi.useFakeTimers() + vi.advanceTimersByTime(2000) to test the 2s revert behavior.

    For src/components/JobStatusBadge.test.tsx: import { JobStatusBadge } from './JobStatusBadge'
    Mock next/navigation: vi.mock('next/navigation', () => ({ useRouter: () => ({ push: mockPush }) }))
    where mockPush is a vi.fn(). Mock global fetch to return { ok: true, json: () => Promise.resolve({status:'complete'}) }.
    Assert mockPush was called with '/results/test-job-id' after interval fires.
    Assert no <pre> element is rendered at any point.

    For src/components/NarrativeSection.test.tsx: import { NarrativeSection } from './NarrativeSection'
    Use a fixture NarrativeResult with summary='Test summary', perceivedPerformance='Test perceived',
    technicalPerformance='Test technical', recommendations=['Fix A', 'Fix B'].
    Assert each string appears via screen.getByText or screen.getAllByText.
    Assert sub-labels Overview, 'How it feels', 'What the data says', 'Recommended actions' appear.

    Do not add @testing-library/react or @testing-library/user-event if they are not already installed —
    check package.json first; if missing, use render from 'vitest' jsdom environment directly or note
    that the import will cause a RED failure (acceptable at Wave 0).
  </action>
  <verify>
    <automated>npm run test:run 2>&1 | tail -20</automated>
  </verify>
  <done>
    npm run test:run discovers all 5 new test files (they appear in the output).
    Tests fail with module-not-found or import errors — NOT with "no tests found".
    The vitest include pattern change is confirmed: .tsx files are picked up.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file → source module | Import contracts; no user data crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | vitest.config.mts edit | accept | Config change is minimal (glob string only); no package installs in this plan |

Note: No package installs in this plan. This plan only edits existing config and creates test stubs.
</threat_model>

<verification>
1. `grep -c "test\.\{ts,tsx\}" vitest.config.mts` returns 1
2. All 5 test files exist under their specified paths
3. `npm run test:run 2>&1 | grep -E "(FAIL|Cannot find module|failed)"` shows failures (not "no tests found")
4. `npm run test:run 2>&1 | grep -E "narrative\.test|graph-utils\.test|ShareButton\.test|JobStatusBadge\.test|NarrativeSection\.test"` — all 5 file names appear
</verification>

<success_criteria>
- vitest.config.mts include pattern is 'src/**/*.test.{ts,tsx}'
- 5 test stub files exist at exact specified paths
- Tests are RED (discovered + failing), not silent (undiscovered)
- Zero new npm packages installed in this plan
</success_criteria>

<output>
Create `.planning/phases/04-results-dashboard/04-01-SUMMARY.md` when done
</output>
