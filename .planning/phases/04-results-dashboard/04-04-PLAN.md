---
phase: 04-results-dashboard
plan: 04
type: execute
wave: 2
depends_on: [04-02, 04-03]
files_modified:
  - src/components/SeverityBadge.tsx
  - src/components/IssueCard.tsx
  - src/components/NarrativeSection.tsx
  - src/components/GraphAbsent.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]

must_haves:
  truths:
    - "SeverityBadge renders a pill with correct bg color and label per severity level 1-4"
    - "IssueCard renders SeverityBadge + category label + technical_description + signal/evidence mono block with left severity border"
    - "NarrativeSection renders all 4 sub-sections with correct sub-labels from UI-SPEC copywriting contract"
    - "GraphAbsent renders 'Causality graph not available' heading and explanation body"
    - "NarrativeSection.test.tsx is GREEN after this plan"
  artifacts:
    - path: "src/components/SeverityBadge.tsx"
      provides: "Pill badge with severity color per UI-SPEC table"
      exports: ["SeverityBadge"]
    - path: "src/components/IssueCard.tsx"
      provides: "Full issue card with severity border and mono evidence block"
      exports: ["IssueCard"]
    - path: "src/components/NarrativeSection.tsx"
      provides: "4-sub-section narrative display component"
      exports: ["NarrativeSection"]
    - path: "src/components/GraphAbsent.tsx"
      provides: "Absent graph placeholder per UI-SPEC copywriting"
      exports: ["GraphAbsent"]
  key_links:
    - from: "src/components/IssueCard.tsx"
      to: "src/components/SeverityBadge.tsx"
      via: "import { SeverityBadge }"
      pattern: "SeverityBadge"
    - from: "src/components/NarrativeSection.tsx"
      to: "src/types/narrative.ts"
      via: "import type { NarrativeResult }"
      pattern: "NarrativeResult"
    - from: "src/components/IssueCard.tsx"
      to: "src/types/narrative.ts"
      via: "import { SEVERITY_LABELS, CATEGORY_LABELS }"
      pattern: "CATEGORY_LABELS"
---

<objective>
Create all four Server Component display components: SeverityBadge, IssueCard, NarrativeSection, and GraphAbsent. These are pure display components with no client-side interactivity.

Purpose: These components constitute the primary display layer for DASH-01 and DASH-02. They are all Server Components that take plain data props and render HTML. No "use client" directive on any of them.

Output: 4 new component files implementing the UI-SPEC design contract for the results page display layer.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/STL-LT-068/Desktop/feeltrace/.planning/ROADMAP.md
@C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-UI-SPEC.md
@C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-RESEARCH.md
@C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-CONTEXT.md

<interfaces>
<!-- From src/types/narrative.ts (created in Plan 04-02): -->
export interface NarrativeResult {
  summary: string
  perceivedPerformance: string
  technicalPerformance: string
  recommendations: string[]
}

export const SEVERITY_LABELS: Record<1 | 2 | 3 | 4, string> = {
  1: 'Low', 2: 'Medium', 3: 'High', 4: 'Critical'
}

export const CATEGORY_LABELS: Record<string, string> = {
  'perceived-perf': 'Perceived Performance',
  'technical-perf': 'Technical Performance',
  'accessibility': 'Accessibility',
}

<!-- Issue fields from prisma/schema.prisma: -->
id: String
category: String        ← map via CATEGORY_LABELS
signal_source: String   ← display as "Signal: {value}" in Geist Mono 14px
severity: Int           ← 1-4, used for SeverityBadge + left border color
raw_evidence: String    ← display as "Evidence: {value}" in Geist Mono 14px
technical_description: String   ← primary body text (16px/400/1.5)

<!-- Severity color table from UI-SPEC: -->
4 Critical: bg=#dc2626 text=#ffffff left-border=#dc2626
3 High:     bg=#ea580c text=#ffffff left-border=#ea580c
2 Medium:   bg=#ca8a04 text=#000000 left-border=#ca8a04
1 Low:      bg=#16a34a text=#ffffff left-border=#16a34a
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SeverityBadge and IssueCard components</name>
  <files>src/components/SeverityBadge.tsx, src/components/IssueCard.tsx</files>
  <read_first>
    - C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-UI-SPEC.md — SeverityBadge and IssueCard component inventory tables + Severity Color Mapping table
    - C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-RESEARCH.md — Code Examples section for SeverityBadge implementation (exact color values)
    - src/types/narrative.ts — SEVERITY_LABELS and CATEGORY_LABELS to import
  </read_first>
  <behavior>
    SeverityBadge:
    - severity=4 renders pill with bg-red-600 text-white label='Critical'
    - severity=3 renders pill with bg-orange-600 text-white label='High'
    - severity=2 renders pill with bg-yellow-600 text-black label='Medium'
    - severity=1 renders pill with bg-green-600 text-white label='Low'
    - No icon — label text only

    IssueCard:
    - Renders SeverityBadge with the issue's severity
    - Renders CATEGORY_LABELS[issue.category] as the category display string
    - Renders issue.technical_description as body text (16px/400/1.5)
    - Renders "Signal: {issue.signal_source}" in font-mono text-sm
    - Renders "Evidence: {issue.raw_evidence}" in font-mono text-sm inside muted box
    - Has left border with 4px solid severity color (inline style border-left)
    - No dangerouslySetInnerHTML anywhere
  </behavior>
  <action>
    Create src/components/SeverityBadge.tsx as a Server Component (no 'use client').
    Import SEVERITY_LABELS from '@/types/narrative'.
    Define SEVERITY_STYLES inline as a const Record<number, { bg: string; text: string }>:
      4: { bg: 'bg-red-600', text: 'text-white' }
      3: { bg: 'bg-orange-600', text: 'text-white' }
      2: { bg: 'bg-yellow-600', text: 'text-black' }
      1: { bg: 'bg-green-600', text: 'text-white' }
    Props: { severity: number }
    Render a <span> with: rounded-full px-2 py-1 text-sm font-semibold + the bg/text classes.
    Text content: SEVERITY_LABELS[severity as 1 | 2 | 3 | 4].
    Fallback to severity 1 styles if key is missing (SEVERITY_STYLES[severity] ?? SEVERITY_STYLES[1]).

    Create src/components/IssueCard.tsx as a Server Component (no 'use client').
    Import SeverityBadge from './SeverityBadge'.
    Import { CATEGORY_LABELS } from '@/types/narrative'.
    Define SEVERITY_BORDER_COLORS inline as a const Record<number, string>:
      4: '#dc2626', 3: '#ea580c', 2: '#ca8a04', 1: '#16a34a'
    Props: { issue: { id: string; category: string; signal_source: string; severity: number; raw_evidence: string; technical_description: string } }
    Layout (vertical stack per UI-SPEC IssueCard spec):
      - Outer div: p-4 mb-4 rounded border border-zinc-200 with inline style borderLeft='4px solid {color}'
      - Row 1: SeverityBadge + space + CATEGORY_LABELS[issue.category] in text-sm text-zinc-500
      - Row 2 (mt-2): technical_description in text-base leading-relaxed
      - Row 3 (mt-3): muted box (bg-zinc-100 rounded p-2 font-mono text-sm)
          "Signal: {signal_source}" on one line
          "Evidence: {raw_evidence}" on the next line
    Never use dangerouslySetInnerHTML.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    npm run typecheck exits 0 — no TypeScript errors in SeverityBadge.tsx or IssueCard.tsx.
    Both files export named functions (SeverityBadge, IssueCard).
    No 'use client' directive in either file.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create NarrativeSection and GraphAbsent components</name>
  <files>src/components/NarrativeSection.tsx, src/components/GraphAbsent.tsx</files>
  <read_first>
    - C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-UI-SPEC.md — NarrativeSection component inventory + GraphAbsent component inventory + Copywriting Contract table
    - C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-RESEARCH.md — Code Examples section for NarrativeSection (exact sub-label strings and ArrowRight usage)
    - src/types/narrative.ts — NarrativeResult interface
  </read_first>
  <behavior>
    NarrativeSection:
    - Renders sub-label 'Overview' above narrative.summary text
    - Renders sub-label 'How it feels' above narrative.perceivedPerformance text
    - Renders sub-label 'What the data says' above narrative.technicalPerformance text
    - Renders sub-label 'Recommended actions' above the recommendations list
    - Renders each string in narrative.recommendations as a list item with ArrowRight icon
    - Section heading is 'What users experience'
    - Background uses secondary surface (bg-zinc-100 in light mode)

    GraphAbsent:
    - Renders heading 'Causality graph not available'
    - Renders body 'The analysis did not find enough high-confidence causal relationships to render a graph reliably. Individual issue details are above.'
    - No React Flow canvas, no empty SVG
  </behavior>
  <action>
    Create src/components/NarrativeSection.tsx as a Server Component (no 'use client').
    Import { ArrowRight } from 'lucide-react'.
    Import type { NarrativeResult } from '@/types/narrative'.
    Props: { narrative: NarrativeResult }

    Structure per UI-SPEC NarrativeSection component inventory:
    - Outer section: bg-zinc-100 dark:bg-zinc-900 rounded-lg p-6
    - Heading h2: text-2xl font-semibold mb-4 — text: "What users experience"
    - Summary block (mb-4):
        <p className="text-sm text-zinc-500 mb-1">Overview</p>
        <p className="text-base leading-relaxed">{narrative.summary}</p>
    - PerceivedPerformance block (mb-4):
        <p className="text-sm text-zinc-500 mb-1">How it feels</p>
        <p className="text-base leading-relaxed">{narrative.perceivedPerformance}</p>
    - TechnicalPerformance block (mb-4):
        <p className="text-sm text-zinc-500 mb-1">What the data says</p>
        <p className="text-base leading-relaxed">{narrative.technicalPerformance}</p>
    - Recommendations block:
        <p className="text-sm text-zinc-500 mb-2">Recommended actions</p>
        <ul className="space-y-2">
          {narrative.recommendations.map((rec, i) => (
            <li key={i} className="flex items-start gap-2 text-base leading-relaxed">
              <ArrowRight size={14} className="text-blue-600 mt-1 shrink-0" />
              <span>{rec}</span>
            </li>
          ))}
        </ul>
    Never use dangerouslySetInnerHTML. Render narrative text as JSX text nodes only.

    Create src/components/GraphAbsent.tsx as a Server Component (no 'use client').
    No props required.
    Structure per UI-SPEC GraphAbsent component inventory and Copywriting Contract:
    - Outer div: bg-zinc-100 rounded-lg p-6 flex flex-col items-center justify-center min-h-[200px]
    - Heading h3: text-2xl font-semibold mb-3 text-center — text: "Causality graph not available"
    - Body p: text-base text-center text-zinc-500 max-w-md — text:
      "The analysis did not find enough high-confidence causal relationships to render a graph reliably. Individual issue details are above."
    No canvas, no SVG, no React Flow imports.
  </action>
  <verify>
    <automated>npm run test:run -- src/components/NarrativeSection.test.tsx</automated>
  </verify>
  <done>
    npm run test:run -- src/components/NarrativeSection.test.tsx exits 0 with all tests GREEN.
    npm run typecheck exits 0 for all 4 new component files.
    No 'use client' directive in NarrativeSection.tsx or GraphAbsent.tsx.
    No dangerouslySetInnerHTML in any of the 4 new components.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB data → React render | narrative text and issue strings from DB are rendered as JSX text nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering (XSS) | NarrativeSection rendering narrative.summary, perceivedPerformance, technicalPerformance, recommendations | mitigate | React JSX auto-escapes all string interpolation in {narrative.summary} etc; no dangerouslySetInnerHTML permitted in these components; verified in acceptance_criteria |
| T-04-05 | Tampering (XSS) | IssueCard rendering technical_description, raw_evidence, signal_source | mitigate | React JSX auto-escapes all string interpolation; no dangerouslySetInnerHTML; content rendered as {text} children only |
</threat_model>

<verification>
1. `npm run test:run -- src/components/NarrativeSection.test.tsx` exits 0 — all sub-section assertions GREEN
2. `npm run typecheck` exits 0 — no errors in any of the 4 new files
3. `grep -rn "dangerouslySetInnerHTML" src/components/NarrativeSection.tsx src/components/IssueCard.tsx src/components/GraphAbsent.tsx src/components/SeverityBadge.tsx` returns no matches
4. `grep -c "use client" src/components/NarrativeSection.tsx src/components/IssueCard.tsx src/components/GraphAbsent.tsx src/components/SeverityBadge.tsx` returns 0 total
</verification>

<success_criteria>
- SeverityBadge.tsx: pill badge rendering with correct Tailwind color classes for severities 1-4
- IssueCard.tsx: left border + SeverityBadge + CATEGORY_LABELS + technical_description + mono evidence block
- NarrativeSection.tsx: all 4 sub-sections with exact UI-SPEC sub-label strings; ArrowRight icons on recommendations
- GraphAbsent.tsx: exact UI-SPEC copywriting; no canvas or SVG
- NarrativeSection.test.tsx GREEN
- npm run typecheck passes for all 4 files
</success_criteria>

<output>
Create `.planning/phases/04-results-dashboard/04-04-SUMMARY.md` when done
</output>
