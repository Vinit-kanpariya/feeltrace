---
phase: 04-results-dashboard
plan: 06
type: execute
wave: 4
depends_on: [04-04, 04-05]
files_modified:
  - src/app/results/[jobId]/page.tsx
  - src/app/results/[jobId]/loading.tsx
  - src/app/results/[jobId]/not-found.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04]

must_haves:
  truths:
    - "GET /results/{jobId} fetches Result+Job+Issues+CausalEdges from Neon and renders the full page"
    - "Issues are ordered Critical-first (severity DESC enforced at Prisma query layer)"
    - "NarrativeSection is the first section after the page title — visible above the fold"
    - "CausalityGraph renders when credibility threshold is met; GraphAbsent renders when it is not"
    - "ShareButton is rendered in the page header area"
    - "Page title is 'UX Analysis: {hostname}' extracted from result.job.url"
    - "notFound() is called when Result record does not exist for the jobId (D-05)"
    - "Inline error section renders when job.status === 'failed' or result.job.error_message is set (D-06)"
    - "loading.tsx provides 3-section shimmer skeleton"
    - "not-found.tsx renders 'Results not found' with exact UI-SPEC copywriting (D-05)"
  artifacts:
    - path: "src/app/results/[jobId]/page.tsx"
      provides: "ResultsPage Server Component — full results display"
      exports: ["default (ResultsPage)"]
    - path: "src/app/results/[jobId]/loading.tsx"
      provides: "Loading skeleton with animate-pulse"
      exports: ["default (ResultsLoading)"]
    - path: "src/app/results/[jobId]/not-found.tsx"
      provides: "404 page with UI-SPEC copywriting"
      exports: ["default (ResultNotFound)"]
  key_links:
    - from: "src/app/results/[jobId]/page.tsx"
      to: "src/lib/prisma.ts"
      via: "import { prisma } from '@/lib/prisma'"
      pattern: "prisma\\.result\\.findUnique"
    - from: "src/app/results/[jobId]/page.tsx"
      to: "src/components/NarrativeSection.tsx"
      via: "import { NarrativeSection }"
      pattern: "NarrativeSection"
    - from: "src/app/results/[jobId]/page.tsx"
      to: "src/components/CausalityGraph.tsx"
      via: "import { CausalityGraph } — passes Node[], Edge[]"
      pattern: "CausalityGraph"
    - from: "src/app/results/[jobId]/page.tsx"
      to: "src/lib/graph-utils.ts"
      via: "import { meetsCredibilityThreshold, buildGraphData }"
      pattern: "meetsCredibilityThreshold"
---

<objective>
Assemble the full /results/[jobId] route: the ResultsPage Server Component that fetches data and renders all components, plus loading.tsx and not-found.tsx co-located in the same route segment.

Purpose: This is the final integration step that wires all the components from Plans 04-04 and 04-05 into a working page that satisfies all four DASH requirements.

Output: Three files under src/app/results/[jobId]/ — the complete public results route.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/STL-LT-068/Desktop/feeltrace/.planning/ROADMAP.md
@C:/Users/STL-LT-068/Desktop/feeltrace/.planning/STATE.md
@C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-CONTEXT.md
@C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-UI-SPEC.md
@C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-RESEARCH.md

<interfaces>
<!-- Prisma query pattern (from route.ts existing pattern + RESEARCH.md Pattern 1): -->
const result = await prisma.result.findUnique({
  where: { jobId },           ← use jobId field NOT id (Result.jobId @unique)
  include: {
    job: true,
    issues: { orderBy: { severity: 'desc' } },   ← sort at DB layer, not JS
    edges: true,
  },
})

<!-- NarrativeResult cast (from STATE.md locked decision): -->
const narrative = result.narrative as unknown as NarrativeResult

<!-- Page title: -->
const hostname = new URL(result.job.url).hostname
// title: `UX Analysis: ${hostname}`

<!-- Graph credibility check (from graph-utils.ts created in Plan 04-03): -->
import { meetsCredibilityThreshold, buildGraphData } from '@/lib/graph-utils'
const showGraph = meetsCredibilityThreshold(result.edges)
const { nodes, edges: graphEdges } = showGraph ? buildGraphData(result.issues, result.edges) : { nodes: [], edges: [] }

<!-- D-06 inline error: -->
if (result.job.status === 'failed' || result.job.error_message) {
  // render inline error section, return HTTP 200
}

<!-- D-05 not-found: -->
if (!result) notFound()   ← call notFound() from 'next/navigation'

<!-- Next.js 15 CRITICAL: params is a Promise: -->
export default async function ResultsPage({
  params,
}: { params: Promise<{ jobId: string }> }) {
  const { jobId } = await params   ← MUST await before use

<!-- Section order per UI-SPEC ResultsPage: -->
1. Page title + ShareButton (flex row)
2. NarrativeSection (mt-8)
3. Issue list with heading (mt-8)
4. CausalityGraph or GraphAbsent (mt-8)

<!-- Issue list section copywriting (UI-SPEC): -->
heading: "Issues Found"
sub-label: "{count} issue{s} ranked by UX impact" (pluralize based on count)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create loading.tsx and not-found.tsx</name>
  <files>src/app/results/[jobId]/loading.tsx, src/app/results/[jobId]/not-found.tsx</files>
  <read_first>
    - C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-RESEARCH.md — Pattern 7 (loading.tsx skeleton) and Pattern 8 (not-found.tsx with Link)
    - C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-UI-SPEC.md — Copywriting Contract (Result not found 404 copywriting) and Loading State interaction contract
  </read_first>
  <action>
    Create src/app/results/[jobId]/loading.tsx as a Server Component (no 'use client').
    This file is automatically used by Next.js App Router as the Suspense fallback for page.tsx.

    Structure (per RESEARCH.md Pattern 7 and UI-SPEC loading state):
    - Outer div: max-w-4xl mx-auto px-4 py-12 animate-pulse
    - Section 1 (narrative skeleton): bg-zinc-100 rounded p-6 mb-8
        3 shimmer divs: h-4 bg-zinc-300 rounded (widths: w-3/4, w-full, w-5/6 mb-3)
    - Section 2 (issue card skeletons): map over [0,1,2]
        Each card: bg-zinc-100 rounded p-4 mb-4
          Badge shimmer: h-5 bg-zinc-300 rounded w-16 mb-3
          Text shimmers: h-4 bg-zinc-300 rounded w-full mb-2 and h-4 bg-zinc-300 rounded w-4/5
    - Section 3 (graph panel skeleton): bg-zinc-100 rounded h-[480px] flex items-center justify-center
        Inner span: text-zinc-400 text-sm — text: "Loading analysis..."

    Create src/app/results/[jobId]/not-found.tsx as a Server Component (no 'use client').
    Import Link from 'next/link'.

    Structure (per RESEARCH.md Pattern 8 and UI-SPEC copywriting contract):
    - Outer main: max-w-4xl mx-auto px-4 py-12
    - h2: text-2xl font-semibold — text: "Results not found"
    - p (mt-4 text-base): "This analysis link may have expired or never existed. " + Link to "/" with
      className="text-blue-600 underline" — text: "Return to the home page"
      + " to run a new analysis."

    Both files export a default function (ResultsLoading, ResultNotFound respectively).
    No 'use client' in either file.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    npm run typecheck exits 0.
    Both files exist at the exact paths.
    not-found.tsx contains 'Results not found' heading text.
    loading.tsx contains 'animate-pulse' class.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ResultsPage (page.tsx) — full route assembly</name>
  <files>src/app/results/[jobId]/page.tsx</files>
  <read_first>
    - C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-RESEARCH.md — Pattern 1 (Server Component Direct Prisma Query), Pattern 2 (NarrativeResult cast), anti-patterns section (CRITICAL pitfalls)
    - C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-UI-SPEC.md — ResultsPage component inventory (layout, section order, section spacing)
    - C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-CONTEXT.md — D-05 (notFound), D-06 (inline error), complete decision set
    - src/app/api/results/[jobId]/route.ts — demonstrates correct await params pattern in Next.js 15
    - src/lib/prisma.ts — import path and prisma export
  </read_first>
  <action>
    Create src/app/results/[jobId]/page.tsx as an async Server Component (no 'use client').

    Imports (in order):
    - import { notFound } from 'next/navigation'
    - import { prisma } from '@/lib/prisma'
    - import type { NarrativeResult } from '@/types/narrative'
    - import { meetsCredibilityThreshold, buildGraphData } from '@/lib/graph-utils'
    - import { NarrativeSection } from '@/components/NarrativeSection'
    - import { IssueCard } from '@/components/IssueCard'
    - import { CausalityGraph } from '@/components/CausalityGraph'
    - import { GraphAbsent } from '@/components/GraphAbsent'
    - import { ShareButton } from '@/components/ShareButton'

    Component signature (Next.js 15 — params is a Promise):
      export default async function ResultsPage({
        params,
      }: { params: Promise<{ jobId: string }> }) {

    Body — step by step:
    1. const { jobId } = await params    ← MUST await (Next.js 15 requirement)
    2. Prisma query:
         const result = await prisma.result.findUnique({
           where: { jobId },
           include: { job: true, issues: { orderBy: { severity: 'desc' } }, edges: true },
         })
    3. if (!result) notFound()
    4. D-06 inline error check:
         if (result.job.status === 'failed' || result.job.error_message) {
           return (
             <main className="max-w-4xl mx-auto px-4 py-12">
               <h2 className="text-2xl font-semibold">Analysis failed</h2>
               <p className="mt-4 text-base">
                 Something went wrong processing this URL.{' '}
                 {result.job.error_message ?? 'Please submit the URL again from the home page.'}
               </p>
             </main>
           )
         }
    5. Cast narrative: const narrative = result.narrative as unknown as NarrativeResult
    6. Extract hostname: const hostname = new URL(result.job.url).hostname
    7. Graph credibility: const showGraph = meetsCredibilityThreshold(result.edges)
    8. If showGraph: const { nodes, edges: graphEdges } = buildGraphData(result.issues, result.edges)
       Else: const nodes = [], graphEdges = []
    9. Issue count for sub-label: const issueCount = result.issues.length
       const issueLabel = issueCount === 1 ? '1 issue ranked by UX impact' : `${issueCount} issues ranked by UX impact`

    Return JSX layout per UI-SPEC ResultsPage (max-w-4xl mx-auto px-4 py-12):
    Section 1 — Header row:
      <div className="flex items-center justify-between">
        <h1 className="text-3xl font-semibold">UX Analysis: {hostname}</h1>
        <ShareButton />
      </div>

    Section 2 — Narrative (mt-8):
      <div className="mt-8">
        <NarrativeSection narrative={narrative} />
      </div>

    Section 3 — Issue list (mt-8):
      <div className="mt-8">
        <h2 className="text-2xl font-semibold mb-1">Issues Found</h2>
        <p className="text-sm text-zinc-500 mb-4">{issueLabel}</p>
        {result.issues.length === 0 ? (
          <div className="bg-zinc-100 rounded-lg p-6">
            <h3 className="text-xl font-semibold">No issues detected</h3>
            <p className="mt-2 text-base">This page passed all signal thresholds. Review the narrative summary for qualitative observations.</p>
          </div>
        ) : (
          result.issues.map(issue => <IssueCard key={issue.id} issue={issue} />)
        )}
      </div>

    Section 4 — Graph (mt-8):
      <div className="mt-8">
        {showGraph ? (
          <>
            <h2 className="text-2xl font-semibold mb-4">Why these issues are connected</h2>
            <CausalityGraph nodes={nodes} edges={graphEdges} />
          </>
        ) : (
          <GraphAbsent />
        )}
      </div>

    CRITICAL implementation notes:
    - params must be awaited — do not use params.jobId directly
    - Prisma lookup is where: { jobId } NOT where: { id: jobId }
    - Issues are ordered at query layer (orderBy: { severity: 'desc' }), not sorted in JS
    - NarrativeResult cast must use double-cast: as unknown as NarrativeResult
    - Pass plain Node[] and Edge[] to CausalityGraph — not Prisma model instances
    - showGraph heading ("Why these issues are connected") is ABSENT when showGraph is false
    - No dangerouslySetInnerHTML anywhere in this file
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    npm run typecheck exits 0 — no TypeScript errors in page.tsx, loading.tsx, or not-found.tsx.
    page.tsx contains 'await params'.
    page.tsx contains 'where: { jobId }' (not 'where: { id: jobId }').
    page.tsx contains 'orderBy: { severity:' (DB-layer sort present).
    page.tsx contains 'as unknown as NarrativeResult' (correct double-cast).
    page.tsx contains 'meetsCredibilityThreshold'.
    page.tsx does NOT contain 'dangerouslySetInnerHTML'.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL param → Prisma query | jobId from URL route param passed to prisma.result.findUnique |
| DB data → JSX render | Result.narrative (Json), Issue fields, CausalEdge fields rendered as JSX text nodes |
| Server Component → Client Component boundary | Serialized plain objects (Node[], Edge[]) passed to CausalityGraph |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Information Disclosure | jobId enumeration via /results/{jobId} | accept | CUIDs are cryptographically unguessable (22-char CUID2 format from cuid2); sequential guessing is computationally infeasible; no rate limiting needed for read-only display route per RESEARCH.md Security Domain |
| T-04-10 | Tampering (XSS) | Result.narrative.summary etc rendered in page.tsx | mitigate | All DB content rendered via JSX text children {} interpolation; React auto-escapes; no dangerouslySetInnerHTML in page.tsx — verified in acceptance criteria |
| T-04-11 | Tampering (SQL Injection) | jobId parameter to prisma.result.findUnique | accept | Prisma parameterizes all queries; findUnique with where: { jobId } is safe against injection regardless of jobId content |
| T-04-12 | Elevation of Privilege | Open redirect via crafted jobId | accept | router.push target in JobStatusBadge uses jobId from the server DB response — not raw user input; page.tsx has no redirect |
| T-04-13 | Denial of Service | Large Result.narrative.recommendations array | accept | LLM narrator is constrained to 2-4 recommendations; at this scale no denial risk; Phase 4 is read-only with no user writes |
</threat_model>

<verification>
1. `npm run typecheck` exits 0 — all three new files compile
2. `grep -c "await params" src/app/results/[jobId]/page.tsx` returns 1
3. `grep -c "where: { jobId }" src/app/results/[jobId]/page.tsx` returns 1
4. `grep -c "orderBy: { severity:" src/app/results/[jobId]/page.tsx` returns 1
5. `grep -c "as unknown as NarrativeResult" src/app/results/[jobId]/page.tsx` returns 1
6. `grep -c "dangerouslySetInnerHTML" src/app/results/[jobId]/page.tsx` returns 0
7. `grep -c "meetsCredibilityThreshold" src/app/results/[jobId]/page.tsx` returns 1
8. `grep -c "Results not found" src/app/results/[jobId]/not-found.tsx` returns 1
9. `grep -c "animate-pulse" src/app/results/[jobId]/loading.tsx` returns 1
</verification>

<success_criteria>
- page.tsx: awaits params, queries by jobId, orderBy severity desc, notFound() on null, D-06 inline error, narrative above fold, 4 sections in correct order, plain-object props to CausalityGraph
- loading.tsx: 3-section skeleton with animate-pulse
- not-found.tsx: exact 'Results not found' copywriting with Link back to home
- npm run typecheck exits 0 for all three files
- No dangerouslySetInnerHTML in any file
</success_criteria>

<output>
Create `.planning/phases/04-results-dashboard/04-06-SUMMARY.md` when done
</output>
