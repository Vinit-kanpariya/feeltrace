---
phase: 04-results-dashboard
plan: 03
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - src/lib/graph-utils.ts
  - src/components/JobStatusBadge.tsx
autonomous: true
requirements: [DASH-03, DASH-04]
user_setup:
  - service: npm
    why: "@xyflow/react and lucide-react required by CausalityGraph and ShareButton"
    env_vars: []
    dashboard_config:
      - task: "Install packages with sfw prefix before running dev server"
        location: "Terminal: sfw npm install @xyflow/react lucide-react"

must_haves:
  truths:
    - "meetsCredibilityThreshold returns false when edges < 2 or no high-confidence edge"
    - "meetsCredibilityThreshold returns true when >= 2 edges AND at least 1 has confidence='high'"
    - "buildGraphData places cause nodes at x=0 and effect nodes at x=320"
    - "JobStatusBadge calls router.push('/results/{jobId}') on complete status (D-03)"
    - "JobStatusBadge shows error message text on failed status (D-03)"
    - "JobStatusBadge has no result state and no <pre> JSON dump (D-03)"
    - "graph-utils.test.ts is GREEN after this plan"
    - "JobStatusBadge.test.tsx is GREEN after this plan"
  artifacts:
    - path: "src/lib/graph-utils.ts"
      provides: "meetsCredibilityThreshold + buildGraphData pure functions"
      exports: ["meetsCredibilityThreshold", "buildGraphData"]
    - path: "src/components/JobStatusBadge.tsx"
      provides: "Rewritten JobStatusBadge per D-03"
  key_links:
    - from: "src/components/JobStatusBadge.tsx"
      to: "next/navigation useRouter"
      via: "import { useRouter } from 'next/navigation'"
      pattern: "router\\.push.*results"
    - from: "src/app/results/[jobId]/page.tsx"
      to: "src/lib/graph-utils.ts"
      via: "import { meetsCredibilityThreshold, buildGraphData }"
      pattern: "meetsCredibilityThreshold|buildGraphData"
---

<objective>
Install the two new npm packages, create src/lib/graph-utils.ts (pure graph computation functions), and rewrite JobStatusBadge per D-03 (remove result state, add router.push navigation).

Purpose: The graph-utils functions are pure and fully testable; implementing them here turns the Wave 0 graph-utils stubs GREEN. The JobStatusBadge rewrite is the critical UX wiring that navigates users from the submission page to the results page after analysis completes.

Output: @xyflow/react + lucide-react installed; graph-utils.ts with 2 exported functions; rewritten JobStatusBadge.tsx.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/STL-LT-068/Desktop/feeltrace/.planning/ROADMAP.md
@C:/Users/STL-LT-068/Desktop/feeltrace/.planning/STATE.md
@C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-CONTEXT.md
@C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-RESEARCH.md

<interfaces>
<!-- CausalEdge model fields (from prisma/schema.prisma): -->
  id: String
  resultId: String
  fromIssueId: String
  toIssueId: String
  relationship: String
  confidence: String   ← 'high' | 'medium' | 'low'
  mechanism: String    ← non-nullable
  explanation: String

<!-- Issue model fields relevant to graph-utils: -->
  id: String
  category: String
  technical_description: String

<!-- React Flow Node type (from @xyflow/react): -->
  interface Node {
    id: string
    position: { x: number; y: number }
    data: { label: string; [key: string]: unknown }
    style?: React.CSSProperties
  }

<!-- React Flow Edge type (from @xyflow/react): -->
  interface Edge {
    id: string
    source: string
    target: string
    label?: string
    style?: React.CSSProperties
  }

<!-- JobStatusBadge current state (to be REMOVED per D-03): -->
  result state: useState<Record<string, unknown> | null>(null)
  fetch to /api/results/{jobId}
  <pre> JSON dump rendering
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Package legitimacy has been manually verified in RESEARCH.md Package Legitimacy Audit:
  - @xyflow/react 12.10.2: npm registry, 6.36M/wk downloads, github.com/xyflow/xyflow, no postinstall, MIT license. [VERIFIED]
  - lucide-react 1.16.0: npm registry, 77.4M/wk downloads, github.com/lucide-icons/lucide, no postinstall, ISC license. [VERIFIED]
  Both are listed as 'Approved' in the Package Legitimacy Audit table in 04-RESEARCH.md.</what-built>
  <how-to-verify>
    Confirm both packages are safe to install:
    1. Open https://www.npmjs.com/package/@xyflow/react — confirm it is the official React Flow package (reactflow.dev maintainers)
    2. Open https://www.npmjs.com/package/lucide-react — confirm it is the official Lucide icon package
    3. Both should show high weekly downloads (millions), MIT/ISC license, no postinstall scripts
    These packages were pre-verified during Phase 4 research (2026-05-26). Manual confirmation takes ~2 minutes.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with sfw npm install, or describe any concern found</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install @xyflow/react and lucide-react</name>
  <files>package.json</files>
  <read_first>
    - package.json — check current dependencies to confirm neither package is already installed
  </read_first>
  <action>
    Run the install command with the sfw prefix (CLAUDE.md requirement — never bare npm install):
      sfw npm install @xyflow/react lucide-react

    This installs into the root Next.js project (not crawler/) — CausalityGraph and ShareButton
    live in src/components/, consumed by the Next.js app.

    Do not run npm install without the sfw prefix under any circumstances.
    Do not install any other packages in this step.
  </action>
  <verify>
    <automated>node -e "require('./node_modules/@xyflow/react/package.json'); console.log('ok')"</automated>
  </verify>
  <done>
    package.json dependencies contains @xyflow/react and lucide-react.
    node_modules/@xyflow/react exists.
    node_modules/lucide-react exists.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/lib/graph-utils.ts (meetsCredibilityThreshold + buildGraphData)</name>
  <files>src/lib/graph-utils.ts</files>
  <read_first>
    - src/lib/prisma.ts — established lib file header comment pattern
    - C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-RESEARCH.md — Pattern 3 (buildGraphData) and Pattern 4 (meetsCredibilityThreshold) for exact implementation details
    - C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-UI-SPEC.md — Causality Graph credibility threshold (3 simultaneous conditions) and node color spec
  </read_first>
  <behavior>
    meetsCredibilityThreshold([]) → false
    meetsCredibilityThreshold([{confidence:'high'}]) → false (only 1 edge; need >= 2)
    meetsCredibilityThreshold([{confidence:'low'},{confidence:'low'}]) → false (no high edge)
    meetsCredibilityThreshold([{confidence:'high'},{confidence:'low'}]) → true
    buildGraphData(2 issues, 1 edge): returns { nodes: Node[], edges: Edge[] }
      - cause node has position.x === 0
      - effect node has position.x === 320
      - rfEdge label equals the mechanism string from the CausalEdge input
      - rfEdge source equals fromIssueId, target equals toIssueId
  </behavior>
  <action>
    Create src/lib/graph-utils.ts as a pure utility module (no React, no side effects).

    Define a minimal local interface for the CausalEdge shape needed by these functions:
      { id: string; fromIssueId: string; toIssueId: string; confidence: string; mechanism: string }
    Define a minimal local interface for the Issue shape:
      { id: string; category: string; technical_description: string }
    Use the Node and Edge types imported from '@xyflow/react'.

    Export meetsCredibilityThreshold(edges: CausalEdgeLike[]): boolean
    Logic (ALL three conditions must be true simultaneously per UI-SPEC):
      1. edges.length >= 1
      2. edges.some(e => e.confidence === 'high')
      3. edges.length >= 2
    (Conditions 1 and 3 collapse: if length >= 2, length >= 1 is also true. Return false if < 2 or no 'high'.)

    Export buildGraphData(issues: IssueLike[], edges: CausalEdgeLike[]): { nodes: Node[]; edges: Edge[] }
    Logic (per Pattern 3 in RESEARCH.md, per UI-SPEC React Flow Node Colors):
      - Determine cause IDs: new Set(edges.map(e => e.fromIssueId))
      - Determine effect IDs: new Set(edges.map(e => e.toIssueId))
      - Filter issues to only those that appear in causeIds or effectIds
      - For each filtered issue: assign x = isCause ? 0 : 320, y = counter*120 per column
      - Node style: cause = { background: '#fef2f2', border: '1px solid #dc2626', color: '#171717' }
                    effect = { background: '#fff7ed', border: '1px solid #ea580c', color: '#171717' }
      - Node data: { label: issue.category }
      - Edge: id=e.id, source=e.fromIssueId, target=e.toIssueId, label=e.mechanism,
              style: { stroke: '#6b7280' }
    Return { nodes, edges: rfEdges }.

    Add a header comment referencing the UI-SPEC credibility threshold and node color spec.
  </action>
  <verify>
    <automated>npm run test:run -- src/lib/graph-utils.test.ts</automated>
  </verify>
  <done>
    npm run test:run -- src/lib/graph-utils.test.ts exits 0 with all tests GREEN.
    All 6 assertions from the Wave 0 stub pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewrite JobStatusBadge.tsx per D-03</name>
  <files>src/components/JobStatusBadge.tsx</files>
  <read_first>
    - src/components/JobStatusBadge.tsx — read current implementation in full; Task 3 is a clean rewrite, not an edit
    - src/types/job.ts — JobStatus, JobStatusResponse types already imported
    - C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-RESEARCH.md — Pattern 6 (JobStatusBadge rewrite) for complete implementation reference
    - C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-CONTEXT.md — D-03 exact requirements
  </read_first>
  <behavior>
    - router.push('/results/{jobId}') is called when polling returns status='complete'
    - Error message text is rendered when status='failed' with error_message set
    - No <pre> element is rendered at any point (result state removed)
    - No fetch to /api/results/{jobId} is made (resultsRes fetch removed)
    - Polling stops (clearInterval) when status becomes 'complete' or 'failed'
  </behavior>
  <action>
    Rewrite src/components/JobStatusBadge.tsx completely. Keep 'use client' directive.

    REMOVE from the current implementation:
    - const [result, setResult] = useState<Record<string, unknown> | null>(null)
    - The fetch to /api/results/${jobId} and setResult call
    - The {result && <pre>...} JSX block

    ADD:
    - import { useRouter } from 'next/navigation'
    - const router = useRouter() inside the component function
    - On data.status === 'complete': call router.push(`/results/${jobId}`) then clearInterval

    KEEP unchanged:
    - 'use client' directive
    - All existing imports except removing anything related to result state
    - const [status, setStatus] = useState<JobStatus>('pending')
    - const [error, setError] = useState<string | null>(null)
    - The useEffect polling loop structure (setInterval at 2000ms, cleanup return)
    - The guard: if (status === 'complete' || status === 'failed') return
    - fetch to /api/jobs/${jobId}, setStatus, setError logic
    - The {error && <p className="text-red-600">{error}</p>} error display
    - The <span>Status: {status}</span> line

    The deps array of useEffect must include router: [jobId, status, router]

    After rewrite, the component has NO result state, NO /api/results fetch, NO <pre> block.
  </action>
  <verify>
    <automated>npm run test:run -- src/components/JobStatusBadge.test.tsx</automated>
  </verify>
  <done>
    npm run test:run -- src/components/JobStatusBadge.test.tsx exits 0 with all tests GREEN.
    JobStatusBadge.tsx does not contain 'setResult' or 'resultsRes' or 'pre' element.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JobStatusBadge → /api/jobs/{jobId} | Client polls server for job status; jobId comes from props |
| JobStatusBadge → router.push | Navigation target is '/results/' + jobId from DB response (not user input) |
| npm package install | Third-party code enters codebase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Information Disclosure | JobStatusBadge polling /api/jobs/{jobId} | accept | jobId is a CUID from the server; client already possesses it from the POST /api/analyze response; no additional exposure |
| T-04-03 | Elevation of Privilege | router.push target in JobStatusBadge | accept | Target is '/results/' + jobId where jobId comes from the server DB response (data.status = 'complete'), not from user-controlled input at this point |
| T-04-SC | Tampering | sfw npm install @xyflow/react lucide-react | mitigate | Both packages pre-verified in Package Legitimacy Audit (RESEARCH.md); human checkpoint before install; sfw prefix enforced per CLAUDE.md |
</threat_model>

<verification>
1. `node -e "require('./node_modules/@xyflow/react/package.json'); console.log('ok')"` prints 'ok'
2. `node -e "require('./node_modules/lucide-react/package.json'); console.log('ok')"` prints 'ok'
3. `npm run test:run -- src/lib/graph-utils.test.ts` exits 0
4. `npm run test:run -- src/components/JobStatusBadge.test.tsx` exits 0
5. `grep -c "setResult\|resultsRes\|<pre" src/components/JobStatusBadge.tsx` returns 0
6. `grep -c "router\.push" src/components/JobStatusBadge.tsx` returns 1
</verification>

<success_criteria>
- @xyflow/react and lucide-react appear in package.json dependencies
- src/lib/graph-utils.ts exports meetsCredibilityThreshold and buildGraphData with correct logic
- graph-utils.test.ts is GREEN (all 6 assertions pass)
- JobStatusBadge.tsx is rewritten per D-03: no result state, no <pre>, router.push on complete
- JobStatusBadge.test.tsx is GREEN (all assertions pass)
</success_criteria>

<output>
Create `.planning/phases/04-results-dashboard/04-03-SUMMARY.md` when done
</output>
