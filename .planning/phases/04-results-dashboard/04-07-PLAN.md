---
phase: 04-results-dashboard
plan: 07
type: execute
wave: 5
depends_on: [04-06]
files_modified: []
autonomous: false
requirements: [DASH-01, DASH-02, DASH-03, DASH-04]

must_haves:
  truths:
    - "Full test suite is GREEN (npm test exits 0)"
    - "Smoke test result page renders without error for react.dev record"
    - "Narrative section is visible above the fold on desktop viewport"
    - "6 issue cards appear ordered Critical-first"
    - "Causality graph renders with at least 2 visible edges"
    - "Share button copies the page URL to clipboard"
    - "JobStatusBadge navigates to /results/{jobId} when analysis completes"
  artifacts:
    - path: "src/app/results/[jobId]/page.tsx"
      provides: "Verified smoke test rendering at /results/cmpmjx5xo0000rcjd0nxrvh5g"
  key_links:
    - from: "browser"
      to: "src/app/results/[jobId]/page.tsx"
      via: "HTTP GET /results/cmpmjx5xo0000rcjd0nxrvh5g"
      pattern: "cmpmjx5xo0000rcjd0nxrvh5g"
---

<objective>
Run the full test suite and perform the browser smoke test against the real Neon DB record from Phase 3.

Purpose: Gate Phase 4 completion. All automated tests must be GREEN and the smoke test result page must render correctly before /gsd:verify-work is run.

Output: No file changes. Test suite green. Smoke test manually verified. Human sign-off collected.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/STL-LT-068/Desktop/feeltrace/.planning/STATE.md
@C:/Users/STL-LT-068/Desktop/feeltrace/.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite</name>
  <files></files>
  <read_first>
    - C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-VALIDATION.md — per-task verification map and sampling rate requirements
  </read_first>
  <action>
    Run the full test suite to confirm all Phase 4 tests are GREEN.

    Execute: npm test

    The test suite should include all prior passing tests (89 from Phases 1-3) plus the new Phase 4
    tests added in Plans 04-01 through 04-05:
    - src/types/narrative.test.ts (7 assertions)
    - src/lib/graph-utils.test.ts (6 assertions)
    - src/components/ShareButton.test.tsx (3 assertions)
    - src/components/JobStatusBadge.test.tsx (3 assertions)
    - src/components/NarrativeSection.test.tsx (8 assertions)

    If any test is failing, diagnose and fix before proceeding to the human checkpoint.
    Common failures at this stage:
    - @testing-library/react import error: check if it needs to be installed (sfw npm install @testing-library/react @testing-library/jest-dom)
    - Mock setup for useRouter: ensure vi.mock('next/navigation') is correct in test files
    - NarrativeResult test fixture shape mismatch: ensure test fixture has all 4 fields
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>npm test exits 0 with all tests GREEN. Output shows no failing tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full Phase 4 results dashboard implementation:
    - Plan 04-01: vitest.config.mts fixed + 5 RED test stubs written
    - Plan 04-02: src/types/narrative.ts (D-01, D-02) + globals.css React Flow import (D-04)
    - Plan 04-03: @xyflow/react + lucide-react installed; graph-utils.ts; JobStatusBadge rewritten (D-03)
    - Plan 04-04: SeverityBadge, IssueCard, NarrativeSection, GraphAbsent Server Components
    - Plan 04-05: CausalityGraph (React Flow client), ShareButton (clipboard client)
    - Plan 04-06: page.tsx + loading.tsx + not-found.tsx assembled

    Smoke test data (from STATE.md Phase 3 completion):
    - Result id: cmpmjx5xo0000rcjd0nxrvh5g
    - URL analyzed: https://react.dev/
    - Issues: 6 records, severity distribution: 2, 4, 2, 4, 2, 2 (two Critical, two Medium, two Medium)
    - CausalEdges: 5 records with mechanisms: ttfb-delays-fcp, render-blocking-js-delays-tti, missing-cdn-increases-ttfb, unused-js-inflates-bundle, deep-dom-increases-layout-cost
    - All 5 edges should meet the credibility threshold (5 >= 2, at least 1 should have confidence='high')
  </what-built>
  <how-to-verify>
    1. Start the dev server: npm run dev
    2. Open http://localhost:3000/results/cmpmjx5xo0000rcjd0nxrvh5g in your browser

    Check each requirement:

    DASH-02 (Narrative above fold):
    - The "What users experience" section is visible without scrolling on a desktop viewport (1440px width)
    - It contains 4 sub-sections: "Overview", "How it feels", "What the data says", "Recommended actions"
    - "Recommended actions" shows a bulleted list with ArrowRight icons

    DASH-01 (Ranked issue list):
    - "Issues Found" heading is visible below the narrative section
    - 6 issue cards appear in the list
    - The first issue card shows "Critical" badge (red) — the severity=4 issues appear first
    - Each card shows the category label, technical description, and a mono "Evidence:" block

    DASH-03 (Causality graph):
    - "Why these issues are connected" heading appears below the issue list
    - A React Flow canvas renders with at least 2 connected nodes and visible edge labels
    - The MiniMap and Controls (zoom in/out buttons) are visible on the canvas
    - If the graph does not render, check the browser console for React Flow errors

    DASH-04 (Share button):
    - "Copy share link" button is visible in the top-right of the page header
    - Click it — the button should show "Link copied" for ~2 seconds then revert
    - Check clipboard: the URL should be http://localhost:3000/results/cmpmjx5xo0000rcjd0nxrvh5g

    D-03 (JobStatusBadge navigation):
    - Go to http://localhost:3000
    - Submit https://react.dev/ (a URL that will be analyzed)
    - Wait for status to reach "complete"
    - Verify the page automatically navigates to /results/{new-job-id}
    (Skip this check if re-running the full pipeline takes too long — the unit test for D-03 is the primary gate)

    Additional checks:
    - Visit http://localhost:3000/results/nonexistent-id — should show "Results not found" page
    - While the page loads for a valid ID, the skeleton animation should be visible briefly

    Report any issues found. If the causality graph threshold is not met (GraphAbsent shows instead), that is correct behavior — report which condition failed (edge count or no high-confidence edge).
  </how-to-verify>
  <resume-signal>
    Type "approved" if all DASH-01 through DASH-04 checks pass.
    Or describe specific issues (e.g., "graph not rendering: console shows ReactFlow error X", "share button shows no success state").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev server → browser | HTTP on localhost; smoke test only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Information Disclosure | Smoke test result visible on localhost | accept | localhost dev server only; smoke test record is react.dev analysis, no personal data |
</threat_model>

<verification>
1. `npm test` exits 0 — all tests GREEN
2. Human verification: narrative section visible above fold on desktop
3. Human verification: 6 issue cards, Critical issues appear first
4. Human verification: React Flow graph renders with visible edges (or GraphAbsent if threshold not met)
5. Human verification: ShareButton copies URL to clipboard and shows 2s success state
6. Human verification: /results/nonexistent-id renders "Results not found" page
</verification>

<success_criteria>
- npm test exits 0 with all Phase 4 tests GREEN
- Smoke test record cmpmjx5xo0000rcjd0nxrvh5g renders without JavaScript errors
- All 4 DASH requirements verified in browser by human
- Human approves via checkpoint resume signal
</success_criteria>

<output>
Create `.planning/phases/04-results-dashboard/04-07-SUMMARY.md` when done
</output>
