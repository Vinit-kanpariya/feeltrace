---
phase: 04-results-dashboard
plan: 02
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - src/types/narrative.ts
  - src/app/globals.css
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]
user_setup: []

must_haves:
  truths:
    - "NarrativeResult interface, SEVERITY_LABELS, and CATEGORY_LABELS are exported from src/types/narrative.ts"
    - "SEVERITY_LABELS maps 1→Low, 2→Medium, 3→High, 4→Critical"
    - "CATEGORY_LABELS maps all 3 keys per D-02"
    - "globals.css imports @xyflow/react stylesheet after @import tailwindcss"
    - "narrative.test.ts is GREEN after this plan"
  artifacts:
    - path: "src/types/narrative.ts"
      provides: "NarrativeResult, SEVERITY_LABELS, CATEGORY_LABELS"
      exports: ["NarrativeResult", "SEVERITY_LABELS", "CATEGORY_LABELS"]
    - path: "src/app/globals.css"
      provides: "Global React Flow stylesheet import"
      contains: "@import '@xyflow/react/dist/style.css'"
  key_links:
    - from: "src/types/narrative.ts"
      to: "src/components/SeverityBadge.tsx"
      via: "import SEVERITY_LABELS"
      pattern: "SEVERITY_LABELS"
    - from: "src/app/globals.css"
      to: "@xyflow/react"
      via: "@import directive"
      pattern: "xyflow/react/dist/style\\.css"
---

<objective>
Create the shared type contract (src/types/narrative.ts) and add the React Flow global stylesheet import (globals.css). These are the two foundational pieces all other Wave 1+ work depends on.

Purpose: Establish the type contracts (D-01, D-02) that display components import, and ensure React Flow CSS is globally available before any CausalityGraph work begins (D-04).

Output: src/types/narrative.ts with NarrativeResult + SEVERITY_LABELS + CATEGORY_LABELS; globals.css with React Flow stylesheet import added after the tailwindcss import.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/STL-LT-068/Desktop/feeltrace/.planning/ROADMAP.md
@C:/Users/STL-LT-068/Desktop/feeltrace/.planning/STATE.md
@C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-CONTEXT.md

<interfaces>
<!-- Source of truth for NarrativeResult shape — copy from here, do not import. -->
<!-- From crawler/src/pipeline/types.ts (per D-01 — each sub-project owns its copy): -->

NarrativeResult interface:
  summary: string
  perceivedPerformance: string
  technicalPerformance: string
  recommendations: string[]

SEVERITY_LABELS (from crawler types):
  1: 'Low', 2: 'Medium', 3: 'High', 4: 'Critical'

CATEGORY_LABELS (per D-02):
  'perceived-perf': 'Perceived Performance'
  'technical-perf': 'Technical Performance'
  'accessibility': 'Accessibility'

<!-- Existing globals.css content (full file is 27 lines): -->
  @import "tailwindcss";
  :root { --background: #ffffff; --foreground: #171717; }
  @theme inline { ... }
  @media (prefers-color-scheme: dark) { ... }
  body { ... }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/types/narrative.ts (D-01, D-02)</name>
  <files>src/types/narrative.ts</files>
  <read_first>
    - src/types/job.ts — established file structure pattern (header comment, exports, JSDoc)
    - C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-CONTEXT.md — D-01 and D-02 exact requirements
  </read_first>
  <behavior>
    - SEVERITY_LABELS[1] === 'Low'
    - SEVERITY_LABELS[2] === 'Medium'
    - SEVERITY_LABELS[3] === 'High'
    - SEVERITY_LABELS[4] === 'Critical'
    - CATEGORY_LABELS['perceived-perf'] === 'Perceived Performance'
    - CATEGORY_LABELS['technical-perf'] === 'Technical Performance'
    - CATEGORY_LABELS['accessibility'] === 'Accessibility'
    - NarrativeResult interface exports: summary, perceivedPerformance, technicalPerformance, recommendations
  </behavior>
  <action>
    Create src/types/narrative.ts following the same header comment pattern as src/types/job.ts.

    Header comment must state: this file is a copy of the NarrativeResult shape from
    crawler/src/pipeline/types.ts — each sub-project owns its copy; do not import from crawler/.

    Export three items:
    1. NarrativeResult interface with fields: summary (string), perceivedPerformance (string),
       technicalPerformance (string), recommendations (string[])
    2. SEVERITY_LABELS constant typed as Record<1 | 2 | 3 | 4, string> with values:
       1: 'Low', 2: 'Medium', 3: 'High', 4: 'Critical'
    3. CATEGORY_LABELS constant typed as Record<string, string> with values:
       'perceived-perf': 'Perceived Performance',
       'technical-perf': 'Technical Performance',
       'accessibility': 'Accessibility'

    Use `export const` for constants and `export interface` for the interface.
    No default exports — named exports only (per job.ts pattern).
  </action>
  <verify>
    <automated>npm run test:run -- src/types/narrative.test.ts</automated>
  </verify>
  <done>
    npm run test:run -- src/types/narrative.test.ts exits 0 with all tests GREEN.
    All 7 label assertions from the Wave 0 stub pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add React Flow stylesheet import to globals.css (D-04)</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css — read full file; the import must be inserted after @import "tailwindcss" and before :root
  </read_first>
  <action>
    Edit src/app/globals.css to add the React Flow stylesheet import.

    The @import '@xyflow/react/dist/style.css' line must appear as the SECOND line of the file,
    immediately after @import "tailwindcss" and before the :root block.

    Import order matters (per Pitfall 6 in RESEARCH.md):
    1. @import "tailwindcss"      ← already present, line 1
    2. @import '@xyflow/react/dist/style.css'   ← INSERT HERE
    3. :root { ... }              ← existing content follows

    Note: @xyflow/react must be installed (Plan 04-02 installs it in Wave 1) before this import
    resolves at build time. Adding the import here before install is acceptable — Next.js won't
    fail on the missing package until a build is run. The install happens in the same wave.

    Do not change any existing :root, @theme, @media, or body rules.
  </action>
  <verify>
    <automated>grep -c "@xyflow/react/dist/style.css" src/app/globals.css</automated>
  </verify>
  <done>
    src/app/globals.css contains the line @import '@xyflow/react/dist/style.css'.
    The Tailwind import still appears on line 1.
    No existing CSS rules were removed or modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| globals.css → @xyflow/react | Third-party CSS loaded globally; affects all pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | globals.css third-party CSS import | accept | @xyflow/react is verified MIT package (Package Legitimacy Audit in RESEARCH.md); postinstall: none; no script execution |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; both packages installed in Plan 04-03 after legitimacy checkpoint |
</threat_model>

<verification>
1. `npm run test:run -- src/types/narrative.test.ts` exits 0 — all 7 label assertions GREEN
2. `grep -c "@xyflow/react/dist/style.css" src/app/globals.css` returns 1
3. `grep -n "tailwindcss" src/app/globals.css` shows @import "tailwindcss" on line 1
4. `npm run typecheck` exits 0 — no type errors in narrative.ts
</verification>

<success_criteria>
- src/types/narrative.ts exported: NarrativeResult interface, SEVERITY_LABELS Record<1|2|3|4, string>, CATEGORY_LABELS Record<string, string>
- SEVERITY_LABELS and CATEGORY_LABELS match exact values per D-01 and D-02
- globals.css @import '@xyflow/react/dist/style.css' appears after @import "tailwindcss"
- narrative.test.ts is GREEN (Wave 0 stub now passes)
</success_criteria>

<output>
Create `.planning/phases/04-results-dashboard/04-02-SUMMARY.md` when done
</output>
