---
phase: 04-results-dashboard
plan: 05
type: execute
wave: 3
depends_on: [04-03]
files_modified:
  - src/components/CausalityGraph.tsx
  - src/components/ShareButton.tsx
autonomous: true
requirements: [DASH-03, DASH-04]

must_haves:
  truths:
    - "CausalityGraph renders a ReactFlow canvas with Background dots + Controls + MiniMap"
    - "CausalityGraph container has explicit pixel height: h-[480px] on desktop, h-[320px] on mobile"
    - "CausalityGraph accepts nodes and edges as plain-object props (not Prisma instances)"
    - "ShareButton calls navigator.clipboard.writeText(window.location.href) on click"
    - "ShareButton shows 'Link copied' success state for 2000ms then reverts to 'Copy share link'"
    - "ShareButton.test.tsx is GREEN after this plan"
  artifacts:
    - path: "src/components/CausalityGraph.tsx"
      provides: "React Flow canvas — 'use client'"
      exports: ["CausalityGraph"]
    - path: "src/components/ShareButton.tsx"
      provides: "Clipboard copy button — 'use client'"
      exports: ["ShareButton"]
  key_links:
    - from: "src/app/results/[jobId]/page.tsx"
      to: "src/components/CausalityGraph.tsx"
      via: "import { CausalityGraph } — passes plain Node[], Edge[] as props"
      pattern: "CausalityGraph"
    - from: "src/components/ShareButton.tsx"
      to: "navigator.clipboard"
      via: "navigator.clipboard.writeText(window.location.href)"
      pattern: "clipboard\\.writeText"
---

<objective>
Create CausalityGraph (React Flow client component) and ShareButton (clipboard client component). Both require "use client" and browser APIs.

Purpose: CausalityGraph implements DASH-03 (directed graph visualization). ShareButton implements DASH-04 (persistent share link). These are the two mandatory Client Components in Phase 4.

Output: CausalityGraph.tsx with ReactFlow canvas + background/controls/minimap; ShareButton.tsx with 2s clipboard success state.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/STL-LT-068/Desktop/feeltrace/.planning/ROADMAP.md
@C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-UI-SPEC.md
@C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-RESEARCH.md

<interfaces>
<!-- React Flow API (@xyflow/react v12.10.2): -->
import { ReactFlow, Background, Controls, MiniMap, type Node, type Edge } from '@xyflow/react'
<!-- Note: stylesheet is in globals.css (D-04), NOT imported inside this component -->

<!-- Lucide React icons for ShareButton: -->
import { Copy, Check } from 'lucide-react'

<!-- CausalityGraph props — plain objects only, no Prisma instances: -->
interface CausalityGraphProps {
  nodes: Node[]
  edges: Edge[]
}

<!-- ShareButton state: -->
const [copied, setCopied] = useState(false)
<!-- On click: clipboard.writeText → setCopied(true) → setTimeout(setCopied(false), 2000) -->

<!-- UI-SPEC copywriting: -->
rest label: 'Copy share link'   icon: Copy (16px)
success label: 'Link copied'    icon: Check (16px)
success fill: bg-blue-600 text-white border-blue-600
rest outline: border-blue-600 text-blue-600
min-height: 44px (WCAG 2.5.5 AA touch target)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CausalityGraph.tsx (React Flow client component)</name>
  <files>src/components/CausalityGraph.tsx</files>
  <read_first>
    - C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-RESEARCH.md — Pattern 3 (CausalityGraph) for full implementation and pitfall notes (especially Pitfall 1 stylesheet, Pitfall 5 container height)
    - C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-UI-SPEC.md — CausalityGraph component inventory (canvas size, controls, background variant)
    - src/app/globals.css — confirm @import '@xyflow/react/dist/style.css' is already there (added in Plan 04-02)
  </read_first>
  <action>
    Create src/components/CausalityGraph.tsx with 'use client' as the first line.

    Imports:
    - import { ReactFlow, Background, Controls, MiniMap, type Node, type Edge } from '@xyflow/react'
    - DO NOT import '@xyflow/react/dist/style.css' here — it is already in globals.css (D-04).
      Importing it here as well would cause style duplication and potential SSR issues.

    Props interface CausalityGraphProps: { nodes: Node[]; edges: Edge[] }

    Component structure:
    - Outer wrapper div: className="w-full h-[320px] md:h-[480px]"
      (mobile-first: 320px default, 480px on md+ breakpoint — matches UI-SPEC canvas size spec)
    - Inside the wrapper: <ReactFlow nodes={nodes} edges={edges} fitView colorMode="light">
    - Inside ReactFlow:
        <Background variant="dots" />
        <Controls />
        <MiniMap />

    The explicit pixel height on the wrapper is MANDATORY. Without it, ReactFlow collapses to
    0px height (Pitfall 5 in RESEARCH.md).

    Only these three sub-components — Background, Controls, MiniMap.
    No custom node types, no edge types, no event handlers (read-only graph).
    Export as named export: export function CausalityGraph(...)
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    npm run typecheck exits 0 — no TypeScript errors in CausalityGraph.tsx.
    CausalityGraph.tsx starts with 'use client'.
    CausalityGraph.tsx does NOT contain any @xyflow/react/dist/style.css import.
    CausalityGraph.tsx wrapper div has h-[320px] md:h-[480px] classes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ShareButton.tsx (clipboard client component)</name>
  <files>src/components/ShareButton.tsx</files>
  <read_first>
    - C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-RESEARCH.md — Pattern 5 (ShareButton) for full implementation including the try/catch fallback
    - C:/Users/STL-LT-068/Desktop/feeltrace/.planning/phases/04-results-dashboard/04-UI-SPEC.md — ShareButton component inventory (exact copywriting, colors, 44px min-height, interaction contract)
  </read_first>
  <behavior>
    - navigator.clipboard.writeText is called with window.location.href when button is clicked
    - Button label is 'Copy share link' at rest with Copy icon
    - Button label changes to 'Link copied' with Check icon after successful copy
    - Button state reverts to rest after exactly 2000ms
    - On clipboard failure: calls alert('Could not copy link. URL: ' + url) — no silent failure
  </behavior>
  <action>
    Create src/components/ShareButton.tsx with 'use client' as the first line.

    Imports:
    - import { useState } from 'react'
    - import { Copy, Check } from 'lucide-react'

    State: const [copied, setCopied] = useState(false)

    handleCopy function (async):
    1. const url = window.location.href
    2. try: await navigator.clipboard.writeText(url) → setCopied(true) → setTimeout(() => setCopied(false), 2000)
    3. catch: alert('Could not copy link. URL: ' + url)

    JSX:
    - <button onClick={handleCopy} className={...}> with dynamic classes:
      rest: 'min-h-[44px] px-4 flex items-center gap-2 rounded border border-blue-600 text-blue-600 bg-transparent'
      success (copied=true): 'min-h-[44px] px-4 flex items-center gap-2 rounded border bg-blue-600 border-blue-600 text-white'
    - Icon: {copied ? <Check size={16} /> : <Copy size={16} />}
    - Label: {copied ? 'Link copied' : 'Copy share link'}

    Use a ternary or template literal for className — no external class merging library.
    Do NOT add any useEffect for timer cleanup — the setState-after-unmount warning is acceptable
    for a 2s timeout at Phase 4 MVP scale. If cleanup is added, use useRef to store the timeout ID
    and clearTimeout in a useEffect return function.

    Export as named export: export function ShareButton()
  </action>
  <verify>
    <automated>npm run test:run -- src/components/ShareButton.test.tsx</automated>
  </verify>
  <done>
    npm run test:run -- src/components/ShareButton.test.tsx exits 0 with all tests GREEN.
    ShareButton.tsx starts with 'use client'.
    ShareButton.tsx min-h-[44px] is present on the button element.
    ShareButton.tsx does not use dangerouslySetInnerHTML.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ShareButton → navigator.clipboard | Browser clipboard API; writes window.location.href only |
| CausalityGraph → ReactFlow DOM | React Flow renders SVG/DOM; receives plain objects from Server Component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Information Disclosure | ShareButton writing to clipboard | accept | Clipboard content is window.location.href — the current page URL, already visible in the browser address bar; no sensitive data is written |
| T-04-07 | Tampering (XSS) | CausalityGraph rendering node labels from issue.category | mitigate | React Flow renders node data.label as React children (JSX auto-escaped); no dangerouslySetInnerHTML in CausalityGraph; edge labels (mechanism strings) also auto-escaped |
| T-04-08 | Tampering | CausalityGraph receiving Prisma model instances | mitigate | Props typed as Node[] and Edge[] (plain objects from @xyflow/react types); parent Server Component must pass only plain objects — enforced by the type boundary (Pitfall 4 in RESEARCH.md) |
</threat_model>

<verification>
1. `npm run test:run -- src/components/ShareButton.test.tsx` exits 0
2. `npm run typecheck` exits 0 for CausalityGraph.tsx and ShareButton.tsx
3. `grep -c "use client" src/components/CausalityGraph.tsx src/components/ShareButton.tsx` returns 2
4. `grep -c "xyflow/react/dist/style" src/components/CausalityGraph.tsx` returns 0 (stylesheet NOT imported here)
5. `grep -c "h-\[320px\]" src/components/CausalityGraph.tsx` returns 1 (explicit height present)
</verification>

<success_criteria>
- CausalityGraph.tsx: 'use client', ReactFlow with Background+Controls+MiniMap, h-[320px] md:h-[480px] wrapper, no stylesheet import inside component
- ShareButton.tsx: 'use client', clipboard.writeText(window.location.href), 2000ms revert, min-h-[44px], Copy/Check icons, fallback alert
- ShareButton.test.tsx GREEN (all assertions pass)
- npm run typecheck passes for both files
</success_criteria>

<output>
Create `.planning/phases/04-results-dashboard/04-05-SUMMARY.md` when done
</output>
