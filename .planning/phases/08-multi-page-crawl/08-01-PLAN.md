---
phase: 08-multi-page-crawl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - crawler/prisma/schema.prisma
  - crawler/src/pipeline/types.ts
  - crawler/src/lib/types.ts
autonomous: true
requirements: [CRAWL-01, CRAWL-02, CRAWL-03]

must_haves:
  truths:
    - "Prisma schema contains CrawledPage, CrawledPageIssue, and CrawledPageEdge models"
    - "Result model has crawledPages relation and cross_page_patterns Json? column"
    - "A new named migration file exists in prisma/migrations/ for the CrawledPage models"
    - "crawler/prisma/schema.prisma is identical to root schema except for generator output path and datasource url"
    - "types.ts exports PipelineResult, PageAnalysisResult, CrossPagePattern, SiteWideNarrative interfaces"
    - "CrawlPass interface has optional internalLinks field"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "CrawledPage, CrawledPageIssue, CrawledPageEdge models; Result.crawledPages relation; Result.cross_page_patterns Json?"
      contains: "model CrawledPage"
    - path: "crawler/prisma/schema.prisma"
      provides: "Sync copy of root schema for crawler Prisma client generation"
      contains: "model CrawledPage"
    - path: "crawler/src/pipeline/types.ts"
      provides: "Phase 8 type contracts used by all crawler modules"
      exports: ["PipelineResult", "PageAnalysisResult", "CrossPagePattern", "SiteWideNarrative"]
    - path: "crawler/src/lib/types.ts"
      provides: "CrawlPass with internalLinks field"
      contains: "internalLinks?: string[]"
  key_links:
    - from: "crawler/src/pipeline/types.ts"
      to: "crawler/src/pipeline/site-wide-merger.ts"
      via: "PageAnalysisResult, SiteWideNarrative, CrossPagePattern imports"
      pattern: "import.*PageAnalysisResult.*from.*types"
    - from: "prisma/schema.prisma"
      to: "prisma/migrations/"
      via: "npm run db:migrate"
      pattern: "CrawledPage"
    - from: "crawler/src/lib/types.ts"
      to: "crawler/src/browser.ts"
      via: "CrawlPass.internalLinks field"
      pattern: "internalLinks\\?"
---

<objective>
Establish the Phase 8 type contracts and database schema that all subsequent plans depend on. This plan defines the data shapes consumed by browser.ts, run-pipeline.ts, site-wide-merger.ts, and processor.ts before any of those files are modified.

Purpose: Wave 0 blocker — zero subsequent plans can compile or run without these type definitions and the migrated Prisma schema. The CrawledPage models are new additions with no nullable/non-nullable migration risk on existing rows.

Output: Four modified files + one new Prisma migration file. No new npm packages required.
</objective>

<execution_context>
@/c/Users/STL-LT-068/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/STL-LT-068/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-multi-page-crawl/08-RESEARCH.md
@.planning/phases/08-multi-page-crawl/08-PATTERNS.md

<interfaces>
<!-- Current schema.prisma — existing models to reference for naming and pattern conventions -->
<!-- From prisma/schema.prisma (lines 38-77) -->

Existing Result model (lines 38-48):
  id, jobId @unique, job relation, narrative Json, screenshot_url String?, tech_stack Json?, issues Issue[], edges CausalEdge[], created_at DateTime

Existing Issue model (lines 50-63) — mirror field layout for CrawledPageIssue:
  id, resultId, result relation, category String, signal_source String, severity Int, raw_evidence String, technical_description String, fix_suggestion String @default(""), severity_justification String @default(""), causedBy CausalEdge[] @relation("ToIssue"), causes CausalEdge[] @relation("FromIssue")

Existing CausalEdge model (lines 65-77) — mirror for CrawledPageEdge:
  id, resultId, result relation, fromIssueId, fromIssue, toIssueId, toIssue, relationship String, confidence String, mechanism String (NON-NULLABLE), explanation String

<!-- Current crawler/src/pipeline/types.ts — append after PERMITTED_MECHANISMS const -->
Existing types: ScoredIssue, EnrichedIssue (with fix_suggestion, severity_justification), CausalEdgeCandidate, NarrativeResult, SEVERITY_LABELS, PERMITTED_MECHANISMS

<!-- Current crawler/src/lib/types.ts CrawlPass (lines 102-113) -->
Existing CrawlPass fields: viewport, domSignals, cssSignals, jsSignals, networkSignals, screenshot?, browserFingerprint?, axeViolations?
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend Prisma schemas with CrawledPage models and run migration</name>
  <files>prisma/schema.prisma, crawler/prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (read entire file — must understand existing model conventions before adding)
    - crawler/prisma/schema.prisma (read entire file — verify generator output path and datasource url header differences to preserve)
  </read_first>
  <action>
    In prisma/schema.prisma, make two additions to the Result model and append three new models after CausalEdge:

    1. Add to Result model (after "edges CausalEdge[]"):
       - crawledPages   CrawledPage[]       (relation — no column on Result itself; backward compatible)
       - cross_page_patterns Json?           (nullable column for CrossPagePattern[] serialized; comment: Phase 8 — cross-page pattern array)

    2. Append after the CausalEdge model:

    Model CrawledPage: id String @id @default(cuid()), resultId String, result Result @relation(fields: [resultId], references: [id]), url String, page_index Int, narrative Json, screenshot_url String?, tech_stack Json?, issues CrawledPageIssue[], edges CrawledPageEdge[], created_at DateTime @default(now()), @@index([resultId])

    Model CrawledPageIssue: id String @id @default(cuid()), crawledPageId String, crawledPage CrawledPage @relation(fields: [crawledPageId], references: [id]), category String, signal_source String, severity Int, raw_evidence String, technical_description String, fix_suggestion String @default(""), severity_justification String @default(""), causedBy CrawledPageEdge[] @relation("ToCrawledPageIssue"), causes CrawledPageEdge[] @relation("FromCrawledPageIssue")
    Add comment above: // SYNC: keep in sync with Issue model

    Model CrawledPageEdge: id String @id @default(cuid()), crawledPageId String, crawledPage CrawledPage @relation(fields: [crawledPageId], references: [id]), fromIssueId String, fromIssue CrawledPageIssue @relation("FromCrawledPageIssue", fields: [fromIssueId], references: [id]), toIssueId String, toIssue CrawledPageIssue @relation("ToCrawledPageIssue", fields: [toIssueId], references: [id]), relationship String, confidence String, mechanism String (NON-NULLABLE — same enforcement as CausalEdge per D-17), explanation String
    Add comment above: // SYNC: keep in sync with CausalEdge model

    Then copy the entire updated root schema into crawler/prisma/schema.prisma, preserving the two header differences:
    - generator output: "../src/generated/prisma" (not "../../src/generated/prisma")
    - datasource url: env("DATABASE_URL") (crawler has no prisma.config.ts)

    After both files are written, run the migration and regenerate clients:
    - cd /c/Users/STL-LT-068/Desktop/feeltrace && npm run db:migrate
      (when prompted for a migration name, enter: phase8_crawled_page)
    - npx prisma generate (in repo root — regenerates src/generated/prisma)
    - cd crawler && npx prisma generate (in crawler/ — regenerates crawler/src/generated/prisma)

    If npm run db:migrate requires interactive input, the task must pause and note: "Migration requires interactive confirmation — executor should run npm run db:migrate in terminal and enter migration name 'phase8_crawled_page' when prompted."
  </action>
  <verify>
    <automated>grep -c "model CrawledPage" /c/Users/STL-LT-068/Desktop/feeltrace/prisma/schema.prisma && grep -c "model CrawledPageIssue" /c/Users/STL-LT-068/Desktop/feeltrace/prisma/schema.prisma && grep -c "model CrawledPageEdge" /c/Users/STL-LT-068/Desktop/feeltrace/prisma/schema.prisma && grep -c "cross_page_patterns" /c/Users/STL-LT-068/Desktop/feeltrace/prisma/schema.prisma && grep -c "crawledPages" /c/Users/STL-LT-068/Desktop/feeltrace/prisma/schema.prisma</automated>
    <automated>ls /c/Users/STL-LT-068/Desktop/feeltrace/prisma/migrations/ | grep phase8</automated>
    <automated>grep -c "model CrawledPage" /c/Users/STL-LT-068/Desktop/feeltrace/crawler/prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains exactly 1 "model CrawledPage", 1 "model CrawledPageIssue", 1 "model CrawledPageEdge"
    - prisma/schema.prisma contains "cross_page_patterns Json?" on Result model
    - prisma/schema.prisma contains "crawledPages   CrawledPage[]" on Result model
    - crawler/prisma/schema.prisma contains identical new models; generator output is "../src/generated/prisma"; datasource url field is "env("DATABASE_URL")"
    - prisma/migrations/ contains a new directory with "phase8_crawled_page" in its name
    - The migration .sql file inside that directory contains CREATE TABLE statements for "CrawledPage", "CrawledPageIssue", "CrawledPageEdge" and ALTER TABLE for Result
    - npm run db:migrate exits 0 (migration applied to Neon DB)
    - npx prisma generate exits 0 in both root and crawler/
  </acceptance_criteria>
  <done>CrawledPage, CrawledPageIssue, CrawledPageEdge models exist in both schemas; named migration file created and applied; Prisma client regenerated in both locations</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 8 type contracts to crawler/src/pipeline/types.ts and crawler/src/lib/types.ts</name>
  <files>crawler/src/pipeline/types.ts, crawler/src/lib/types.ts</files>
  <read_first>
    - crawler/src/pipeline/types.ts (read entire file — append new types after PERMITTED_MECHANISMS const)
    - crawler/src/lib/types.ts (read entire file — locate CrawlPass interface to add internalLinks field)
    - crawler/src/pipeline/page-type-detector.ts (read first 20 lines — confirm PageType export name for PipelineResult type)
  </read_first>
  <action>
    In crawler/src/pipeline/types.ts, append four new interfaces after the PERMITTED_MECHANISMS const. Add a comment "// Phase 8: multi-page crawl types" before the block.

    PipelineResult interface: fields enrichedIssues EnrichedIssue[], edges CausalEdgeCandidate[], narrative NarrativeResult, screenshotUrl string | null, techProfile TechProfile (import TechProfile from '../lib/types'), pageType (import PageType from './page-type-detector'). Export the interface.

    PageAnalysisResult interface: fields url string, pageIndex number, enrichedIssues EnrichedIssue[], edges CausalEdgeCandidate[], narrative NarrativeResult, screenshotUrl string | null, techProfile TechProfile, pageType (same PageType import), discoveredLinks string[] (populated for page 0 root; empty array for subsequent pages; comment this). Export the interface.

    CrossPagePattern interface: fields signal_source string, page_count number, worst_severity number, affected_urls string[], representative_evidence string. Export the interface.

    SiteWideNarrative interface: fields narrative NarrativeResult, crossPagePatterns CrossPagePattern[]. Export the interface.

    In crawler/src/lib/types.ts, add one optional field to the CrawlPass interface after the existing axeViolations field:
    - internalLinks?: string[]
    Add inline comment: // Phase 8 — desktop pass only; populated by extractInternalLinks() in browser.ts

    Import additions for types.ts: add "import type { TechProfile } from '../lib/types'" and "import type { PageType } from './page-type-detector'" at the top of crawler/src/pipeline/types.ts if not already present. Use import type to avoid circular runtime dependencies.
  </action>
  <verify>
    <automated>cd /c/Users/STL-LT-068/Desktop/feeltrace/crawler && npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
    <automated>grep -n "PipelineResult\|PageAnalysisResult\|CrossPagePattern\|SiteWideNarrative" /c/Users/STL-LT-068/Desktop/feeltrace/crawler/src/pipeline/types.ts</automated>
    <automated>grep -n "internalLinks" /c/Users/STL-LT-068/Desktop/feeltrace/crawler/src/lib/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - crawler/src/pipeline/types.ts exports PipelineResult, PageAnalysisResult, CrossPagePattern, SiteWideNarrative interfaces
    - PageAnalysisResult has fields: url string, pageIndex number, enrichedIssues EnrichedIssue[], edges CausalEdgeCandidate[], narrative NarrativeResult, screenshotUrl string | null, techProfile TechProfile, pageType PageType, discoveredLinks string[]
    - SiteWideNarrative has fields: narrative NarrativeResult, crossPagePatterns CrossPagePattern[]
    - crawler/src/lib/types.ts CrawlPass interface contains "internalLinks?: string[]"
    - npx tsc --noEmit in crawler/ exits 0 (no new type errors introduced)
  </acceptance_criteria>
  <done>All four Phase 8 type interfaces exported from types.ts; CrawlPass.internalLinks? added to lib/types.ts; no TypeScript errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema → DB | Migration runs against live Neon DB; no user-controlled input at this stage |
| Type definitions → Compiler | Types are structural contracts; no runtime trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | prisma/schema.prisma — CrawledPageEdge.mechanism | mitigate | mechanism field is NON-NULLABLE (no @default) — mirrors CausalEdge.mechanism (D-17); Prisma rejects any create without explicit mechanism value |
| T-08-02 | Tampering | Result.cross_page_patterns Json? | accept | Nullable JSON column; value is written only by internal crawler pipeline, not user input; no injection surface |
| T-08-03 | DoS | db:migrate against live Neon DB | accept | Migration adds new tables and columns only; no destructive operations on existing rows; backward compatible |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | Phase 8 installs no new packages (confirmed in RESEARCH.md Package Legitimacy Audit); no slopcheck needed |
</threat_model>

<verification>
1. prisma/schema.prisma contains CrawledPage, CrawledPageIssue, CrawledPageEdge models and Result.crawledPages + Result.cross_page_patterns
2. crawler/prisma/schema.prisma is identical to root except generator output path and datasource url
3. prisma/migrations/ contains a new migration directory with phase8_crawled_page in the name
4. crawler/src/pipeline/types.ts exports PipelineResult, PageAnalysisResult, CrossPagePattern, SiteWideNarrative
5. crawler/src/lib/types.ts CrawlPass has internalLinks?: string[]
6. npx tsc --noEmit in crawler/ passes
</verification>

<success_criteria>
- Migration applied: npm run db:migrate exits 0; migration file exists in prisma/migrations/
- Prisma clients regenerated in both root (src/generated/prisma) and crawler (crawler/src/generated/prisma)
- All four new type interfaces exported and TypeScript-valid
- CrawlPass.internalLinks? present; no existing field removed
- No regression: existing CausalEdge.mechanism non-nullable constraint preserved
</success_criteria>

<output>
Create `.planning/phases/08-multi-page-crawl/08-01-SUMMARY.md` when done
</output>
