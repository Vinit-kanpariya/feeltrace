---
phase: 08-multi-page-crawl
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - crawler/src/browser.test.ts
  - crawler/src/pipeline/site-wide-merger.test.ts
  - src/app/results/[jobId]/page.test.tsx
autonomous: true
requirements: [CRAWL-01, CRAWL-02, CRAWL-03]

must_haves:
  truths:
    - "browser.test.ts test file exists and all tests fail (RED state — extractInternalLinks not yet implemented)"
    - "site-wide-merger.test.ts exists and detectCrossPagePatterns tests fail; runSiteWideAnalysis tests fail"
    - "Test files exercise all behaviors listed in RESEARCH.md Validation Architecture"
    - "page.test.tsx exists with two failing test cases for accordion rendering (RED state — PageAccordionSection not yet implemented)"
  artifacts:
    - path: "crawler/src/browser.test.ts"
      provides: "Unit tests for extractInternalLinks — same-origin filter, dedup, normalization, exclusions"
      exports: []
    - path: "crawler/src/pipeline/site-wide-merger.test.ts"
      provides: "Unit tests for detectCrossPagePatterns and runSiteWideAnalysis"
      exports: []
    - path: "src/app/results/[jobId]/page.test.tsx"
      provides: "Unit test stubs for results page accordion — crawledPages > 0 renders accordion, crawledPages === 0 does not"
      exports: []
  key_links:
    - from: "crawler/src/browser.test.ts"
      to: "crawler/src/browser.ts"
      via: "import { extractInternalLinks } from './browser'"
      pattern: "extractInternalLinks"
    - from: "crawler/src/pipeline/site-wide-merger.test.ts"
      to: "crawler/src/pipeline/site-wide-merger.ts"
      via: "import { detectCrossPagePatterns, runSiteWideAnalysis } from './site-wide-merger'"
      pattern: "detectCrossPagePatterns|runSiteWideAnalysis"
    - from: "src/app/results/[jobId]/page.test.tsx"
      to: "src/components/PageAccordionSection.tsx"
      via: "PageAccordionSection rendered conditionally when crawledPages.length > 0"
      pattern: "PageAccordionSection"
---

<objective>
Create failing unit test stubs for extractInternalLinks (browser.ts), detectCrossPagePatterns + runSiteWideAnalysis (site-wide-merger.ts), and the results page accordion (page.tsx). These tests define the behavioral contract before implementation, ensuring Wave 2+ code is written to pass specific assertions.

Purpose: Nyquist Rule compliance — every verify block in implementation plans points to these test files. Tests must exist in RED state before implementation begins so the executor can run them to confirm GREEN after writing code.

Output: Three new test files; zero implementation files touched. Tests import from not-yet-created modules and will fail with import errors initially — this is expected and correct RED state.
</objective>

<execution_context>
@/c/Users/STL-LT-068/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/STL-LT-068/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-multi-page-crawl/08-RESEARCH.md
@.planning/phases/08-multi-page-crawl/08-PATTERNS.md

<interfaces>
<!-- From 08-PATTERNS.md: browser.test.ts pattern -->
Analog: crawler/src/pipeline/stage1-scorer.test.ts (pure function fixture pattern)
File header: // @vitest-environment node
Mock page.evaluate: makeMockPage(hrefs: (string | null)[]) returns { evaluate: vi.fn().mockResolvedValue(hrefs) }

<!-- From 08-PATTERNS.md: site-wide-merger.test.ts pattern -->
Analog: crawler/src/pipeline/stage3-narrator.test.ts (structure) + stage1-5-vision-scanner.test.ts (mock Groq)
makePageResult helper: builds PageAnalysisResult with overrides for enrichedIssues

<!-- PageAnalysisResult type (from 08-01-PLAN types.ts) -->
interface PageAnalysisResult {
  url: string; pageIndex: number; enrichedIssues: EnrichedIssue[]; edges: CausalEdgeCandidate[];
  narrative: NarrativeResult; screenshotUrl: string | null; techProfile: TechProfile;
  pageType: PageType; discoveredLinks: string[];
}

<!-- CrossPagePattern type (from 08-01-PLAN types.ts) -->
interface CrossPagePattern {
  signal_source: string; page_count: number; worst_severity: number;
  affected_urls: string[]; representative_evidence: string;
}

<!-- SiteWideNarrative type (from 08-01-PLAN types.ts) -->
interface SiteWideNarrative {
  narrative: NarrativeResult; crossPagePatterns: CrossPagePattern[];
}

<!-- Results page accordion test pattern (from src/components/NarrativeSection.test.tsx) -->
Analog: src/components/NarrativeSection.test.tsx (React Testing Library render + screen.getByText)
Import style: import { describe, it, expect } from 'vitest'; import { render, screen } from '@testing-library/react'
PageAccordionSection import: import { PageAccordionSection } from '@/components/PageAccordionSection' (RED — not yet created)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create crawler/src/browser.test.ts with extractInternalLinks unit tests</name>
  <files>crawler/src/browser.test.ts</files>
  <read_first>
    - crawler/src/pipeline/stage1-scorer.test.ts (read entire file — copy fixture pattern and test structure conventions)
    - crawler/src/pipeline/stage3-narrator.test.ts (read first 30 lines — confirm vitest import style used in this codebase)
    - crawler/vitest.config.ts (read entire file — confirm test file discovery glob to ensure browser.test.ts is picked up)
  </read_first>
  <action>
    Create crawler/src/browser.test.ts. The file must:

    1. Start with // @vitest-environment node on line 1
    2. Import: import { describe, it, expect, vi } from 'vitest'
    3. Import: import { extractInternalLinks } from './browser' (this import will fail until Plan 08-03 is implemented — that is intentional RED state)

    4. Define a makeMockPage helper that accepts hrefs: (string | null)[] and returns an object with evaluate: vi.fn().mockResolvedValue(hrefs). The Page type from playwright-core is not imported — use a minimal structural type cast (as Parameters<typeof extractInternalLinks>[0]).

    5. Write a describe('extractInternalLinks') block containing exactly these test cases:
       - 'CRAWL-01: returns only same-origin links' — given base https://example.com and hrefs ['/about', 'https://other.com/page', 'https://example.com/contact'], expect result to contain 'https://example.com/about' and 'https://example.com/contact' but not 'https://other.com/page'
       - 'CRAWL-01: excludes fragment-only hrefs' — hrefs ['#section', '#top'], expect empty array
       - 'CRAWL-01: excludes mailto: and tel: hrefs' — hrefs ['mailto:hi@example.com', 'tel:+1234567890'], expect empty array
       - 'CRAWL-01: deduplicates URLs' — hrefs ['/about', '/about', '/about/'], expect result.length to equal 1 (trailing slash normalization makes /about/ and /about the same)
       - 'CRAWL-01: normalizes relative paths to absolute' — hrefs ['./pricing', '../blog'], base https://example.com/app/, expect result to include 'https://example.com/app/pricing' (or equivalent resolved URL — use new URL('./pricing', 'https://example.com/app/').href to compute expected value in the test)
       - 'CRAWL-01: skips malformed hrefs without throwing' — hrefs ['not a url at all', '://bad', null], expect no throw and result to be an array (possibly empty)
  </action>
  <verify>
    <automated>cd /c/Users/STL-LT-068/Desktop/feeltrace && npm test -- --reporter=dot crawler/src/browser.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File crawler/src/browser.test.ts exists
    - File starts with // @vitest-environment node on line 1
    - File imports extractInternalLinks from './browser'
    - describe block contains at least 6 test cases covering: same-origin filter, fragment exclusion, mailto/tel exclusion, deduplication, relative path normalization, malformed href safety
    - Running the test file produces FAIL or import error (not a parse error) — RED state is correct
    - No syntax errors: npx tsc --noEmit on the test file alone should show only "Cannot find module './browser'" type errors, not structural TypeScript errors
  </acceptance_criteria>
  <done>browser.test.ts exists with 6+ test cases in RED state; all tests define clear behavioral expectations for extractInternalLinks</done>
</task>

<task type="auto">
  <name>Task 2: Create crawler/src/pipeline/site-wide-merger.test.ts with pattern detection and LLM merge tests</name>
  <files>crawler/src/pipeline/site-wide-merger.test.ts</files>
  <read_first>
    - crawler/src/pipeline/stage3-narrator.test.ts (read entire file — use as structural template for LLM mock pattern)
    - crawler/src/pipeline/stage1-5-vision-scanner.test.ts (read entire file — use vi.mock('groq-sdk') pattern for Groq mocking)
    - crawler/src/pipeline/types.ts (read entire file — use actual type shapes for PageAnalysisResult fixture builder)
  </read_first>
  <action>
    Create crawler/src/pipeline/site-wide-merger.test.ts. The file must:

    1. Start with // @vitest-environment node on line 1
    2. Imports: import { describe, it, expect, vi, beforeEach } from 'vitest'
    3. Import: import { detectCrossPagePatterns, runSiteWideAnalysis } from './site-wide-merger' (RED — module doesn't exist yet)
    4. Import: import type { PageAnalysisResult } from './types'

    5. Define makePageResult(url: string, pageIndex: number, overrides?: { enrichedIssues?: PageAnalysisResult['enrichedIssues'] }): PageAnalysisResult helper. Returns a minimal PageAnalysisResult with url, pageIndex, enrichedIssues (default []), edges [], narrative { summary: 'Summary for ${url}', perceivedPerformance: '', technicalPerformance: '', recommendations: [] }, screenshotUrl null, techProfile {} as any, pageType 'unknown' as any, discoveredLinks [].

    6. Write describe('detectCrossPagePatterns') with:
       - 'CRAWL-02: returns patterns for signal_sources on >= minPages pages' — create 3 PageAnalysisResult fixtures each with an enrichedIssue with signal_source 'networkSignals.renderBlockingCount' and severity 3; call detectCrossPagePatterns(pages, 3); expect result to have length 1 and result[0].signal_source === 'networkSignals.renderBlockingCount'
       - 'CRAWL-02: returns empty array when threshold not met' — 2 pages with same signal_source, minPages=3; expect empty array
       - 'CRAWL-02: sorts by worst_severity descending' — 3 pages with signal_source A (max severity 3) and 3 pages with signal_source B (max severity 4); expect result[0].signal_source === B (severity 4 first)
       - 'CRAWL-02: includes affected_urls in result' — 3 pages, each with the same signal_source; expect result[0].affected_urls to have length 3

    7. Write describe('runSiteWideAnalysis') with:
       - 'returns per-page narrative directly when pageResults.length === 1' — pass a single PageAnalysisResult with a known narrative; expect return.narrative === pageResult.narrative and return.crossPagePatterns to be an empty array. No Groq mock needed for this test.
       - 'returns SiteWideNarrative with crossPagePatterns array' — mock Groq using vi.mock('groq-sdk') following the stage1-5-vision-scanner.test.ts pattern; pass 2 PageAnalysisResult fixtures; expect the return type to have a narrative field and a crossPagePatterns field (both present).

    For the Groq mock: copy the vi.mock('groq-sdk') approach from stage1-5-vision-scanner.test.ts exactly — mock chat.completions.create to return a choices[0].message.content with a minimal [SUMMARY] / [PERCEIVED] / [TECHNICAL] / [RECOMMENDATIONS] block (same format as stage3-narrator output) so parseNarrativeOutput can parse it.
  </action>
  <verify>
    <automated>cd /c/Users/STL-LT-068/Desktop/feeltrace && npm test -- --reporter=dot crawler/src/pipeline/site-wide-merger.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File crawler/src/pipeline/site-wide-merger.test.ts exists
    - File imports detectCrossPagePatterns and runSiteWideAnalysis from './site-wide-merger'
    - describe('detectCrossPagePatterns') contains at least 4 test cases covering threshold, empty result, sorting, affected_urls
    - describe('runSiteWideAnalysis') contains at least 2 test cases: single-page shortcut and multi-page with Groq mock
    - Running the test file produces FAIL or import error — RED state is correct
    - makePageResult helper is present and typed correctly against PageAnalysisResult
  </acceptance_criteria>
  <done>site-wide-merger.test.ts exists with 6+ test cases in RED state; detectCrossPagePatterns and runSiteWideAnalysis behavioral contracts defined before implementation</done>
</task>

<task type="auto">
  <name>Task 3: Create src/app/results/[jobId]/page.test.tsx with accordion rendering stubs</name>
  <files>src/app/results/[jobId]/page.test.tsx</files>
  <read_first>
    - src/components/NarrativeSection.test.tsx (read entire file — copy RTL render + screen pattern and import style)
    - src/components/IssueCard.test.tsx (read entire file — confirm fixture shape and any mock patterns for complex props)
    - src/app/results/[jobId]/page.tsx (read first 60 lines — understand the component export name and imports to stub correctly)
  </read_first>
  <action>
    Create src/app/results/[jobId]/page.test.tsx. This file defines CRAWL-03 behavioral contracts for the per-page accordion. The PageAccordionSection component does not exist yet (created in Plan 08-06) — imports will fail in RED state, which is correct.

    The file must:

    1. Start with a comment block (no @vitest-environment directive needed — jsdom environment is set in root vitest.config.ts for src/ tests):
       // src/app/results/[jobId]/page.test.tsx
       // Wave 0 RED stubs for CRAWL-03 — contracts for per-page accordion rendering.
       // These tests WILL fail until src/components/PageAccordionSection.tsx is created in Plan 08-06.

    2. Imports:
       import { describe, it, expect, vi } from 'vitest'
       import { render, screen } from '@testing-library/react'
       import { PageAccordionSection } from '@/components/PageAccordionSection'

    3. Define a fixture for a minimal page object that matches PageAccordionSectionProps:
       const fixturePage = {
         id: 'page-1',
         url: 'https://example.com/about',
         page_index: 0,
         narrative: { summary: 'About page summary', perceivedPerformance: 'Fast', technicalPerformance: 'Efficient', recommendations: [] },
         screenshot_url: null,
         issues: [
           { id: 'issue-1', category: 'performance', signal_source: 'networkSignals.renderBlockingCount', severity: 3, raw_evidence: 'Blocking scripts found', technical_description: 'Render-blocking JS', fix_suggestion: 'Defer scripts', severity_justification: 'High impact' }
         ],
         edges: [],
       }

    4. Write describe('PageAccordionSection') containing two test cases:

       Test 1 — 'CRAWL-03: renders accordion section when crawledPages.length > 0':
       - render(<PageAccordionSection page={fixturePage} defaultOpen={true} />)
       - expect(screen.getByText('https://example.com/about')).toBeDefined()
       - This confirms the component renders the page URL when provided. defaultOpen={true} ensures accordion content is visible without user interaction.

       Test 2 — 'CRAWL-03: does not render accordion when crawledPages.length === 0 (backward compat)':
       - This test verifies the guard in page.tsx, not PageAccordionSection itself. Render a wrapper that conditionally renders PageAccordionSection:
         const crawledPages: typeof fixturePage[] = []
         const { container } = render(
           <div>
             {crawledPages.length > 0 && <PageAccordionSection page={crawledPages[0]} />}
           </div>
         )
         expect(container.querySelector('[data-testid="accordion"]') ?? null).toBeNull()
       - This asserts that when crawledPages is empty, no accordion content is rendered — preserving backward compatibility for historical single-URL jobs.

    Note: Both tests will fail with "Cannot find module '@/components/PageAccordionSection'" until Plan 08-06 creates the component. This is the intended RED state.
  </action>
  <verify>
    <automated>cd /c/Users/STL-LT-068/Desktop/feeltrace && npm test -- --reporter=dot "src/app/results" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File src/app/results/[jobId]/page.test.tsx exists
    - File imports PageAccordionSection from '@/components/PageAccordionSection'
    - describe('PageAccordionSection') contains exactly 2 test cases: accordion renders when crawledPages > 0, no accordion when crawledPages === 0
    - Running the test file produces FAIL or import error — RED state is correct
    - No syntax errors in the test file itself
  </acceptance_criteria>
  <done>page.test.tsx exists with 2 CRAWL-03 test stubs in RED state; accordion rendering contracts defined before PageAccordionSection is implemented in Plan 08-06</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → implementation | Tests define expected behavior contracts; no external trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | vi.mock('groq-sdk') in test — mock returns controlled data | accept | Test-only mock; no production path; Groq mock returns static safe string |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
1. crawler/src/browser.test.ts exists and has 6+ it() blocks in a describe('extractInternalLinks') block
2. crawler/src/pipeline/site-wide-merger.test.ts exists with describe('detectCrossPagePatterns') and describe('runSiteWideAnalysis') blocks
3. src/app/results/[jobId]/page.test.tsx exists with describe('PageAccordionSection') containing 2 test cases for CRAWL-03
4. All three test files are syntactically valid TypeScript (no parse errors from tsc)
5. All three test files are in RED state (fail due to missing module imports — expected)
</verification>

<success_criteria>
- Three test files created; no implementation files modified
- browser.test.ts tests cover: same-origin filter, fragment exclusion, mailto/tel exclusion, dedup, relative normalization, malformed href safety
- site-wide-merger.test.ts tests cover: threshold filtering, empty array case, severity sorting, affected_urls, single-page shortcut, multi-page with mock
- page.test.tsx tests cover: accordion renders when crawledPages.length > 0; no accordion rendered when crawledPages.length === 0
- Tests fail with import errors (not parse errors) — confirms RED state is syntactically valid
</success_criteria>

<output>
Create `.planning/phases/08-multi-page-crawl/08-02-SUMMARY.md` when done
</output>
