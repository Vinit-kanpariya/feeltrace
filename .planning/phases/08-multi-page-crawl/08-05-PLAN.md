---
phase: 08-multi-page-crawl
plan: 05
type: execute
wave: 3
depends_on: [08-03, 08-04]
files_modified:
  - crawler/src/processor.ts
autonomous: true
requirements: [CRAWL-01, CRAWL-02]

must_haves:
  truths:
    - "processJob crawls the root URL and discovers internal links from desktop.internalLinks"
    - "processJob crawls up to MAX_CRAWL_PAGES pages sequentially"
    - "processJob excludes the root URL from discovered additional pages (no duplicate crawl)"
    - "Per-page failures are non-blocking: if page N fails, pages N+1..MAX continue"
    - "runAIPipeline is called with mode='multi' for each page; no DB writes happen per-page"
    - "runSiteWideAnalysis is called once after all per-page results are collected"
    - "A single atomic prisma.$transaction writes one Result (site-wide) + N CrawledPage records"
    - "If 0 pages succeed, job is marked failed; if 1+ pages succeed, job is marked complete"
    - "MAX_CRAWL_PAGES defaults to 5 via process.env.MAX_CRAWL_PAGES; hard cap is 10"
    - "Per-page timeout is 90s; total crawl timeout is 8 minutes"
  artifacts:
    - path: "crawler/src/processor.ts"
      provides: "Multi-page loop orchestration with per-page isolation and atomic DB write"
      exports: ["processJob"]
  key_links:
    - from: "crawler/src/processor.ts"
      to: "crawler/src/browser.ts runDualViewportCrawl"
      via: "crawlAndAnalyzePage inner function calls runDualViewportCrawl"
      pattern: "runDualViewportCrawl"
    - from: "crawler/src/processor.ts"
      to: "crawler/src/pipeline/run-pipeline.ts runAIPipeline"
      via: "mode: 'multi' call per page"
      pattern: "mode.*multi"
    - from: "crawler/src/processor.ts"
      to: "crawler/src/pipeline/site-wide-merger.ts runSiteWideAnalysis"
      via: "called once after all pages complete"
      pattern: "runSiteWideAnalysis"
    - from: "crawler/src/processor.ts writeCrawlResults"
      to: "prisma.$transaction"
      via: "single atomic write: Result + CrawledPage[]"
      pattern: "prisma\\.\\$transaction"
---

<objective>
Replace the single-URL processJob body with a multi-page loop that crawls up to MAX_CRAWL_PAGES sequentially, aggregates per-page PipelineResult objects, calls Stage 4 site-wide merge, and writes everything in one atomic transaction.

Purpose: This is the orchestration hub of the entire multi-page feature. All the building blocks (extractInternalLinks, runAIPipeline mode='multi', runSiteWideAnalysis) are ready; processor.ts assembles them into the end-to-end flow.

Output: processor.ts fully rewritten with multi-page loop, withTimeout helper, crawlAndAnalyzePage helper, and writeCrawlResults function.
</objective>

<execution_context>
@/c/Users/STL-LT-068/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/STL-LT-068/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-multi-page-crawl/08-RESEARCH.md
@.planning/phases/08-multi-page-crawl/08-PATTERNS.md
@.planning/phases/07-ai-pipeline-depth/07-04-SUMMARY.md

<interfaces>
<!-- Current processor.ts — understand what to replace vs preserve -->
Current imports: prisma, runDualViewportCrawl, runAIPipeline, fetchPSISignals
Current constants: SLA_MS = 55_000
Current processJob: idempotency check lines 10-18; crawl+PSI race lines 28-36; analyze lines 45-48; error handler lines 52-59

<!-- runDualViewportCrawl return type (browser.ts lines 208-256) -->
Returns: { mobile: CrawlPass; desktop: CrawlPass; screenshot: Buffer | null; techProfile: TechProfile }
After 08-03: desktop.internalLinks is populated with discovered same-origin links

<!-- runAIPipeline new signature (after 08-04) -->
runAIPipeline(jobId, signals, screenshot, techProfile, externalSignals, mode: 'multi') returns Promise<PipelineResult>

<!-- runSiteWideAnalysis signature (after 08-04) -->
runSiteWideAnalysis(client: Groq, pageResults: PageAnalysisResult[]): Promise<SiteWideNarrative>

<!-- Prisma schema additions (after 08-01) -->
CrawledPage: id, resultId, result, url, page_index, narrative Json, screenshot_url String?, tech_stack Json?, issues CrawledPageIssue[], edges CrawledPageEdge[], created_at
CrawledPageIssue: id, crawledPageId, crawledPage, category, signal_source, severity, raw_evidence, technical_description, fix_suggestion String @default(""), severity_justification String @default("")
CrawledPageEdge: id, crawledPageId, crawledPage, fromIssueId, fromIssue, toIssueId, toIssue, relationship, confidence, mechanism (non-nullable), explanation
Result: now has crawledPages CrawledPage[] and cross_page_patterns Json?

<!-- getGroqClient — already in codebase -->
import { getGroqClient } from './lib/groq-client'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite processor.ts with multi-page loop and atomic DB write</name>
  <files>crawler/src/processor.ts</files>
  <read_first>
    - crawler/src/processor.ts (read entire file — note every import and the exact idempotency check + error handler pattern to preserve verbatim)
    - crawler/src/pipeline/run-pipeline.ts (read lines 1-50 — confirm new signature with mode param)
    - crawler/src/pipeline/site-wide-merger.ts (read entire file — confirm runSiteWideAnalysis accepts Groq client as first arg)
    - crawler/src/pipeline/types.ts (read entire file — confirm PageAnalysisResult and SiteWideNarrative shapes for writeCrawlResults)
  </read_first>
  <action>
    Replace processor.ts entirely. Preserve: the existing idempotency check pattern (lines 10-18), the error handler pattern (lines 52-59), and the import for prisma.

    New imports to add: runSiteWideAnalysis from './pipeline/site-wide-merger', getGroqClient from './lib/groq-client', and type imports for PageAnalysisResult and SiteWideNarrative from './pipeline/types'.

    New constants (replace SLA_MS = 55_000):
    - const MAX_PAGES = Math.min(parseInt(process.env.MAX_CRAWL_PAGES ?? '5', 10), 10) — hard cap at 10
    - const PER_PAGE_TIMEOUT_MS = 90_000
    - const TOTAL_CRAWL_TIMEOUT_MS = 480_000

    Module-level withTimeout helper (non-exported):
    - function withTimeout<T>(promise: Promise<T>, ms: number, message: string): Promise<T> — uses Promise.race with a setTimeout reject

    Inner helper crawlAndAnalyzePage (non-exported, async):
    - Parameters: jobId: string, pageUrl: string, pageIndex: number
    - Returns: Promise<PageAnalysisResult>
    - Body: call runDualViewportCrawl(pageUrl, jobId) and fetchPSISignals(pageUrl, 30_000) in parallel via Promise.all; destructure { mobile, desktop, screenshot, techProfile } from crawl result; call runAIPipeline(jobId, { mobile, desktop }, screenshot, techProfile, psiResult, 'multi') to get PipelineResult; return PageAnalysisResult: { url: pageUrl, pageIndex, enrichedIssues: pipelineResult.enrichedIssues, edges: pipelineResult.edges, narrative: pipelineResult.narrative, screenshotUrl: pipelineResult.screenshotUrl, techProfile: pipelineResult.techProfile, pageType: pipelineResult.pageType, discoveredLinks: desktop.internalLinks ?? [] }

    Inner helper writeCrawlResults (non-exported, async):
    - Parameters: jobId: string, allPageResults: PageAnalysisResult[], siteWide: SiteWideNarrative
    - Returns: Promise<void>
    - Body: use prisma.$transaction(async (tx) => { ... }) to:
      1. Cast siteWide.narrative and siteWide.crossPagePatterns to Prisma Json types via unknown intermediate
      2. Create the Result record with tx.result.create: { data: { jobId, narrative: narrativeJson, screenshot_url: allPageResults[0]?.screenshotUrl ?? null, tech_stack: allPageResults[0]?.techProfile as unknown as ..., cross_page_patterns: crossPagePatternsJson } }
      3. For each pageResult in allPageResults: tx.crawledPage.create with data: { resultId: result.id, url: pageResult.url, page_index: pageResult.pageIndex, narrative: pageResult.narrative as unknown as ..., screenshot_url: pageResult.screenshotUrl, tech_stack: pageResult.techProfile as unknown as ..., issues: { create: pageResult.enrichedIssues.map(i => ({ category, signal_source, severity, raw_evidence, technical_description, fix_suggestion: i.fix_suggestion, severity_justification: i.severity_justification })) } }, include: { issues: { orderBy: { id: 'asc' } } } — capture the returned page to get issue IDs for edges
      4. For each crawledPage result, create CrawledPageEdge records via tx.crawledPageEdge.createMany with the same fromIndex/toIndex → issue ID mapping pattern as the existing CausalEdge logic in run-pipeline.ts (build issueIdByPosition Map, validate bounds, throw on out-of-bounds index)

    New processJob body — preserve the idempotency check (lines 10-18) unchanged. Replace everything from line 20 onwards:
    - const startedAt = Date.now()
    - const allPageResults: PageAnalysisResult[] = []
    - try block:
      - prisma.job.update status 'crawling'
      - rootResult = await withTimeout(crawlAndAnalyzePage(jobId, url, 0), PER_PAGE_TIMEOUT_MS, message)
      - allPageResults.push(rootResult)
      - const normalizedRoot = url.split('#')[0].replace(/\/$/, '')
      - const additionalUrls = (rootResult.discoveredLinks).filter(u => u !== normalizedRoot).slice(0, MAX_PAGES - 1)
      - for loop over additionalUrls: try { const pageResult = await withTimeout(crawlAndAnalyzePage(jobId, additionalUrls[i], i + 1), PER_PAGE_TIMEOUT_MS, message); allPageResults.push(pageResult) } catch (pageErr) { console.warn('[processor] Job ' + jobId + ': page ' + additionalUrls[i] + ' failed — continuing', pageErr) }
      - if (allPageResults.length === 0): throw new Error('All pages failed — no results to write')
      - prisma.job.update status 'analyzing'
      - const client = getGroqClient()
      - const siteWide = await runSiteWideAnalysis(client, allPageResults)
      - await writeCrawlResults(jobId, allPageResults, siteWide)
      - prisma.job.update status 'complete'
      - console.log '[processor] Job ' + jobId + ' completed in ' + (Date.now() - startedAt) + 'ms — ' + allPageResults.length + ' pages'
    - catch block: copy existing error handler verbatim (message.slice(0, 500), status: 'failed')

    Note: The 8-minute total timeout (TOTAL_CRAWL_TIMEOUT_MS) is defined but NOT wrapped around the loop — individual per-page timeouts (PER_PAGE_TIMEOUT_MS = 90s × MAX_PAGES = 450s max) provide the effective budget. The TOTAL_CRAWL_TIMEOUT_MS constant is declared for documentation and future use.
  </action>
  <verify>
    <automated>cd /c/Users/STL-LT-068/Desktop/feeltrace/crawler && npx tsc --noEmit 2>&1 | head -20</automated>
    <automated>cd /c/Users/STL-LT-068/Desktop/feeltrace && npm test -- --reporter=dot 2>&1 | grep -E "Tests:|FAIL" | tail -5</automated>
    <automated>grep -n "MAX_PAGES\|PER_PAGE_TIMEOUT_MS\|runSiteWideAnalysis\|writeCrawlResults\|withTimeout\|crawlAndAnalyzePage" /c/Users/STL-LT-068/Desktop/feeltrace/crawler/src/processor.ts</automated>
  </verify>
  <acceptance_criteria>
    - processor.ts imports: runSiteWideAnalysis, getGroqClient, PageAnalysisResult type
    - processor.ts has const MAX_PAGES = Math.min(parseInt(process.env.MAX_CRAWL_PAGES ?? '5', 10), 10)
    - processor.ts has const PER_PAGE_TIMEOUT_MS = 90_000
    - processor.ts defines withTimeout helper function
    - processor.ts defines crawlAndAnalyzePage helper function that calls runAIPipeline with mode: 'multi'
    - processor.ts defines writeCrawlResults that uses prisma.$transaction with CrawledPage nested creates
    - processJob calls crawlAndAnalyzePage for root URL, filters discovered links excluding rootUrl, loops over additional URLs with per-page try/catch
    - processJob calls runSiteWideAnalysis(client, allPageResults) after the loop
    - processJob calls writeCrawlResults for the atomic DB write
    - npx tsc --noEmit in crawler/ exits 0
    - Full test suite passes (npm test exits 0; no regressions)
  </acceptance_criteria>
  <done>processor.ts rewrites with multi-page loop; per-page isolation; Stage 4 call; atomic DB write for Result + N CrawledPage records; TypeScript compiles; tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MAX_CRAWL_PAGES env var → page count | Env var controls how many pages are crawled |
| Discovered URLs → crawlAndAnalyzePage | Each discovered link URL is passed to Playwright; potential SSRF |
| allPageResults → writeCrawlResults → Prisma DB | Aggregated pipeline output written to Neon |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Tampering (SSRF) | additionalUrls fed to crawlAndAnalyzePage → runDualViewportCrawl | mitigate | isPrivateHost check in context.route() in browser.ts blocks RFC-1918 URLs at network level for every page crawl; applies automatically to all discovered URLs |
| T-08-12 | DoS (crawler trap) | MAX_CRAWL_PAGES env var set to large number | mitigate | Hard cap: Math.min(parseInt(...), 10) — absolute maximum is 10 pages regardless of env value |
| T-08-13 | DoS (page loop timeout) | Single slow page exceeding per-page budget | mitigate | withTimeout(90s) per page: timed-out page is caught, logged, and skipped; remaining pages continue |
| T-08-14 | Tampering | Prisma $transaction partial write | mitigate | Atomic transaction: if any crawledPage.create fails, entire write rolls back; job remains in 'analyzing' and outer catch marks it 'failed' |
| T-08-15 | Tampering | Duplicate root URL in additionalUrls | mitigate | normalizedRoot filter: additionalUrls.filter(u => u !== normalizedRoot) before slice |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
1. npx tsc --noEmit in crawler/ exits 0
2. Full npm test suite exits 0 (no regressions from processor.ts rewrite)
3. processor.ts contains MAX_PAGES constant with Math.min(..., 10) hard cap
4. processor.ts contains withTimeout, crawlAndAnalyzePage, writeCrawlResults functions
5. processJob calls runSiteWideAnalysis after the page loop
6. prisma.$transaction in writeCrawlResults creates Result + CrawledPage records atomically
</verification>

<success_criteria>
- processor.ts implements multi-page loop with per-page 90s timeout and per-page error isolation
- Root URL duplicate excluded from additional pages
- runAIPipeline called with mode='multi' for each page
- Single atomic transaction writes all results
- MAX_PAGES hard-capped at 10 regardless of env
- TypeScript compiles; no test regressions
</success_criteria>

<output>
Create `.planning/phases/08-multi-page-crawl/08-05-SUMMARY.md` when done
</output>
