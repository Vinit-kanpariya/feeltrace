---
phase: 08-multi-page-crawl
plan: 06
type: execute
wave: 4
depends_on: [08-01, 08-05]
files_modified:
  - src/app/results/[jobId]/page.tsx
  - src/components/PageAccordionSection.tsx
autonomous: false
requirements: [CRAWL-03]

must_haves:
  truths:
    - "Results page renders per-page accordion section when crawledPages.length > 0"
    - "Results page renders identically to Phase 7 when crawledPages.length === 0 (historical jobs)"
    - "Root page accordion section (page_index=0) is open by default; additional pages are closed"
    - "Each accordion section shows: page URL, per-page NarrativeSection, per-page IssueCard list"
    - "IssueCard is reused unchanged — CrawledPageIssue shape matches IssueCard props"
    - "PageAccordionSection is a Client Component ('use client' on line 1)"
    - "Cross-page patterns block renders if result.cross_page_patterns is non-null and non-empty"
  artifacts:
    - path: "src/app/results/[jobId]/page.tsx"
      provides: "Extended Prisma query with crawledPages include; accordion + cross-page patterns render"
      exports: []
    - path: "src/components/PageAccordionSection.tsx"
      provides: "Client Component accordion with URL header, NarrativeSection, IssueCard list"
      exports: ["PageAccordionSection"]
  key_links:
    - from: "src/app/results/[jobId]/page.tsx"
      to: "prisma.result.findUnique"
      via: "crawledPages include with nested issues and edges"
      pattern: "crawledPages"
    - from: "src/app/results/[jobId]/page.tsx"
      to: "src/components/PageAccordionSection.tsx"
      via: "PageAccordionSection import and usage"
      pattern: "PageAccordionSection"
    - from: "src/components/PageAccordionSection.tsx"
      to: "src/components/IssueCard.tsx"
      via: "IssueCard reused for per-page issues"
      pattern: "IssueCard"
---

<objective>
Add the per-page accordion UI to the results page. The Prisma query gains a crawledPages include; results page renders a new Section 7 accordion; PageAccordionSection is created as a new Client Component. Historical jobs with no crawledPages render identically to Phase 7.

Purpose: CRAWL-03 — users see per-page breakdown of issues alongside the site-wide summary. IssueCard is reused unchanged; only the accordion container is new.

Output: page.tsx extended; PageAccordionSection.tsx created; visual checkpoint for human verification.
</objective>

<execution_context>
@/c/Users/STL-LT-068/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/STL-LT-068/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-multi-page-crawl/08-RESEARCH.md
@.planning/phases/08-multi-page-crawl/08-PATTERNS.md
@.planning/phases/07-ai-pipeline-depth/07-04-SUMMARY.md

<interfaces>
<!-- From 08-PATTERNS.md: results page query extension -->
Extended Prisma query (replace existing findUnique call):
  prisma.result.findUnique({
    where: { jobId },
    include: {
      job: true,
      issues: { orderBy: { severity: 'desc' } },
      edges: true,
      crawledPages: {                    // NEW
        orderBy: { page_index: 'asc' },
        include: {
          issues: { orderBy: { severity: 'desc' } },
          edges: true,
        },
      },
    },
  })

<!-- From 08-PATTERNS.md: PageAccordionSection props -->
interface PageAccordionSectionProps {
  page: {
    id: string; url: string; page_index: number; narrative: unknown;
    screenshot_url: string | null;
    issues: { id: string; category: string; signal_source: string; severity: number;
              raw_evidence: string; technical_description: string;
              fix_suggestion: string; severity_justification: string; }[];
    edges: unknown[];
  }
  defaultOpen?: boolean
}

<!-- From 07-04-SUMMARY: IssueCard props shape -->
IssueCardProps: { id, category, signal_source, severity, raw_evidence, technical_description, fix_suggestion?: string, severity_justification?: string }
CrawledPageIssue has identical fields — reuse IssueCard unchanged.

<!-- From 08-PATTERNS.md: NarrativeSection dark theme class conventions -->
className="rounded-xl bg-[#131f35] border border-white/[0.08] overflow-hidden"

<!-- Cross-page patterns: stored in Result.cross_page_patterns Json? column -->
Render as a simple list before the accordion section. Each pattern: signal_source (page_count pages, max severity worst_severity). Show only if result.cross_page_patterns is truthy and is an array with length > 0.

<!-- ShareButton.tsx accordion state pattern -->
const [isOpen, setIsOpen] = useState(defaultOpen)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PageAccordionSection.tsx Client Component</name>
  <files>src/components/PageAccordionSection.tsx</files>
  <read_first>
    - src/components/ShareButton.tsx (read entire file — useState toggle pattern and 'use client' placement)
    - src/components/NarrativeSection.tsx (read entire file — dark theme class conventions, card layout, narrative prop shape)
    - src/components/IssueCard.tsx (read entire file — IssueCardProps shape to confirm CrawledPageIssue is compatible)
    - src/types/narrative.ts (read entire file — NarrativeResult type for cast)
  </read_first>
  <action>
    Create src/components/PageAccordionSection.tsx. CRITICAL: 'use client' must be on line 1 (before any imports).

    Imports after 'use client': useState from 'react'; IssueCard from './IssueCard'; NarrativeSection from './NarrativeSection'; NarrativeResult from '@/types/narrative' (type import).

    Props interface PageAccordionSectionProps: page object with id string, url string, page_index number, narrative unknown (Prisma Json), screenshot_url string | null, issues array (fields: id, category, signal_source, severity, raw_evidence, technical_description, fix_suggestion string, severity_justification string), edges unknown[]; defaultOpen boolean optional (default false).

    Component body:
    - const [isOpen, setIsOpen] = useState(defaultOpen)
    - const narrative = page.narrative as unknown as NarrativeResult
    - Return a div with className "rounded-xl bg-[#131f35] border border-white/[0.08] overflow-hidden"
    - Inside: a button element that toggles isOpen; className "w-full px-5 py-3.5 flex items-center justify-between text-left hover:bg-white/[0.03] transition-colors"
      - Left side: the page URL as a span with className "text-sm font-mono text-slate-300 truncate" — truncate long URLs
      - Right side: a chevron SVG (or text ▾/▸) indicating open/closed state; className "text-slate-500 flex-shrink-0 ml-3"
    - Below the button, conditionally render when isOpen: div with className "px-5 py-5 space-y-4 border-t border-white/[0.07]"
      - NarrativeSection component passing narrative={narrative}
      - If page.issues.length > 0: div with className "mt-6 space-y-3" containing page.issues.map(issue => IssueCard with key={issue.id} and issue={issue})
      - If page.issues.length === 0: p element "No issues detected on this page." with className "text-sm text-slate-500"

    Export the component as named export: export function PageAccordionSection(...)
  </action>
  <verify>
    <automated>cd /c/Users/STL-LT-068/Desktop/feeltrace && npm run typecheck 2>&1 | head -20</automated>
    <automated>grep -n "'use client'" /c/Users/STL-LT-068/Desktop/feeltrace/src/components/PageAccordionSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/PageAccordionSection.tsx exists
    - First line is 'use client' (exactly, with single quotes)
    - grep -n "'use client'" returns line 1
    - Component exports PageAccordionSection as named export
    - Component accepts page and defaultOpen props
    - Component uses useState for open/close toggle
    - Component renders NarrativeSection and IssueCard[] when open
    - npm run typecheck exits 0 (or shows no new errors related to PageAccordionSection.tsx)
  </acceptance_criteria>
  <done>PageAccordionSection.tsx created as Client Component; 'use client' on line 1; accordion toggle with NarrativeSection and IssueCard list; TypeScript-valid</done>
</task>

<task type="auto">
  <name>Task 2: Extend results page Prisma query and render per-page accordion + cross-page patterns</name>
  <files>src/app/results/[jobId]/page.tsx</files>
  <read_first>
    - src/app/results/[jobId]/page.tsx (read entire file — locate the exact Prisma findUnique call, the JSX structure, and Section numbering to insert Section 7 in the correct location)
    - src/components/PageAccordionSection.tsx (confirm exports — just created in Task 1)
  </read_first>
  <action>
    In src/app/results/[jobId]/page.tsx make three changes:

    1. Add import: import { PageAccordionSection } from '@/components/PageAccordionSection'

    2. Extend the existing prisma.result.findUnique call: add crawledPages to the include object with orderBy: { page_index: 'asc' } and nested include: { issues: { orderBy: { severity: 'desc' } }, edges: true }. Do not change any other part of the query.

    3. Update the inline type annotation for the result's issues.map (if one exists from Phase 7) to include fix_suggestion: string and severity_justification: string — these are already non-nullable in the Prisma schema post-07-02 migration, so keep them as string (not string?).

    4. After the last existing section block (before </main> or the closing wrapper div), add two new conditional sections:

    Section 7a — Cross-page patterns block:
    Condition: result.cross_page_patterns is truthy and Array.isArray(result.cross_page_patterns) and result.cross_page_patterns.length > 0
    If true: render a div with mt-8, containing:
    - h2 "Site-wide Patterns" with className "text-base font-semibold text-slate-100 mb-1"
    - p subtitle: "Issues detected across multiple crawled pages" with className "text-sm text-slate-500 mb-4"
    - ul or div listing each pattern from (result.cross_page_patterns as unknown as Array<{signal_source: string, page_count: number, worst_severity: number}>): show signal_source and "(detected on N pages)" — minimal presentational list, className "space-y-2"

    Section 7b — Per-page accordion:
    Condition: result.crawledPages.length > 0
    If true: render a div with mt-8, containing:
    - h2 "Per-page Breakdown" with className "text-base font-semibold text-slate-100 mb-1"
    - p subtitle: "{N} page{N !== 1 ? 's' : ''} crawled" with className "text-sm text-slate-500 mb-4"
    - div with className "space-y-3" containing result.crawledPages.map(page => PageAccordionSection with key={page.id}, page={page}, and defaultOpen={page.page_index === 0})

    Backward compatibility check: the Prisma query always includes crawledPages. For pre-Phase-8 Result rows, Prisma returns crawledPages as []. The length > 0 guard means the accordion section is simply not rendered for historical jobs — no change to existing rendering.

    If the inline type for issues.map needs updating for the crawledPages shape (Prisma returns CrawledPageIssue which has fix_suggestion/severity_justification as String non-nullable), ensure the type annotation inside the map matches: { id: string, category: string, signal_source: string, severity: number, raw_evidence: string, technical_description: string, fix_suggestion: string, severity_justification: string }.
  </action>
  <verify>
    <automated>cd /c/Users/STL-LT-068/Desktop/feeltrace && npm run typecheck 2>&1 | head -20</automated>
    <automated>grep -n "crawledPages\|PageAccordionSection\|cross_page_patterns" /c/Users/STL-LT-068/Desktop/feeltrace/src/app/results/[jobId]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx imports PageAccordionSection from '@/components/PageAccordionSection'
    - page.tsx Prisma query includes crawledPages with orderBy page_index asc and nested issues + edges
    - page.tsx renders PageAccordionSection for each crawledPage when crawledPages.length > 0
    - page.tsx passes defaultOpen={page.page_index === 0} to PageAccordionSection
    - page.tsx conditionally renders cross_page_patterns block when patterns array is non-empty
    - npm run typecheck exits 0 (no new type errors)
    - grep -n "crawledPages" returns at least 2 lines (one in include, one in JSX condition)
  </acceptance_criteria>
  <done>Results page query extended with crawledPages; accordion section renders per-page breakdown; cross-page patterns shown; historical jobs render identically to Phase 7; TypeScript compiles</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - PageAccordionSection Client Component with accordion toggle, NarrativeSection, and IssueCard list per page
    - Results page extended with crawledPages Prisma query and two new sections (cross-page patterns + per-page accordion)
    - Root page section (page_index=0) opens by default; additional pages collapsed
  </what-built>
  <how-to-verify>
    For a new multi-page job (after Phase 8 crawler is deployed):
    1. Submit a URL with multiple pages (e.g., a site with clear internal links) at /
    2. Wait for the job to complete (status: complete)
    3. Open /results/[jobId]
    4. Verify: site-wide narrative section renders at the top (unchanged from Phase 7)
    5. Verify: "Site-wide Patterns" block appears if cross-page patterns were detected
    6. Verify: "Per-page Breakdown" section appears below with the count of pages crawled
    7. Verify: root page accordion section is OPEN by default showing its NarrativeSection and IssueCard list
    8. Verify: additional pages are CLOSED by default; clicking opens them revealing their content
    9. Verify: IssueCard rows show fix and impact rows for Phase 7 enriched fields (same as before)
    For an old single-URL job (pre-Phase-8):
    10. Open any existing /results/[jobId] — verify page renders identically to Phase 7 (no accordion section visible)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any rendering issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prisma JSON cast → NarrativeResult | crawledPage.narrative is Prisma Json type, cast to NarrativeResult in PageAccordionSection |
| cross_page_patterns Json? → rendered list | JSON column cast to typed array for rendering |
| CrawledPageIssue fields → IssueCard JSX | Database strings rendered as JSX text (no dangerouslySetInnerHTML) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-16 | Tampering (XSS) | LLM-generated fix_suggestion / severity_justification in IssueCard | mitigate | Rendered as JSX text children (not dangerouslySetInnerHTML) per T-7-10 pattern from Phase 7; React auto-escapes all text content |
| T-08-17 | Tampering (XSS) | Page URL in accordion header | mitigate | Rendered as JSX text in span element; React escapes HTML entities in text — no href attribute used for raw URL display |
| T-08-18 | Information Disclosure | cross_page_patterns JSON rendered in UI | accept | FeelTrace has no multi-tenant auth; all analyses are public by design (shareable link model); no PII in cross-page pattern data |
| T-08-19 | Tampering | NarrativeResult JSON cast from Prisma Json type | accept | Double-cast (as unknown as NarrativeResult) is the project-standard pattern for Prisma JSON columns; values written by our own pipeline; not user-supplied input |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
1. npm run typecheck exits 0 (no new type errors)
2. PageAccordionSection.tsx line 1 is 'use client'
3. results/[jobId]/page.tsx imports PageAccordionSection
4. results/[jobId]/page.tsx Prisma query includes crawledPages
5. Human checkpoint: accordion renders correctly for multi-page jobs; historical jobs unchanged
</verification>

<success_criteria>
- PageAccordionSection.tsx created with 'use client' on line 1, useState toggle, NarrativeSection, IssueCard list
- Results page query includes crawledPages with nested issues and edges
- Per-page accordion renders when crawledPages.length > 0; hidden for historical single-URL jobs
- Root page open by default (page_index === 0); other pages closed
- Cross-page patterns block renders when result.cross_page_patterns is non-null and non-empty
- TypeScript compiles without errors
- Human checkpoint approved
</success_criteria>

<output>
Create `.planning/phases/08-multi-page-crawl/08-06-SUMMARY.md` when done
</output>
