---
phase: 08-multi-page-crawl
plan: 04
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - crawler/src/pipeline/run-pipeline.ts
  - crawler/src/pipeline/site-wide-merger.ts
autonomous: true
requirements: [CRAWL-02]

must_haves:
  truths:
    - "runAIPipeline accepts mode: 'single' | 'multi' = 'single' parameter and its behavior is byte-for-byte unchanged when called with no mode argument or mode='single'"
    - "runAIPipeline returns PipelineResult in 'multi' mode instead of writing to DB"
    - "site-wide-merger.ts exports detectCrossPagePatterns and runSiteWideAnalysis"
    - "detectCrossPagePatterns is a pure synchronous function (no LLM, no async)"
    - "runSiteWideAnalysis calls Groq with per-page summaries (top 5 issues only) to stay within 12,000 TPM"
    - "runSiteWideAnalysis returns per-page narrative directly when pageResults.length === 1 (no LLM call)"
    - "site-wide-merger.test.ts passes GREEN"
  artifacts:
    - path: "crawler/src/pipeline/run-pipeline.ts"
      provides: "runAIPipeline with mode param; PipelineResult return in multi mode"
      exports: ["runAIPipeline"]
    - path: "crawler/src/pipeline/site-wide-merger.ts"
      provides: "Stage 4 cross-page pattern detection and site-wide LLM narrative"
      exports: ["detectCrossPagePatterns", "runSiteWideAnalysis"]
  key_links:
    - from: "crawler/src/pipeline/run-pipeline.ts"
      to: "crawler/src/processor.ts"
      via: "runAIPipeline called with mode: 'multi'; returns PipelineResult"
      pattern: "mode.*multi"
    - from: "crawler/src/pipeline/site-wide-merger.ts"
      to: "crawler/src/processor.ts"
      via: "runSiteWideAnalysis(client, allPageResults) call"
      pattern: "runSiteWideAnalysis"
    - from: "crawler/src/pipeline/site-wide-merger.ts"
      to: "crawler/src/pipeline/stage3-narrator.ts"
      via: "dynamic import of parseNarrativeOutput for LLM output parsing"
      pattern: "parseNarrativeOutput"
---

<objective>
Implement the two pipeline changes needed for multi-page mode: (1) add the mode param to runAIPipeline so it can return results without writing to DB, and (2) create site-wide-merger.ts with deterministic cross-page pattern detection and Stage 4 LLM site-wide narrative generation.

Purpose: processor.ts (Plan 08-05) calls runAIPipeline(mode='multi') for each page to get PipelineResult, then calls runSiteWideAnalysis to merge all results into one site-wide narrative before the single atomic DB write.

Output: run-pipeline.ts extended with mode param; site-wide-merger.ts created from scratch; site-wide-merger.test.ts goes GREEN.
</objective>

<execution_context>
@/c/Users/STL-LT-068/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/STL-LT-068/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-multi-page-crawl/08-RESEARCH.md
@.planning/phases/08-multi-page-crawl/08-PATTERNS.md

<interfaces>
<!-- Current runAIPipeline signature (crawler/src/pipeline/run-pipeline.ts line 44) -->
export async function runAIPipeline(
  jobId: string,
  signals: { mobile: CrawlPass; desktop: CrawlPass },
  screenshot: Buffer | null,
  techProfile: TechProfile,
  externalSignals: ExternalSignals | null,
): Promise<void>

<!-- PipelineResult type (added to types.ts in 08-01) -->
export interface PipelineResult {
  enrichedIssues: EnrichedIssue[]
  edges: CausalEdgeCandidate[]
  narrative: NarrativeResult
  screenshotUrl: string | null
  techProfile: TechProfile
  pageType: PageType
}

<!-- PageAnalysisResult type (added to types.ts in 08-01) -->
export interface PageAnalysisResult {
  url: string; pageIndex: number; enrichedIssues: EnrichedIssue[]; edges: CausalEdgeCandidate[];
  narrative: NarrativeResult; screenshotUrl: string | null; techProfile: TechProfile;
  pageType: PageType; discoveredLinks: string[];
}

<!-- SiteWideNarrative type (added to types.ts in 08-01) -->
export interface SiteWideNarrative { narrative: NarrativeResult; crossPagePatterns: CrossPagePattern[] }

<!-- stage3-narrator.ts exports (for parseNarrativeOutput reuse) -->
parseNarrativeOutput(text: string): NarrativeResult  -- existing function, parse [SUMMARY]/[PERCEIVED]/[TECHNICAL]/[RECOMMENDATIONS] sections

<!-- groq-client.ts -->
import { getGroqClient } from '../lib/groq-client'
const client = getGroqClient()

<!-- Groq free-tier constraint: 12,000 TPM — Stage 4 input must be per-page summaries only (top 5 issues), not full EnrichedIssue[] arrays -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend runAIPipeline with mode param and PipelineResult return</name>
  <files>crawler/src/pipeline/run-pipeline.ts</files>
  <read_first>
    - crawler/src/pipeline/run-pipeline.ts (read entire file — understand the full flow: zero-issues path lines 82-98, main path lines 100-178; identify the exact prisma.$transaction block to conditionally skip)
    - crawler/src/pipeline/types.ts (read entire file — confirm PipelineResult and PageType are exported; verify import paths)
  </read_first>
  <behavior>
    - runAIPipeline(jobId, signals, screenshot, techProfile, externalSignals) — no mode arg — behaves identically to pre-Phase-8 (writes to DB, returns void)
    - runAIPipeline(jobId, signals, screenshot, techProfile, externalSignals, 'single') — writes to DB, returns void (same as no mode)
    - runAIPipeline(jobId, signals, screenshot, techProfile, externalSignals, 'multi') — does NOT write to DB, returns PipelineResult { enrichedIssues, edges, narrative, screenshotUrl, techProfile, pageType }
    - Zero-issues path with mode='multi' returns PipelineResult with enrichedIssues: [], edges: [], and a default narrative
  </behavior>
  <action>
    In crawler/src/pipeline/run-pipeline.ts make the following changes (do not alter any existing logic for the single-mode path):

    1. Add import at the top: import type { PipelineResult } from './types'

    2. Modify the function signature: add mode: 'single' | 'multi' = 'single' as the 6th parameter (after externalSignals). Change return type from Promise<void> to Promise<void | PipelineResult>.

    3. At the zero-issues path (currently ends with "return" after prisma.result.create, around line 98): add a mode branch BEFORE the prisma.result.create call:
       - If mode === 'multi': return early with PipelineResult { enrichedIssues: [], edges: [], narrative: { summary: 'No significant issues found. This page performs well across the measured signal categories.', perceivedPerformance: '', technicalPerformance: '', recommendations: [] }, screenshotUrl, techProfile, pageType: detectPageType(techProfile, signals.desktop.domSignals) }
       - If mode === 'single': proceed with the existing prisma.result.create and return (unchanged)

    4. At the end of the function, after Stage 3 narration completes and BEFORE the prisma.$transaction block (around line 119): add a mode branch:
       - If mode === 'multi': return { enrichedIssues, edges, narrative, screenshotUrl, techProfile, pageType }
       - If mode === 'single': fall through to the existing prisma.$transaction block (unchanged)

    The entire prisma.$transaction block (lines 119-178) remains untouched. Only the two return-early branches are added. Single-mode callers (processor.ts currently, and any future callers) experience zero change in behavior.

    After making changes, verify the function compiles and the existing test suite still passes.
  </action>
  <verify>
    <automated>cd /c/Users/STL-LT-068/Desktop/feeltrace/crawler && npx tsc --noEmit 2>&1 | head -20</automated>
    <automated>cd /c/Users/STL-LT-068/Desktop/feeltrace && npm test -- --reporter=dot 2>&1 | grep -E "PASS|FAIL|Tests:" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - runAIPipeline function signature in run-pipeline.ts includes mode: 'single' | 'multi' = 'single' as 6th parameter
    - Return type is Promise<void | PipelineResult>
    - PipelineResult is imported from './types'
    - Zero-issues multi-mode branch: returns PipelineResult with empty enrichedIssues and edges arrays
    - Main-path multi-mode branch: returns PipelineResult before prisma.$transaction
    - Existing single-mode path: prisma.$transaction block is completely unchanged
    - npx tsc --noEmit in crawler/ exits 0
    - Full test suite continues to pass (npm test exits 0; no previously-passing tests newly fail)
  </acceptance_criteria>
  <done>runAIPipeline has mode param; multi-mode returns PipelineResult without DB write; single-mode behavior byte-for-byte unchanged; TypeScript compiles; existing tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create crawler/src/pipeline/site-wide-merger.ts (Stage 4)</name>
  <files>crawler/src/pipeline/site-wide-merger.ts</files>
  <read_first>
    - crawler/src/pipeline/stage3-narrator.ts (read entire file — copy LLM call structure, parseNarrativeOutput usage, log prefix convention, and model name)
    - crawler/src/pipeline/site-wide-merger.test.ts (read entire file — understand all test assertions that must pass)
    - crawler/src/pipeline/types.ts (read entire file — PageAnalysisResult, SiteWideNarrative, CrossPagePattern shapes)
  </read_first>
  <behavior>
    - detectCrossPagePatterns(pages, 3) with 3 pages each having signal_source 'net.renderBlocking' severity 3 returns [{signal_source: 'net.renderBlocking', page_count: 3, worst_severity: 3, affected_urls: [3 urls], representative_evidence: <evidence from most severe>}]
    - detectCrossPagePatterns(pages, 3) with only 2 pages sharing a signal_source returns []
    - detectCrossPagePatterns orders results by worst_severity descending
    - runSiteWideAnalysis([singlePage]) returns { narrative: singlePage.narrative, crossPagePatterns: [] } without calling Groq
    - runSiteWideAnalysis([page1, page2]) calls Groq once with compact per-page summaries (top 5 issues each) and returns SiteWideNarrative
  </behavior>
  <action>
    Create crawler/src/pipeline/site-wide-merger.ts as a new file. Structure:

    Header comment: // Stage 4: site-wide merger — cross-page pattern detection (deterministic) + site-wide LLM narrative. Source: .planning/phases/08-multi-page-crawl/08-PATTERNS.md

    Imports:
    - import Groq from 'groq-sdk'
    - import type { PageAnalysisResult, SiteWideNarrative, CrossPagePattern, NarrativeResult } from './types'

    Export 1 — detectCrossPagePatterns(pageResults: PageAnalysisResult[], minPages: number = 3): CrossPagePattern[] — pure synchronous function, no LLM:
    Build a Map<string, { urls: string[], maxSeverity: number, evidence: string }> keyed by signal_source. Iterate pages → issues: if key exists, push url, update maxSeverity/evidence if issue.severity > existing.maxSeverity; if key absent, create entry with [page.url], issue.severity, issue.raw_evidence. Filter entries where urls.length >= minPages. Map to CrossPagePattern. Sort descending by worst_severity, then by page_count.

    Export 2 — runSiteWideAnalysis(client: Groq, pageResults: PageAnalysisResult[]): Promise<SiteWideNarrative>:
    1. Call detectCrossPagePatterns(pageResults) synchronously.
    2. If pageResults.length === 1: return { narrative: pageResults[0].narrative, crossPagePatterns: [] } — no LLM call.
    3. Build pageSummaries: for each page, select top 5 issues by severity descending, map to string "[sev N] signal_source: raw_evidence". Include url, pageType, narrative.summary, topIssues array.
    4. Build systemPrompt string: tell the LLM it analyzed N pages; list cross-page patterns if any (format: "- signal_source (K pages, max severity N): evidence"); instruct it to produce a site-wide narrative in [SUMMARY] / [PERCEIVED] / [TECHNICAL] / [RECOMMENDATIONS] format (same as Stage 3) with a summary that opens by addressing the most impactful cross-site pattern, perceived UX across the site, technical root causes, and 3-5 prioritized recommendations.
    5. Build userPrompt: JSON.stringify(pageSummaries, null, 2) prefixed with "Analyze ${pageResults.length} pages from the same website:"
    6. Call client.chat.completions.create with model: 'llama-3.3-70b-versatile', max_tokens: 1024, messages: [{ role: 'system', content: systemPrompt }, { role: 'user', content: userPrompt }]
    7. Parse response with parseNarrativeOutput — import it via: import { parseNarrativeOutput } from './stage3-narrator' (static import, not dynamic)
    8. Log: console.log('[pipeline] Stage 4: site-wide analysis for ' + pageResults.length + ' pages complete')
    9. Return { narrative, crossPagePatterns }

    Groq 429 fallback: wrap the client.chat.completions.create call in try/catch. On any error, log a warning and return { narrative: pageResults[0].narrative, crossPagePatterns } (partial result — uses root page narrative as fallback, preserves cross-page patterns from deterministic detection).
  </action>
  <verify>
    <automated>cd /c/Users/STL-LT-068/Desktop/feeltrace && npm test -- --reporter=dot crawler/src/pipeline/site-wide-merger.test.ts 2>&1 | tail -10</automated>
    <automated>cd /c/Users/STL-LT-068/Desktop/feeltrace/crawler && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - site-wide-merger.ts exports detectCrossPagePatterns and runSiteWideAnalysis
    - site-wide-merger.test.ts all tests pass GREEN
    - detectCrossPagePatterns is a synchronous function (no async, no Groq)
    - runSiteWideAnalysis uses model 'llama-3.3-70b-versatile' and max_tokens 1024
    - runSiteWideAnalysis input to LLM is per-page summaries with top 5 issues (not full EnrichedIssue arrays)
    - Single-page shortcut: pageResults.length === 1 returns immediately without calling Groq
    - Groq 429/error fallback: catch block returns partial result rather than throwing
    - npx tsc --noEmit in crawler/ exits 0
  </acceptance_criteria>
  <done>site-wide-merger.ts created; all site-wide-merger.test.ts tests GREEN; detectCrossPagePatterns and runSiteWideAnalysis exported and TypeScript-valid</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Per-page issue data → Stage 4 LLM | Per-page summaries (our own pipeline output) sent as LLM input |
| Groq API → site-wide narrative | LLM response parsed by parseNarrativeOutput |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Tampering (prompt injection) | Stage 4 LLM input from per-page summaries | mitigate | Stage 4 input is structured per-page summaries from our own pipeline (signal_source + raw_evidence strings) — not raw page content; limited injection surface; raw_evidence is a brief measurement string ("2400ms > threshold 2000ms"), not free-form user content |
| T-08-09 | DoS (TPM limit) | Groq 12,000 TPM free tier | mitigate | Stage 4 input capped at top 5 issues per page × N pages; Groq 429 catch block returns partial result rather than failing the job |
| T-08-10 | Tampering | mode param allows bypassing DB write | accept | mode is a function parameter called only by internal crawler code (processor.ts); not user-controlled; TypeScript string literal union enforces valid values at compile time |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
1. npm test -- crawler/src/pipeline/site-wide-merger.test.ts exits 0 (all tests GREEN)
2. runAIPipeline signature in run-pipeline.ts has mode: 'single' | 'multi' = 'single' as 6th param
3. site-wide-merger.ts exported functions: detectCrossPagePatterns and runSiteWideAnalysis
4. Full test suite still passes (no regression)
5. npx tsc --noEmit in crawler/ exits 0
</verification>

<success_criteria>
- site-wide-merger.test.ts all tests GREEN (detectCrossPagePatterns: threshold, empty, sort, affected_urls; runSiteWideAnalysis: single-page shortcut, multi-page with mock)
- runAIPipeline mode='single' behavior byte-for-byte unchanged (existing tests prove this)
- runAIPipeline mode='multi' returns PipelineResult without DB write
- detectCrossPagePatterns is synchronous, pure, no dependencies
- Stage 4 LLM input stays under TPM limit (per-page summaries, top 5 issues)
</success_criteria>

<output>
Create `.planning/phases/08-multi-page-crawl/08-04-SUMMARY.md` when done
</output>
