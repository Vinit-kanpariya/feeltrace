---
phase: 08-multi-page-crawl
plan: 03
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - crawler/src/browser.ts
autonomous: true
requirements: [CRAWL-01]

must_haves:
  truths:
    - "extractInternalLinks is exported from browser.ts"
    - "extractInternalLinks returns only same-origin, non-fragment, non-mailto, non-tel URLs"
    - "extractInternalLinks deduplicates URLs and normalizes trailing slashes"
    - "extractInternalLinks strips URL fragments (treats /about and /about#section as the same page)"
    - "crawlWithViewport returns internalLinks in its result (desktop pass only; empty array on mobile pass)"
    - "extractInternalLinks is called inside crawlWithViewport BEFORE context.close()"
    - "browser.test.ts passes (GREEN state)"
  artifacts:
    - path: "crawler/src/browser.ts"
      provides: "extractInternalLinks exported function; CrawlPass return extended with internalLinks"
      exports: ["extractInternalLinks"]
  key_links:
    - from: "crawler/src/browser.ts extractInternalLinks"
      to: "crawler/src/processor.ts"
      via: "CrawlPass.internalLinks field read as desktop.internalLinks ?? []"
      pattern: "internalLinks"
    - from: "crawler/src/browser.ts crawlWithViewport"
      to: "context.close()"
      via: "extractInternalLinks called before context.close() line"
      pattern: "extractInternalLinks.*page.*url"
---

<objective>
Implement extractInternalLinks in browser.ts and extend crawlWithViewport to call it on the desktop pass before context.close(). This is the core CRAWL-01 link discovery function.

Purpose: processor.ts (Plan 08-05) reads discovered links from CrawlPass.internalLinks to seed the multi-page loop. This plan makes that field available.

Output: browser.ts modified with extractInternalLinks exported function + crawlWithViewport return extended with internalLinks field; browser.test.ts goes GREEN.
</objective>

<execution_context>
@/c/Users/STL-LT-068/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/STL-LT-068/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-multi-page-crawl/08-RESEARCH.md
@.planning/phases/08-multi-page-crawl/08-PATTERNS.md

<interfaces>
<!-- From crawler/src/browser.ts (lines 185-198) — current crawlWithViewport return and context.close() position -->
Current return (lines 189-198):
  return { viewport: options.viewport, domSignals, cssSignals, jsSignals, networkSignals, screenshot, browserFingerprint, axeViolations }

context.close() is on line 185 — extractInternalLinks MUST be called BEFORE this line.

<!-- From 08-01 plan: CrawlPass now has internalLinks?: string[] in lib/types.ts -->
CrawlPass.internalLinks?: string[]  // Phase 8 — desktop pass only

<!-- From 08-PATTERNS.md extractInternalLinks implementation pattern -->
Function signature: export async function extractInternalLinks(page: Page, baseUrl: string): Promise<string[]>
Filtering rules: skip null/undefined; skip hrefs starting with '#', 'mailto:', 'tel:'; use new URL(href, baseUrl).href for normalization; strip fragment via .split('#')[0]; strip trailing slash via .replace(/\/$/, '') || absolute; skip if not same origin; dedup via Set<string>; skip non-http/https protocols.

<!-- Insertion point in crawlWithViewport (line 144-185 block) -->
After axeViolations capture (line 138) and before screenshot (line 148):
  Screenshot is currently at lines 144-183 in the desktop-only block.
  context.close() is at line 185.
  Link extraction goes at the END of the desktop-only block, AFTER screenshot and browserFingerprint, BEFORE context.close().
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement extractInternalLinks and extend crawlWithViewport desktop pass</name>
  <files>crawler/src/browser.ts</files>
  <read_first>
    - crawler/src/browser.ts (read entire file — critical: identify exact line numbers for context.close(), the desktop-only block, and the return statement)
    - crawler/src/lib/types.ts (read CrawlPass interface — confirm internalLinks?: string[] was added in 08-01)
    - crawler/src/browser.test.ts (read entire file — understand all behavioral assertions that must pass)
  </read_first>
  <behavior>
    - extractInternalLinks(page, 'https://example.com') with hrefs ['/about', 'https://other.com'] returns ['https://example.com/about'] (excludes cross-origin)
    - extractInternalLinks(page, 'https://example.com') with hrefs ['#section'] returns [] (excludes fragment-only)
    - extractInternalLinks(page, 'https://example.com') with hrefs ['mailto:a@b.com'] returns [] (excludes mailto)
    - extractInternalLinks(page, 'https://example.com') with hrefs ['/about', '/about/', '/about#section'] returns exactly 1 URL (dedup after normalization)
    - crawlWithViewport for desktop pass returns result.internalLinks as a string array
    - crawlWithViewport for mobile pass returns result.internalLinks as [] (not populated)
  </behavior>
  <action>
    In crawler/src/browser.ts make three changes:

    1. Add the extractInternalLinks exported async function. Place it AFTER the isPrivateHost function and BEFORE the waitForSpaHydration function (or after waitForSpaHydration, before the ThrottleOptions interface — either location is acceptable as long as it is a module-level export). The function signature is: export async function extractInternalLinks(page: Page, baseUrl: string): Promise<string[]>

    The implementation: get origin via new URL(baseUrl).origin; call page.evaluate returning Array.from(document.querySelectorAll('a[href]')).map(a => a.getAttribute('href')) as (string | null)[]; iterate over hrefs with a Set<string> for dedup; for each href: skip null, skip hrefs starting with '#' / 'mailto:' / 'tel:'; use new URL(href, baseUrl).href wrapped in try/catch (skip on error); strip fragment with .split('#')[0]; strip trailing slash with .replace(/\/$/, '') — but only if the result is non-empty (use: const normalized = absolute.split('#')[0].replace(/\/$/, '') || absolute); skip if normalized doesn't start with origin; skip if not starts with 'http://' or 'https://'; skip if already in Set; add to Set and push to links array. Return links array (not sliced — caller applies MAX_PAGES - 1 slice).

    2. Inside crawlWithViewport, at the END of the "if (options.viewport === 'desktop')" block (after browserFingerprint capture, before context.close() on the line that follows): add link extraction:
       - Declare: let internalLinks: string[] = []
       - Add inside the desktop-only block: internalLinks = await extractInternalLinks(page, url)
       - Add: console.log('[browser] Discovered ' + internalLinks.length + ' internal links from ' + url)
       - CRITICAL: this must be before the await context.close() call. Verify placement by checking the line immediately after the if-block closes vs where context.close() appears.

    3. In the return statement of crawlWithViewport, add internalLinks to the returned object:
       return { viewport: options.viewport, domSignals, cssSignals, jsSignals, networkSignals, screenshot, browserFingerprint, axeViolations, internalLinks }

    Note: internalLinks is declared as [] at the top of crawlWithViewport (covers mobile pass) and assigned inside the desktop block — so mobile passes return [] and desktop passes return discovered links.
  </action>
  <verify>
    <automated>cd /c/Users/STL-LT-068/Desktop/feeltrace && npm test -- --reporter=dot crawler/src/browser.test.ts 2>&1 | tail -10</automated>
    <automated>cd /c/Users/STL-LT-068/Desktop/feeltrace/crawler && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - browser.test.ts all tests pass (GREEN — npm test exits 0 on that file)
    - extractInternalLinks is exported from crawler/src/browser.ts (grep -n "export.*extractInternalLinks" finds a match)
    - The function is defined BEFORE the crawlWithViewport function in the file
    - extractInternalLinks call appears in browser.ts BEFORE the context.close() call (grep -n "extractInternalLinks\|context.close" — extractInternalLinks line number < context.close() line number)
    - crawlWithViewport return statement includes internalLinks field
    - npx tsc --noEmit in crawler/ exits 0
  </acceptance_criteria>
  <done>extractInternalLinks implemented and exported; browser.test.ts all 6+ tests pass GREEN; CrawlPass.internalLinks populated on desktop pass; TypeScript compiles without errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discovered links → crawlAndAnalyzePage | URLs extracted from user's page enter the crawl loop; potential SSRF surface |
| page.evaluate() → crawler process | JavaScript executed in browser context returns hrefs; could return large arrays |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Tampering (SSRF) | extractInternalLinks returns discovered URLs → crawl loop | mitigate | SSRF guard: existing isPrivateHost check in context.route() handler applies automatically to all pages crawled via runDualViewportCrawl — same browser context blocks RFC-1918 URLs at the network level |
| T-08-06 | DoS | Malicious page with 10,000 a[href] tags inflating link set | mitigate | extractInternalLinks returns the full discovered list; caller in processor.ts slices to MAX_PAGES - 1 (bounded at 9 regardless of list size) |
| T-08-07 | Tampering | Duplicate root URL in discovered links | mitigate | processor.ts (Plan 08-05) filters rootUrl from additionalUrls before looping; extractInternalLinks does not exclude it (would require caller's rootUrl knowledge) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
1. npm test -- crawler/src/browser.test.ts exits 0 (all tests GREEN)
2. grep -n "export.*extractInternalLinks" crawler/src/browser.ts returns a result
3. crawlWithViewport return includes internalLinks field
4. extractInternalLinks call is before context.close() (line order check)
5. npx tsc --noEmit in crawler/ exits 0
</verification>

<success_criteria>
- All browser.test.ts test cases pass (same-origin filter, fragment exclusion, mailto/tel exclusion, dedup, relative normalization, malformed href safety)
- extractInternalLinks exported at module level
- crawlWithViewport desktop pass populates internalLinks; mobile pass returns []
- No TypeScript errors introduced
</success_criteria>

<output>
Create `.planning/phases/08-multi-page-crawl/08-03-SUMMARY.md` when done
</output>
