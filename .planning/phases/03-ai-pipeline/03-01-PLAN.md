---
phase: 03-ai-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crawler/package.json
autonomous: false
requirements:
  - AI-02
  - AI-03
user_setup:
  - service: google-ai-studio
    why: "Stage 2 (reasoning) and Stage 3 (narration) both call gemini-2.0-flash via Google AI Studio free tier"
    env_vars:
      - name: GEMINI_API_KEY
        source: "aistudio.google.com → Get API key → Create API key"
    dashboard_config:
      - task: "Add GEMINI_API_KEY to crawler/.env (local dev)"
        location: "Edit crawler/.env — set GEMINI_API_KEY=AIza..."
      - task: "Add GEMINI_API_KEY to Fly.io secrets (production)"
        location: "fly secrets set GEMINI_API_KEY=AIza... --app feeltrace-crawler"

must_haves:
  truths:
    - "@google/generative-ai is listed in crawler/package.json dependencies"
    - "GEMINI_API_KEY is available in crawler/.env for local development"
    - "crawl process can import @google/generative-ai without module resolution errors"
  artifacts:
    - path: "crawler/package.json"
      provides: "@google/generative-ai dependency entry"
      contains: '"@google/generative-ai"'
  key_links:
    - from: "crawler/src/pipeline/stage2-reasoner.ts"
      to: "@google/generative-ai"
      via: "npm install"
      pattern: '"@google/generative-ai"'
---

<objective>
Install the Google Generative AI SDK into the crawler sub-project after the developer provisions the GEMINI_API_KEY.

Purpose: Stage 2 and Stage 3 of the AI pipeline require the @google/generative-ai package and a valid API key. The key is provisioned free via Google AI Studio (aistudio.google.com) — no billing required. This plan gates the rest of Phase 3 on that human prerequisite.

Output: crawler/package.json updated with @google/generative-ai; GEMINI_API_KEY confirmed present in crawler/.env.
</objective>

<execution_context>
@C:\Users\STL-LT-068\Desktop\feeltrace\.planning\phases\03-ai-pipeline\03-RESEARCH.md
@C:\Users\STL-LT-068\Desktop\feeltrace\.planning\phases\03-ai-pipeline\03-PATTERNS.md
</execution_context>

<context>
@C:\Users\STL-LT-068\Desktop\feeltrace\.planning\ROADMAP.md
@C:\Users\STL-LT-068\Desktop\feeltrace\.planning\phases\03-ai-pipeline\03-RESEARCH.md

<interfaces>
<!-- From crawler/package.json — current dependencies block -->
{
  "dependencies": {
    "@hono/node-server": "^2.0.3",
    "@neondatabase/serverless": "^1.1.0",
    "@prisma/adapter-neon": "^7.8.0",
    "@prisma/client": "7.8.0",
    "@upstash/qstash": "2.11.0",
    "hono": "4.12.21",
    "p-queue": "9.3.0",
    "playwright-core": "1.60.0",
    "zod": "4.4.3"
  }
}
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provision GEMINI_API_KEY</name>
  <read_first>
    crawler/.env — verify current env file contents before adding the key
  </read_first>
  <what-built>
    Nothing is built yet — this task gates the install on key availability.
  </what-built>
  <how-to-verify>
    1. Log in at aistudio.google.com → Get API key → Create API key (free, no billing required)
    2. Copy the key value (begins with AIza)
    3. Add to crawler/.env: GEMINI_API_KEY=AIza...
    4. For production Fly.io: run `fly secrets set GEMINI_API_KEY=AIza... --app feeltrace-crawler`
    5. Confirm: `grep -c GEMINI_API_KEY crawler/.env` returns 1
  </how-to-verify>
  <resume-signal>Type "key added" after GEMINI_API_KEY is present in crawler/.env</resume-signal>
  <acceptance_criteria>
    - crawler/.env contains a line matching GEMINI_API_KEY=AIza
    - `grep -v '^#' crawler/.env | grep -c GEMINI_API_KEY` returns 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Install @google/generative-ai in crawler/</name>
  <read_first>
    crawler/package.json — read current dependencies before modifying
  </read_first>
  <action>
    Change directory to crawler/ and run the SDK install using the sfw prefix (CLAUDE.md requirement — never run bare npm install):
    `cd crawler && sfw npm install @google/generative-ai`

    After install, verify crawler/package.json now lists "@google/generative-ai" in the dependencies block. The entry should appear alphabetically between "@hono/node-server" and "@neondatabase/serverless" per the PATTERNS.md package order.

    Do NOT run this install from the root project directory — @google/generative-ai belongs only in crawler/ (the pipeline runs in the crawler process). Installing it in the root would add it to the Next.js bundle.

    After install, run `node -e "require('@google/generative-ai')"` from within crawler/ to confirm the module resolves without errors.
  </action>
  <verify>
    <automated>cd crawler && node -e "require('@google/generative-ai')" && echo "SDK resolves OK"</automated>
  </verify>
  <done>
    crawler/package.json contains "@google/generative-ai" in dependencies. `node -e "require('@google/generative-ai')"` exits 0 from crawler/ directory.
  </done>
  <acceptance_criteria>
    - crawler/package.json contains the string "@google/generative-ai"
    - `cd crawler && node -e "require('@google/generative-ai')" && echo OK` exits 0
    - crawler/node_modules/@google/generative-ai/ directory exists
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → env file | ANTHROPIC_API_KEY is entered manually; must never be committed to git |
| crawler process → Anthropic API | Outbound HTTPS; key transmitted in Authorization header |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | ANTHROPIC_API_KEY in crawler/.env | mitigate | .env is in .gitignore (verified Phase 1); never log the key value; set via Fly.io secrets for production |
| T-03-02 | Tampering | npm install supply chain | mitigate | sfw prefix on install command per CLAUDE.md; package legitimacy audit completed in RESEARCH.md ([OK] verdict for @anthropic-ai/sdk) |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck run on @anthropic-ai/sdk (2026-05-26) — [OK] verdict; no blocking checkpoint required |
</threat_model>

<verification>
- crawler/package.json lists @google/generative-ai under dependencies
- `cd crawler && node -e "require('@google/generative-ai')"` exits 0
- GEMINI_API_KEY is present in crawler/.env (non-empty, non-commented line)
- crawler/.env is NOT tracked by git (confirmed via `git check-ignore crawler/.env`)
</verification>

<success_criteria>
@google/generative-ai installed in crawler/. GEMINI_API_KEY confirmed present in local env. Phase 3 Wave 2 plans (03-02) can proceed.
</success_criteria>

<output>
Create `.planning/phases/03-ai-pipeline/03-01-SUMMARY.md` when done
</output>
