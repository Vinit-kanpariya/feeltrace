---
phase: 03-ai-pipeline
plan: "05"
type: execute
wave: 4
depends_on:
  - "03-03"
  - "03-04"
files_modified:
  - crawler/src/processor.ts
autonomous: false
requirements:
  - AI-01
  - AI-02
  - AI-03
  - AI-04

must_haves:
  truths:
    - "Submitting a real URL via the Next.js dashboard triggers a job that transitions pending → crawling → extracting → analyzing → complete"
    - "After the job reaches complete, a Result record exists in Neon with a non-null narrative JSON object"
    - "The Result's narrative JSON has non-empty summary, perceivedPerformance, and technicalPerformance fields"
    - "At least one Issue record is linked to the Result with severity populated as an integer"
    - "The TODO stub in processor.ts is replaced by `await runAIPipeline(jobId, _signals)`"
  artifacts:
    - path: "crawler/src/processor.ts"
      provides: "processJob with runAIPipeline wired in place of TODO stub"
      contains: "await runAIPipeline(jobId, _signals)"
  key_links:
    - from: "crawler/src/processor.ts"
      to: "crawler/src/pipeline/run-pipeline.ts"
      via: "import { runAIPipeline }"
      pattern: "from './pipeline/run-pipeline'"
---

<objective>
Wire the completed AI pipeline into processor.ts by replacing the Phase 3 TODO stub, then verify the full end-to-end flow with a real URL and confirm that Result/Issue/CausalEdge records are written to Neon.

Purpose: This is the final integration step that makes Phase 3 functional. A single import and a single function call replace the TODO stub. The human checkpoint confirms the complete vertical slice works end-to-end: URL → crawl → score → reason → narrate → DB.

Output: crawler/src/processor.ts with runAIPipeline replacing the TODO stub. End-to-end verification against a real site (e.g., https://nextjs.org) confirming DB records written.
</objective>

<execution_context>
@C:\Users\STL-LT-068\Desktop\feeltrace\.planning\phases\03-ai-pipeline\03-RESEARCH.md
@C:\Users\STL-LT-068\Desktop\feeltrace\.planning\phases\03-ai-pipeline\03-PATTERNS.md
</execution_context>

<context>
@C:\Users\STL-LT-068\Desktop\feeltrace\.planning\ROADMAP.md
@C:\Users\STL-LT-068\Desktop\feeltrace\.planning\phases\03-ai-pipeline\03-RESEARCH.md
@C:\Users\STL-LT-068\Desktop\feeltrace\.planning\phases\03-ai-pipeline\03-PATTERNS.md
@C:\Users\STL-LT-068\Desktop\feeltrace\.planning\phases\02-crawler-service\02-06-SUMMARY.md

<interfaces>
<!-- crawler/src/processor.ts — current stub to replace (lines 37-40) -->
    await prisma.job.update({ where: { id: jobId }, data: { status: 'analyzing' } })

    // TODO Phase 3: invoke AI pipeline with _signals, write Result/Issue/CausalEdge records
    await prisma.job.update({ where: { id: jobId }, data: { status: 'complete' } })

<!-- Replacement (from PATTERNS.md lines 488-496) -->
    await prisma.job.update({ where: { id: jobId }, data: { status: 'analyzing' } })

    // Phase 3: AI pipeline — scoreSignals → LLM reasoning → LLM narration → DB write
    await runAIPipeline(jobId, _signals)

    await prisma.job.update({ where: { id: jobId }, data: { status: 'complete' } })
    console.log(`[processor] Job ${jobId} completed in ${Date.now() - startedAt}ms`)

<!-- Import to add at top of processor.ts (after existing imports on lines 1-2) -->
import { runAIPipeline } from './pipeline/run-pipeline'

<!-- runAIPipeline signature from run-pipeline.ts -->
export async function runAIPipeline(
  jobId: string,
  signals: { mobile: CrawlPass; desktop: CrawlPass }
): Promise<void>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire runAIPipeline into processor.ts</name>
  <files>crawler/src/processor.ts</files>
  <read_first>
    crawler/src/processor.ts — read the entire file to see exact current state before editing
    crawler/src/pipeline/run-pipeline.ts — confirm runAIPipeline signature and import path
  </read_first>
  <action>
    Make exactly two changes to crawler/src/processor.ts:

    Change 1 — Add import at the top of the file, after the existing two import lines (lines 1-2):
    `import { runAIPipeline } from './pipeline/run-pipeline'`

    Change 2 — Replace lines 39-40 (the TODO stub + immediate complete update) with the three-line replacement:
    ```
    // Phase 3: AI pipeline — scoreSignals → LLM reasoning → LLM narration → DB write
    await runAIPipeline(jobId, _signals)
    ```
    Keep the existing `await prisma.job.update({ where: { id: jobId }, data: { status: 'complete' } })` line immediately after. Keep the existing `console.log` line immediately after that.

    The surrounding try/catch block (lines 20-49) must remain completely intact. Do not move, remove, or modify any other lines.

    After editing, verify that `_signals` (the variable declared on line 34) is now passed to runAIPipeline instead of being unused. The leading underscore prefix on `_signals` was a Phase 2 placeholder signalling unused-variable intentionality per INFRA-03. With Phase 3 wired, rename `_signals` to `signals` (remove the underscore) so TypeScript no longer needs the suppression prefix.

    Run `cd crawler && npm run build 2>&1 | tail -20` to confirm the TypeScript compilation succeeds and the build is deployable.
  </action>
  <verify>
    <automated>cd crawler && npm run build 2>&1 | tail -5 && echo "BUILD OK" && npm run lint 2>&1 | tail -10 && echo "LINT OK"</automated>
  </verify>
  <done>
    crawler/src/processor.ts contains `import { runAIPipeline } from './pipeline/run-pipeline'` and `await runAIPipeline(jobId, signals)`. `npm run build` exits 0 from crawler/. The TODO comment is removed.
  </done>
  <acceptance_criteria>
    - crawler/src/processor.ts contains `import { runAIPipeline } from './pipeline/run-pipeline'`
    - crawler/src/processor.ts contains `await runAIPipeline(jobId`
    - crawler/src/processor.ts does NOT contain the string `TODO Phase 3`
    - `cd crawler && npm run build` exits 0
    - `cd crawler && npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end smoke test with a real URL</name>
  <read_first>
    .planning/phases/02-crawler-service/02-06-SUMMARY.md — local run instructions (npm run build + node --env-file=.env dist/index.js + ngrok)
    crawler/.env — verify ANTHROPIC_API_KEY is present
  </read_first>
  <what-built>
    Task 1 wired runAIPipeline into processJob. The full pipeline is now live: URL → Playwright crawl → signal extraction → Stage 1 scoring → Stage 2 LLM reasoning → Stage 3 LLM narration → Prisma DB write.
  </what-built>
  <how-to-verify>
    1. Ensure crawler/.env has ANTHROPIC_API_KEY set (from Plan 03-01)
    2. Build and start the crawler locally:
       `cd crawler && npm run build && node --env-file=.env dist/index.js`
    3. In a separate terminal, start ngrok:
       `ngrok http 3001`
       Copy the ngrok HTTPS URL (e.g., https://abc123.ngrok.io)
    4. Update RAILWAY_CRAWLER_URL in crawler/.env to the current ngrok URL (only the base URL, e.g., https://abc123.ngrok.io/crawl)
    5. Submit a test URL from the Next.js dashboard (use https://nextjs.org or another real React site)
    6. Poll the dashboard until the job status reaches "complete" (30-60 seconds expected)
    7. Check the database with Prisma Studio (`npm run db:studio` from project root):
       - Navigate to Result table — confirm a record exists with a non-null narrative JSON
       - Navigate to Issue table — confirm at least 1 issue linked to the Result with a numeric severity
       - Navigate to CausalEdge table — may be empty if no high-confidence edges found (acceptable)
    8. Check crawler logs in the terminal for `[pipeline] Job ... completed` messages
    9. Expected log sequence:
       - `[processor] Job ... signals collected: mobile=mobile, desktop=desktop`
       - `[pipeline] Job ...: N issues scored`
       - `[pipeline] Job ...: Stage 2 complete — N causal edges`
       - `[pipeline] Job ...: Stage 3 complete`
       - `[pipeline] Job ... DB write complete — N issues, N edges`
       - `[processor] Job ... completed in ...ms`
  </how-to-verify>
  <resume-signal>Type "verified" if Result + Issues exist in DB with real content. Type "failed: [description]" if an error occurred.</resume-signal>
  <acceptance_criteria>
    - Result record exists in Neon DB with narrative JSON containing non-empty summary field
    - narrative.perceivedPerformance is a non-empty string (AI-04)
    - narrative.technicalPerformance is a non-empty string (AI-04)
    - At least 1 Issue record linked to the Result with severity in {1,2,3,4}
    - Crawler logs show the full [pipeline] sequence without errors
    - No unhandled exceptions in crawler process output
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| processJob → runAIPipeline | In-process function call — signals passed as typed objects, no serialization |
| crawler process → Neon DB | PrismaNeon adapter over HTTPS — write path uses ORM parameter binding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | Tampering | processor.ts modification breaking existing job lifecycle | mitigate | Only two surgical changes: one import, one function call replacing TODO stub; surrounding try/catch and status transitions unchanged; build verification confirms no regressions |
| T-03-15 | Denial of Service | runAIPipeline hanging and blocking the p-queue | mitigate | processJob already has a 55s SLA timeout (D-28) via Promise.race; if runAIPipeline exceeds that, the outer timeout fires and the catch block sets status:failed |
| T-03-16 | Information Disclosure | ANTHROPIC_API_KEY visible in crawler logs | mitigate | Key is never passed through any logging path — only used as a constructor argument to the Anthropic SDK client |
</threat_model>

<verification>
- `cd crawler && npm run build` exits 0
- crawler/src/processor.ts does not contain 'TODO Phase 3'
- Real URL job reaches 'complete' status in Neon DB
- Result.narrative JSON has non-empty summary, perceivedPerformance, technicalPerformance fields
- At least 1 Issue record with severity integer in {1,2,3,4}
- Full test suite still passes: `npm test -- --run` exits 0 from project root
</verification>

<success_criteria>
processJob wired to runAIPipeline. End-to-end flow verified with a real site: URL submission → crawl → AI pipeline → DB writes. Result, Issue, and (optionally) CausalEdge records exist in Neon with real AI-generated content. All four requirements AI-01 through AI-04 satisfied end-to-end.
</success_criteria>

<output>
Create `.planning/phases/03-ai-pipeline/03-05-SUMMARY.md` when done
</output>
