---
phase: 03-ai-pipeline
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - crawler/src/pipeline/types.ts
  - crawler/src/pipeline/stage1-scorer.ts
  - crawler/src/pipeline/stage1-scorer.test.ts
autonomous: true
requirements:
  - AI-01
  - AI-04

must_haves:
  truths:
    - "Submitting signal values above each threshold produces a ScoredIssue with the correct severity integer"
    - "TTFB > 2000ms produces a ScoredIssue with category 'perceived-perf' and severity 4"
    - "totalJSBytes > 500000 on mobile produces a ScoredIssue with category 'technical-perf' and severity 4"
    - "When mobile and desktop both exceed the same threshold, exactly one ScoredIssue is emitted (deduplication)"
    - "All test cases in stage1-scorer.test.ts pass with npm test -- --run stage1-scorer"
  artifacts:
    - path: "crawler/src/pipeline/types.ts"
      provides: "ScoredIssue, EnrichedIssue, CausalEdgeCandidate, NarrativeResult, SEVERITY_LABELS"
      contains: "export interface ScoredIssue"
    - path: "crawler/src/pipeline/stage1-scorer.ts"
      provides: "scoreSignals pure function"
      contains: "export function scoreSignals"
    - path: "crawler/src/pipeline/stage1-scorer.test.ts"
      provides: "AI-01 and AI-04 unit tests"
      contains: "describe('scoreSignals'"
  key_links:
    - from: "crawler/src/pipeline/stage1-scorer.ts"
      to: "crawler/src/pipeline/types.ts"
      via: "import type { ScoredIssue }"
      pattern: "from './types'"
    - from: "crawler/src/pipeline/stage1-scorer.ts"
      to: "crawler/src/lib/types.ts"
      via: "import type { CrawlPass }"
      pattern: "from '../lib/types'"
---

<objective>
Define all pipeline type contracts and implement Stage 1 deterministic scoring — the pure-function layer that converts CrawlPass signals into a flat ScoredIssue list with no LLM involvement.

Purpose: AI-01 requires that severity labels come from deterministic threshold rules, not from LLM inference. This plan delivers the full scoring table as a testable pure function. It is the foundation every downstream stage depends on. The category field (perceived-perf vs technical-perf) also satisfies AI-04 at the scoring layer.

Output: crawler/src/pipeline/types.ts (all pipeline interfaces), crawler/src/pipeline/stage1-scorer.ts (threshold rules), crawler/src/pipeline/stage1-scorer.test.ts (AI-01 and AI-04 unit tests passing).
</objective>

<execution_context>
@C:\Users\STL-LT-068\Desktop\feeltrace\.planning\phases\03-ai-pipeline\03-RESEARCH.md
@C:\Users\STL-LT-068\Desktop\feeltrace\.planning\phases\03-ai-pipeline\03-PATTERNS.md
</execution_context>

<context>
@C:\Users\STL-LT-068\Desktop\feeltrace\.planning\ROADMAP.md
@C:\Users\STL-LT-068\Desktop\feeltrace\.planning\phases\03-ai-pipeline\03-RESEARCH.md
@C:\Users\STL-LT-068\Desktop\feeltrace\.planning\phases\03-ai-pipeline\03-PATTERNS.md

<interfaces>
<!-- From crawler/src/lib/types.ts — signal input types for Stage 1 -->
export interface CrawlPass {
  viewport: 'mobile' | 'desktop'
  domSignals: DOMSignals       // maxDOMDepth, missingAltCount, formWithoutLabelCount, ...
  cssSignals: CSSSignals       // unusedCSSPercent, fontDisplayStrategies.block, paintTriggerPropertyCount, ...
  jsSignals: JSSignals         // totalJSBytes, renderBlockingCount, unusedJSPercent, ...
  networkSignals: NetworkSignals  // firstRequestTTFB, renderBlockingCount, oversizedImageCount, cdnCount, ...
}

<!-- From prisma/schema.prisma — DB write target shape for Issue -->
model Issue {
  severity              Int     // Critical=4, High=3, Medium=2, Low=1
  category              String  // 'perceived-perf' | 'technical-perf' | 'accessibility'
  signal_source         String  // e.g. "networkSignals.firstRequestTTFB (mobile)"
  raw_evidence          String  // e.g. "2400ms (threshold: >2000ms Critical)"
  technical_description String  // filled in Stage 2
}

<!-- Vitest test pattern from crawler/src/extractors/js.test.ts -->
// @vitest-environment node
import { describe, it, expect } from 'vitest'
import { classifyScripts } from './js'

function makeScript(overrides = {}) {
  return { async: false, defer: false, type: 'text/javascript', src: '', inHead: false, ...overrides }
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define pipeline type contracts in types.ts</name>
  <files>crawler/src/pipeline/types.ts</files>
  <read_first>
    crawler/src/lib/types.ts — canonical signal types; pipeline/types.ts must NOT redeclare these, only import from them
    crawler/src/pipeline/stage1-scorer.test.ts (will not exist yet — but read types.ts first to align contracts)
  </read_first>
  <behavior>
    - ScoredIssue has fields: category ('perceived-perf' | 'technical-perf' | 'accessibility'), signal_source (string), severity (1 | 2 | 3 | 4), raw_evidence (string), viewport ('mobile' | 'desktop' | 'both')
    - EnrichedIssue extends ScoredIssue with: technical_description (string)
    - CausalEdgeCandidate has: fromIndex (number), toIndex (number), mechanism (string), relationship (string), confidence ('high' | 'medium' | 'low'), explanation (string)
    - NarrativeResult has: summary (string), perceivedPerformance (string), technicalPerformance (string), recommendations (string[])
    - SEVERITY_LABELS constant exported: { 1: 'Low', 2: 'Medium', 3: 'High', 4: 'Critical' }
    - PERMITTED_MECHANISMS exported as readonly string array of the 13 mechanism strings from RESEARCH.md
  </behavior>
  <action>
    Create crawler/src/pipeline/types.ts. Follow the export interface style from crawler/src/lib/types.ts — no default exports, no classes, export interface declarations only.

    Add a top-of-file comment: "// Phase 3 AI pipeline type contracts. Source: .planning/phases/03-ai-pipeline/03-RESEARCH.md"

    Declare interfaces in this order: ScoredIssue, EnrichedIssue, CausalEdgeCandidate, NarrativeResult.

    After the interfaces, export two constants:
    - SEVERITY_LABELS: a const Record mapping numeric severity to label strings, with values 1→'Low', 2→'Medium', 3→'High', 4→'Critical'. This is used by Phase 4 display layer per PATTERNS.md instructions.
    - PERMITTED_MECHANISMS: a readonly string array listing exactly the 13 mechanism strings from RESEARCH.md Pattern 2 (render-blocking-delays-fcp, ttfb-delays-fcp, large-js-bundle-delays-tti, render-blocking-js-delays-tti, unused-js-inflates-bundle, font-block-causes-foit, missing-cdn-increases-ttfb, oversized-images-increase-lcp, deep-dom-increases-layout-cost, paint-trigger-properties-cause-jank, unlabelled-forms-block-conversion, excessive-css-inflates-render-blocking, third-party-scripts-contend-bandwidth). This array is the single source of truth referenced in both stage2-reasoner.ts and the zod validation schema.

    Use type-only imports for any external types needed (there are none — types.ts is self-contained).
  </action>
  <verify>
    <automated>cd crawler && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    crawler/src/pipeline/types.ts exists. `npx tsc --noEmit` exits 0 from crawler/. All five exports present: ScoredIssue, EnrichedIssue, CausalEdgeCandidate, NarrativeResult, SEVERITY_LABELS, PERMITTED_MECHANISMS.
  </done>
  <acceptance_criteria>
    - crawler/src/pipeline/types.ts exists
    - File exports ScoredIssue interface with fields: category, signal_source, severity, raw_evidence, viewport
    - File exports EnrichedIssue interface with field: technical_description (in addition to ScoredIssue fields)
    - File exports CausalEdgeCandidate interface with fields: fromIndex, toIndex, mechanism, relationship, confidence, explanation
    - File exports NarrativeResult interface with fields: summary, perceivedPerformance, technicalPerformance, recommendations
    - File exports SEVERITY_LABELS constant (grep: `export const SEVERITY_LABELS`)
    - File exports PERMITTED_MECHANISMS constant with exactly 13 entries
    - `cd crawler && npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Stage 1 scorer and tests</name>
  <files>crawler/src/pipeline/stage1-scorer.ts, crawler/src/pipeline/stage1-scorer.test.ts</files>
  <read_first>
    crawler/src/pipeline/types.ts — ScoredIssue interface (just created in Task 1)
    crawler/src/lib/types.ts — CrawlPass and all signal interfaces
    crawler/src/extractors/js.ts — classifyScripts pattern (exported pure function with imperative threshold logic)
    crawler/src/extractors/js.test.ts — fixture builder + describe/it test patterns
    .planning/phases/03-ai-pipeline/03-RESEARCH.md — full threshold table in Pattern 1 and deduplication rule
  </read_first>
  <behavior>
    stage1-scorer.test.ts required cases (from RESEARCH.md Validation Architecture):

    AI-01 threshold tests:
    - TTFB = 2400ms (above 2000ms threshold): scoreSignals output contains one issue with severity 4
    - TTFB = 900ms (above 800ms, below 2000ms): contains one issue with severity 3
    - TTFB = 200ms (below all thresholds): no TTFB issue emitted
    - totalJSBytes = 600000 on mobile only: one issue with severity 4
    - renderBlockingCount (network) = 6: one issue with severity 4
    - fontDisplayStrategies.block = 1: one issue with severity 3

    AI-01 deduplication test:
    - Mobile TTFB = 2400ms AND desktop TTFB = 2400ms: exactly ONE issue emitted (not two), viewport field is 'both'
    - Mobile TTFB = 2400ms (Critical) AND desktop TTFB = 900ms (High): TWO issues emitted (different severity levels)

    AI-04 category tests:
    - TTFB issue has category 'perceived-perf'
    - totalJSBytes issue has category 'technical-perf'
    - missingAltCount issue has category 'accessibility'
    - fontDisplayStrategies.block issue has category 'perceived-perf'
  </behavior>
  <action>
    Write the test file first (TDD red phase), then implement to make them pass.

    TEST FILE (crawler/src/pipeline/stage1-scorer.test.ts):
    - Directive: `// @vitest-environment node`
    - Import: `import { describe, it, expect } from 'vitest'` and `import { scoreSignals } from './stage1-scorer'`
    - Create a `makeCrawlPass(viewport: 'mobile' | 'desktop', overrides?)` fixture builder that returns a CrawlPass with all signal values set to zero/empty (below all thresholds). Overrides merge into the specific signal sub-object being tested.
    - Organize tests into describe blocks: 'network signal thresholds', 'JS signal thresholds', 'CSS signal thresholds', 'DOM signal thresholds', 'deduplication', 'category assignment (AI-04)'. Each describe has it() per threshold boundary.

    IMPLEMENTATION FILE (crawler/src/pipeline/stage1-scorer.ts):
    - Import: `import type { CrawlPass } from '../lib/types'` and `import type { ScoredIssue } from './types'`
    - Export a single function: `export function scoreSignals(mobile: CrawlPass, desktop: CrawlPass): ScoredIssue[]`
    - Implement all 23 threshold rules from RESEARCH.md Pattern 1 threshold table (TTFB thresholds, renderBlockingCount, oversizedImageCount, totalJSBytes mobile, JS renderBlockingCount, unusedJSPercent, unusedCSSPercent, fontDisplayStrategies.block, paintTriggerPropertyCount, missingAltCount, formWithoutLabelCount, maxDOMDepth, cdnCount === 0).
    - Implement deduplication: for each threshold rule, check both mobile and desktop. If both exceed the same threshold level, emit one issue with viewport 'both' and raw_evidence from the worse viewport. Emit separate issues only when the two viewports trigger different severity levels for the same signal.
    - Implement a private helper `deduplicateIssues(issues: ScoredIssue[]): ScoredIssue[]` that filters duplicate signal_source + severity combinations. This helper must be testable (export it for unit tests or test via scoreSignals behavior tests).
    - Severity mapping: Critical=4, High=3, Medium=2, Low=1. Document this in a module-level comment: `// Severity: 4=Critical, 3=High, 2=Medium, 1=Low — maps to Issue.severity Int in Prisma schema`.
    - raw_evidence format: `"${value}${unit} (threshold: >${threshold}${unit} ${label})"` e.g. `"2400ms (threshold: >2000ms Critical)"`.
    - Use `const issues: ScoredIssue[] = []` initialized before threshold checks; push to it; return `deduplicateIssues(issues)` at end.
    - No I/O, no async, no external dependencies — pure synchronous function.
  </action>
  <verify>
    <automated>npm test -- --run crawler/src/pipeline/stage1-scorer.test.ts</automated>
  </verify>
  <done>
    All stage1-scorer.test.ts tests pass. `npm test -- --run crawler/src/pipeline/stage1-scorer.test.ts` exits 0. scoreSignals is a pure synchronous function with no I/O.
  </done>
  <acceptance_criteria>
    - crawler/src/pipeline/stage1-scorer.ts exists and exports `scoreSignals`
    - crawler/src/pipeline/stage1-scorer.test.ts exists with `// @vitest-environment node` directive
    - `npm test -- --run crawler/src/pipeline/stage1-scorer.test.ts` exits 0 with all tests passing
    - TTFB 2400ms input produces output item with severity 4 and category 'perceived-perf'
    - TTFB 900ms input produces output item with severity 3
    - Mobile+desktop same TTFB threshold: output array length is 1 (deduplication confirmed)
    - totalJSBytes 600000 (mobile) input produces output item with category 'technical-perf'
    - `cd crawler && npx tsc --noEmit` still exits 0 after adding both files
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signal data → scorer | CrawlPass values are numeric/enumerated — no string content from page HTML passes through |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | stage1-scorer threshold logic | accept | Stage 1 is pure deterministic code with unit tests; no external input surfaces — thresholds are constants in source |
| T-03-04 | Denial of Service | oversized signal arrays (entries: HAREntry[]) | accept | HAR entries are already aggregated into counts by Stage 2 extractors; scorer only reads aggregate fields, not raw entries array |
</threat_model>

<verification>
- `npm test -- --run crawler/src/pipeline/stage1-scorer.test.ts` exits 0
- `cd crawler && npx tsc --noEmit` exits 0
- crawler/src/pipeline/types.ts exports all 6 items: ScoredIssue, EnrichedIssue, CausalEdgeCandidate, NarrativeResult, SEVERITY_LABELS, PERMITTED_MECHANISMS
- TTFB 2400ms → severity 4 + category 'perceived-perf' (verified by test)
- Mobile+desktop same threshold → single issue with viewport 'both' (verified by test)
</verification>

<success_criteria>
Pipeline type contracts defined. Stage 1 scorer implemented as a pure function with all 23 threshold rules. Deduplication logic working. AI-01 (deterministic severity) and AI-04 (category classification) verified by passing unit tests.
</success_criteria>

<output>
Create `.planning/phases/03-ai-pipeline/03-02-SUMMARY.md` when done
</output>
