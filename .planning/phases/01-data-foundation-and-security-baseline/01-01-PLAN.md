---
phase: 01-data-foundation-and-security-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - postcss.config.mjs
  - vitest.config.mts
  - .env.local
  - .gitignore
  - src/app/layout.tsx
  - src/app/globals.css
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - CRAWL-01

must_haves:
  truths:
    - "Running `npm run dev` starts Next.js on port 3000 without errors"
    - "Running `npm run test:run` executes the vitest suite without crashing the runner"
    - "Running `npm run typecheck` exits 0 with no TypeScript errors"
    - "Running `npm run lint` exits 0 with no ESLint errors"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with all Phase 1 dependencies declared"
      contains: "@upstash/qstash"
    - path: "vitest.config.mts"
      provides: "Vitest configuration with jsdom environment and path aliases"
      contains: "defineConfig"
    - path: "tsconfig.json"
      provides: "TypeScript config with @/ path alias pointing to src/"
      contains: "paths"
    - path: ".env.local"
      provides: "Local environment variable stubs (values filled by developer)"
  key_links:
    - from: "vitest.config.mts"
      to: "src/lib/ssrf.test.ts"
      via: "test environment: jsdom + tsconfigPaths"
      pattern: "tsconfigPaths"
    - from: "tsconfig.json"
      to: "src/**"
      via: "@/ path alias"
      pattern: '"@/\\*"'

user_setup:
  - service: neon
    why: "PostgreSQL database for job persistence"
    env_vars:
      - name: DATABASE_URL
        source: "Neon Console → Project → Connection Details → select 'Pooled connection' → copy (hostname has -pooler suffix, port 6543)"
      - name: DIRECT_URL
        source: "Neon Console → Project → Connection Details → select 'Direct connection' → copy (no -pooler suffix, port 5432)"
  - service: upstash-qstash
    why: "Job queue — QStash delivers crawl jobs to Railway"
    env_vars:
      - name: QSTASH_TOKEN
        source: "Upstash Console → QStash tab → API Keys → QSTASH_TOKEN"
      - name: QSTASH_CURRENT_SIGNING_KEY
        source: "Upstash Console → QStash tab → API Keys → QSTASH_CURRENT_SIGNING_KEY"
      - name: QSTASH_NEXT_SIGNING_KEY
        source: "Upstash Console → QStash tab → API Keys → QSTASH_NEXT_SIGNING_KEY"
  - service: upstash-redis
    why: "Rate limit counter storage — separate Redis database from QStash"
    dashboard_config:
      - task: "Create a new Redis database (separate from QStash account, same Upstash account)"
        location: "Upstash Console → Redis tab → Create Database"
    env_vars:
      - name: UPSTASH_REDIS_REST_URL
        source: "Upstash Console → Redis → your database → REST API → UPSTASH_REDIS_REST_URL"
      - name: UPSTASH_REDIS_REST_TOKEN
        source: "Upstash Console → Redis → your database → REST API → UPSTASH_REDIS_REST_TOKEN"
  - service: railway-stub
    why: "QStash needs a valid HTTPS destination URL to deliver jobs to; Railway stub accepts POST and returns 200"
    env_vars:
      - name: RAILWAY_CRAWLER_URL
        source: "Railway Console → your stub service → Settings → Domains → copy the public HTTPS URL, append /api/crawl"
---

<objective>
Bootstrap the FeelTrace Next.js project from a completely empty directory: initialize the project, install all Phase 1 dependencies, configure TypeScript path aliases, configure Vitest, and create the .env.local stub with all required environment variable names.

Purpose: Every subsequent plan depends on this scaffold existing. Without package.json, tsconfig.json, and vitest.config.mts, no task can install packages, resolve @/ imports, or run tests.

Output: A runnable Next.js 15 project with all Phase 1 dependencies installed, Vitest configured, TypeScript path aliases set up, and a .env.local template ready for developer credentials.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation-and-security-baseline/01-CONTEXT.md
@.planning/phases/01-data-foundation-and-security-baseline/01-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provision external services</name>
  <what-built>
    External services (Neon, Upstash QStash, Upstash Redis, Railway stub) are required before any environment variables can be populated. Claude cannot create external service accounts.
  </what-built>
  <how-to-verify>
    Step 1 — Provision Neon PostgreSQL (neon.com, free tier):
      - Create a new project
      - From Connection Details: copy both the Pooled connection string (DATABASE_URL, -pooler hostname, port 6543) and the Direct connection string (DIRECT_URL, no -pooler, port 5432)

    Step 3 — Provision Upstash QStash (console.upstash.com, free tier):
      - Go to QStash tab
      - Copy the three QStash API keys: QSTASH_TOKEN, QSTASH_CURRENT_SIGNING_KEY, QSTASH_NEXT_SIGNING_KEY

    Step 4 — Provision Upstash Redis (console.upstash.com, free tier):
      - Go to Redis tab
      - Create a NEW database (separate from QStash)
      - Copy UPSTASH_REDIS_REST_URL and UPSTASH_REDIS_REST_TOKEN from the REST API section

    Step 5 — Deploy Railway stub (railway.app, free tier):
      - Create a new project
      - Deploy a minimal Node.js/Express service that accepts POST on any path and returns HTTP 200
      - Copy the public HTTPS URL, append /api/crawl to form the RAILWAY_CRAWLER_URL value

    All four steps complete, all env var values are in hand.
  </how-to-verify>
  <resume-signal>Type "ready" when all service credentials are collected</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Scaffold Next.js project and install all Phase 1 dependencies</name>
  <read_first>
    - .planning/phases/01-data-foundation-and-security-baseline/01-RESEARCH.md (Standard Stack section — exact package names and versions; Installation command block; Pitfall 1 on Prisma 7 import path; Vitest Configuration code example)
    - CLAUDE.md (commands section — npm run dev, npm test, npm run build; sfw prefix requirement)
  </read_first>
  <files>package.json, package-lock.json, tsconfig.json, next.config.ts, tailwind.config.ts, postcss.config.mjs, vitest.config.mts, .env.local, .gitignore, src/app/layout.tsx, src/app/globals.css</files>
  <action>
    Step 1 — Create Next.js app with App Router. Run from the feeltrace project root (not inside a subfolder):
      npx create-next-app@15 . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm
    Answer the prompts: accept defaults, use TypeScript, use Tailwind, use App Router, use src/ directory, use @/* alias.

    Step 2 — Install production dependencies using sfw prefix (CLAUDE.md requirement):
      sfw npm install @prisma/adapter-neon@7.8.0 @neondatabase/serverless@1.1.0 @upstash/qstash@2.11.0 @upstash/ratelimit@2.0.8 @upstash/redis@1.38.0 zod@4.4.3

    Step 3 — Install dev dependencies using sfw prefix:
      sfw npm install --save-dev prisma@7.8.0 vitest@4.1.7 @vitejs/plugin-react vite-tsconfig-paths @testing-library/react @testing-library/dom

    Step 4 — Write vitest.config.mts at project root. Contents per RESEARCH.md Vitest Configuration example:
      - import defineConfig from vitest/config
      - plugins: [tsconfigPaths(), react()]
      - test.environment: 'jsdom'

    Step 5 — Add npm scripts to package.json (these add to or replace create-next-app defaults):
      - "test": "vitest"
      - "test:run": "vitest run"
      - "typecheck": "tsc --noEmit"
      - "db:push": "prisma db push"
      - "db:migrate": "prisma migrate deploy"
      - "db:studio": "prisma studio"
      (dev, build, lint are set by create-next-app; run via npm run dev, npm run build, npm run lint)

    Step 6 — Verify tsconfig.json has the @/* path alias pointing to ./src/*. create-next-app with --import-alias "@/*" sets this automatically; confirm the paths block contains the mapping.

    Step 7 — Create .env.local at project root. It must contain these eight variable names (fill placeholder values — the developer will replace them with real credentials from Step 1 of Task 1):
      Variable names required (one per line, key=placeholder format):
        DATABASE_URL — set to the Neon pooled connection string (hostname has -pooler, port 6543)
        DIRECT_URL — set to the Neon direct connection string (no -pooler, port 5432)
        QSTASH_TOKEN — set to the value from Upstash QStash API Keys
        QSTASH_CURRENT_SIGNING_KEY — set to the value from Upstash QStash API Keys
        QSTASH_NEXT_SIGNING_KEY — set to the value from Upstash QStash API Keys
        UPSTASH_REDIS_REST_URL — set to the Redis REST URL from Upstash Redis database
        UPSTASH_REDIS_REST_TOKEN — set to the Redis REST token from Upstash Redis database
        RAILWAY_CRAWLER_URL — set to the Railway stub HTTPS URL with /api/crawl appended

    Step 8 — Ensure .gitignore contains .env.local (create-next-app adds this by default; verify it is present).

    IMPORTANT: Do not add @prisma/client to any import anywhere — Prisma 7 imports come from ../generated/prisma or @/generated/prisma. This is addressed in Plan 03 when prisma.ts is written.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - package.json exists and contains "@upstash/qstash": "2.11.0", "@upstash/ratelimit": "2.0.8", "@upstash/redis": "1.38.0", "@prisma/adapter-neon": "7.8.0", "zod": "4.4.3", "prisma": "7.8.0" (dev), "vitest": "4.1.7" (dev)
    - package.json scripts contain "test", "typecheck", "db:push", "db:migrate", "db:studio"
    - vitest.config.mts exists at project root and contains "tsconfigPaths" and "jsdom"
    - tsconfig.json contains the @/* path alias mapping pointing to ./src/*
    - .env.local exists and contains the variable names DATABASE_URL, DIRECT_URL, QSTASH_TOKEN, UPSTASH_REDIS_REST_URL, RAILWAY_CRAWLER_URL (values populated by developer)
    - .gitignore contains .env.local
    - npm run typecheck exits 0
    - npm run lint exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer workstation → external services | Credentials for Neon, Upstash, Railway collected and stored in .env.local |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env.local | mitigate | .gitignore must contain .env.local; verified in acceptance criteria; never commit credentials |
| T-01-02 | Tampering | npm package installs | mitigate | All installs use sfw npm install per CLAUDE.md; slopcheck runs before install |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + Package Legitimacy Audit in RESEARCH.md verified all packages; no ASSUMED or SUS packages remain |
</threat_model>

<verification>
After both tasks complete:
- npm run dev starts without errors (Next.js compiles successfully on port 3000)
- npm run typecheck exits 0
- npm run lint exits 0
- npm run test:run exits 0 (no test files yet — vitest exits cleanly with 0 tests)
- .env.local exists, is gitignored, and contains all 8 required variable names
</verification>

<success_criteria>
Next.js 15 project is scaffolded with all Phase 1 dependencies installed. Vitest, TypeScript, and npm scripts are configured. Developer has all external service credentials collected and .env.local populated with real values. The project compiles and typechecks cleanly.
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation-and-security-baseline/01-01-SUMMARY.md` when done.
</output>
