---
phase: 01-data-foundation-and-security-baseline
plan: 04
type: execute
wave: 3
depends_on:
  - 01-03
files_modified:
  - src/generated/prisma/
autonomous: true
requirements:
  - INFRA-03

must_haves:
  truths:
    - "pnpm db:push exits 0 — schema applied to Neon without errors"
    - "Prisma client is generated at src/generated/prisma/ — the generated types directory exists"
    - "All four models (Job, Result, Issue, CausalEdge) exist in the Neon database after push"
    - "The JobStatus enum exists in the database as a PostgreSQL enum type"
    - "pnpm typecheck exits 0 after generation — the 'Cannot find module ../generated/prisma' error is resolved"
  artifacts:
    - path: "src/generated/prisma/"
      provides: "Prisma-generated TypeScript client — PrismaClient, all model types, JobStatus enum"
      contains: "PrismaClient"
  key_links:
    - from: "src/lib/prisma.ts"
      to: "src/generated/prisma"
      via: "Prisma generate writes client to this path; import resolves after generation"
      pattern: "PrismaClient"
---

<objective>
[BLOCKING] Run prisma generate to produce the TypeScript client at src/generated/prisma/, then run pnpm db:push to apply the schema to the Neon PostgreSQL database. This must complete before any API route that queries the DB can be verified.

Purpose: No subsequent code that imports from @/lib/prisma or queries the DB can be tested without the generated client and applied schema. Plans 05 and 06 depend on this plan completing.

Output: src/generated/prisma/ directory with generated Prisma client; all four tables created in Neon.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-foundation-and-security-baseline/01-CONTEXT.md
@.planning/phases/01-data-foundation-and-security-baseline/01-RESEARCH.md
@.planning/phases/01-data-foundation-and-security-baseline/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: [BLOCKING] Generate Prisma client and push schema to Neon</name>
  <read_first>
    - prisma/schema.prisma (verify output = "../src/generated/prisma" before generating)
    - prisma.config.ts (verify DIRECT_URL is referenced — used for db:push)
    - .env.local (verify DATABASE_URL and DIRECT_URL are populated with real Neon credentials)
    - .planning/phases/01-data-foundation-and-security-baseline/01-RESEARCH.md (Pitfall 1 — generated path; Pitfall 2 — pooler vs direct URL; schema_push_requirement note from planning context)
  </read_first>
  <files>src/generated/prisma/</files>
  <action>
    Step 1 — Verify prerequisites before running any Prisma commands:
    - Confirm .env.local contains DATABASE_URL with -pooler in hostname (port 6543)
    - Confirm .env.local contains DIRECT_URL without -pooler (port 5432)
    - Confirm prisma/schema.prisma generator block has output = "../src/generated/prisma"
    - Confirm prisma.config.ts references env('DIRECT_URL')

    Step 2 — Generate the Prisma TypeScript client:
      pnpm exec prisma generate
    This writes the generated client to src/generated/prisma/ per the output path in schema.prisma.
    After this step, "Cannot find module '../generated/prisma'" TypeScript errors are resolved.

    Step 3 — Push schema to Neon (creates all tables and enum):
      pnpm db:push
    This uses prisma.config.ts (DIRECT_URL, port 5432) to apply the schema.
    On a fresh Neon database this creates: Job, Result, Issue, CausalEdge tables and the JobStatus enum.
    This is a GREENFIELD schema push — no data loss is possible, so --accept-data-loss flag is not needed.

    Step 4 — Verify TypeScript now resolves cleanly:
      pnpm typecheck
    The "Cannot find module '../generated/prisma'" error should be gone.

    If db:push fails with a connection error:
    - Verify DIRECT_URL in .env.local is the correct Neon direct connection string (no -pooler, port 5432)
    - Verify the Neon database is active (Neon free tier suspends after inactivity)
    - Re-run pnpm db:push after fixing the URL
  </action>
  <verify>
    <automated>pnpm db:push && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - pnpm exec prisma generate exits 0 and creates src/generated/prisma/ directory
    - pnpm db:push exits 0 with no error output
    - src/generated/prisma/ directory exists and contains index.d.ts (or equivalent TypeScript declarations)
    - pnpm typecheck exits 0 (no 'Cannot find module ../generated/prisma' errors)
    - Running pnpm exec prisma studio (optional) shows Job, Result, Issue, CausalEdge tables in the schema browser
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prisma CLI → Neon PostgreSQL via DIRECT_URL | DIRECT_URL (port 5432) is used for schema migration only; requires direct DB access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Information Disclosure | DIRECT_URL in .env.local | mitigate | .env.local is gitignored (Plan 01 confirmed); DIRECT_URL is only used by Prisma CLI, never by application runtime |
| T-01-11 | Tampering | Schema push to wrong database | accept | Schema push is idempotent on greenfield DB; no existing data to corrupt; DIRECT_URL points to the intended Neon project |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new installs; pnpm exec prisma is from already-installed prisma dev dependency |
</threat_model>

<verification>
After the task completes:
- pnpm db:push exits 0
- pnpm typecheck exits 0
- src/generated/prisma/ directory exists with generated Prisma client files
- All API routes in Plan 05 can now import { prisma } from '@/lib/prisma' without TypeScript errors
</verification>

<success_criteria>
Neon PostgreSQL has all four Phase 1 tables (Job, Result, Issue, CausalEdge) and the JobStatus enum. The Prisma TypeScript client is generated at src/generated/prisma/. Subsequent plans can write DB queries against Prisma without any additional schema setup.
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation-and-security-baseline/01-04-SUMMARY.md` when done.
</output>
