---
phase: 01-data-foundation-and-security-baseline
plan: 08
type: execute
wave: 6
depends_on:
  - 01-07
files_modified: []
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - CRAWL-01

must_haves:
  truths:
    - "User submits a valid URL and receives a job ID; status badge shows 'pending' within 2 seconds"
    - "User submits a private IP URL (e.g., http://192.168.1.1) and sees an inline validation error"
    - "User submits more than 5 URLs from the same IP within 1 hour and receives a 429 error displayed inline"
    - "pnpm test --run exits 0 with all unit tests passing (SSRF tests + API route tests)"
    - "Neon database has at least one Job record after a valid submission"
    - "Job record status is 'pending' (since Phase 2 crawler is not built yet)"
  artifacts:
    - path: ".planning/phases/01-data-foundation-and-security-baseline/01-08-SUMMARY.md"
      provides: "Human verification sign-off with observed behaviors and any issues found"
  key_links:
    - from: "Browser"
      to: "http://localhost:3000"
      via: "pnpm dev"
      pattern: "localhost:3000"
---

<objective>
End-to-end smoke test and human verification of the Phase 1 walking skeleton. The developer runs the app locally, exercises the happy path and key error cases, and confirms the full stack works: browser → Edge Middleware → API route → Neon DB → polling display.

Purpose: Automated tests cover the SSRF validator and API route logic in isolation. This checkpoint verifies the full integrated stack: real Neon database, real Upstash Redis rate limiter, real QStash publish call, real DOM rendering.

Output: Human sign-off that Phase 1 is working end-to-end, documented in the plan summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-foundation-and-security-baseline/01-CONTEXT.md
@.planning/phases/01-data-foundation-and-security-baseline/01-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run full automated test suite and production build</name>
  <read_first>
    - .planning/phases/01-data-foundation-and-security-baseline/01-RESEARCH.md (Validation Architecture section — sampling rate, phase gate requirements)
  </read_first>
  <files></files>
  <action>
    Run the complete automated validation sequence. All commands must exit 0:

    Step 1 — Full unit test suite:
      pnpm test --run
    Expected: SSRF tests (9 passing) + API route tests (5 passing) = 14 tests total, 0 failures

    Step 2 — TypeScript type check:
      pnpm typecheck
    Expected: exits 0, no errors

    Step 3 — Linter:
      pnpm lint
    Expected: exits 0, no ESLint warnings or errors

    Step 4 — Production build:
      pnpm build
    Expected: exits 0; Next.js compiles all pages and API routes without errors

    If any step fails, fix the underlying issue before proceeding to the human verification task. Do not skip or --force past failures.
  </action>
  <verify>
    <automated>pnpm test --run && pnpm typecheck && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - pnpm test --run exits 0 with at least 14 tests passing (9 SSRF + 5 API route)
    - pnpm typecheck exits 0
    - pnpm lint exits 0
    - pnpm build exits 0
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human end-to-end smoke test</name>
  <what-built>
    Full walking skeleton: Next.js app (pnpm dev) → AnalyzeForm → POST /api/analyze (Edge Middleware rate limit + SSRF check) → Job created in Neon → JobStatusBadge polls GET /api/jobs/[jobId] every 2 seconds → status displays 'pending' (job stays pending since Phase 2 crawler is not built).
  </what-built>
  <how-to-verify>
    Step 1 — Start the dev server:
      pnpm dev
    Visit http://localhost:3000 — you should see the AnalyzeForm with a URL input and Analyze button.

    Step 2 — Happy path (valid public URL):
      Enter: https://example.com
      Click Analyze
      Expected:
        - The form shows a job ID or the status badge appears
        - Status badge shows "pending" within 2 seconds
        - Status remains "pending" (no crawler is running in Phase 1 — this is correct)

    Step 3 — SSRF blocked URL:
      Enter: http://192.168.1.1
      Click Analyze
      Expected: Inline error message referencing "blocked" or "private" — no job created

    Step 4 — Invalid URL format:
      Enter: not-a-url
      Click Analyze
      Expected: Inline error message about invalid URL format

    Step 5 — Rate limit (requires 6 submissions in quick succession):
      Click Analyze 6 times within 1 minute (reuse any URL)
      Expected: 6th submission returns inline error with "Too many requests" message and retry time
      Note: In local dev, all requests share the '127.0.0.1' IP — limit applies to all local requests combined

    Step 6 — Verify Neon database has a Job record:
      Run: pnpm db:studio
      Open the Job table — at least one record should exist with status 'pending' and a URL from Step 2

    Step 7 — Verify QStash received the publish:
      Open Upstash Console → QStash → Messages
      Confirm at least one message was published (it will fail delivery since the Railway stub may not be set up yet — the delivery failure is expected in Phase 1)

    All 7 steps complete without unexpected errors.
  </how-to-verify>
  <human-check>
    Confirm each step worked as expected. If Step 7 (QStash delivery) shows "failed" deliveries, that is expected — Railway crawler is Phase 2.
  </human-check>
  <resume-signal>Type "approved" if all steps passed, or describe any failures</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human tester → localhost:3000 | Integration test exercises all trust boundaries established in Plans 01–07 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-22 | Denial of Service | Local development rate limit sharing | accept | All local requests share '127.0.0.1' IP; rate limit applies globally in dev — acceptable for testing; Vercel production provides real per-IP enforcement |
| T-01-23 | Tampering | QStash delivery failures in Phase 1 | accept | Without Railway crawler (Phase 2), QStash messages will exhaust retries; job stays 'pending'; this is expected Phase 1 behavior documented in CONTEXT.md |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new installs in this plan |
</threat_model>

<verification>
Phase 1 is complete when:
- pnpm test --run exits 0 (14+ tests)
- pnpm build exits 0
- Human verification step is approved
- At least one Job record exists in Neon with status 'pending'
- The SSRF error case shows an inline error (no DB write for blocked URLs)
- The rate limit error case shows an inline error with retry time
</verification>

<success_criteria>
Phase 1 walking skeleton is verified end-to-end by a human developer. All five success criteria from ROADMAP.md Phase 1 are confirmed:
1. User submits URL and sees job ID + polling status — verified in Step 2
2. Private IP submission returns validation error — verified in Step 3
3. Rate limit exceeded returns 429 — verified in Step 5
4. Job record persisted in DB without raw signals — verified in Step 6 (only url + status columns, no HTML/CSS data)
5. Polling endpoint returns correct status — verified in Step 2 (status: 'pending' displayed)
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation-and-security-baseline/01-08-SUMMARY.md` when done.
</output>
