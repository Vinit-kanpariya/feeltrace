---
phase: 05-tech-debt-and-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crawler/src/lib/gemini.ts
  - crawler/package.json
  - crawler/src/lib/types.ts
autonomous: true
requirements:
  - DEBT-01
  - DEBT-03

must_haves:
  truths:
    - "No file named gemini.ts exists anywhere in the crawler package"
    - "@google/generative-ai does not appear in crawler/package.json dependencies"
    - "TechProfile.database, TechProfile.auth, TechProfile.payments, and TechProfile.services are optional (?) in crawler/src/lib/types.ts"
    - "Both TechProfile interfaces (app and crawler) compile without errors and agree that database/auth/payments/services may be absent"
  artifacts:
    - path: "crawler/src/lib/gemini.ts"
      provides: "DELETED — must not exist after plan executes"
    - path: "crawler/package.json"
      provides: "Dependency list without @google/generative-ai"
    - path: "crawler/src/lib/types.ts"
      provides: "TechProfile with optional backend fields"
  key_links:
    - from: "crawler/src/lib/types.ts TechProfile"
      to: "src/types/tech.ts TechProfile"
      via: "shared field optionality contract"
      pattern: "database\\?|auth\\?|payments\\?|services\\?"
---

<objective>
Remove the dead Gemini LLM integration from the crawler package and align the TechProfile type interface between the crawler and the Next.js app.

Purpose: The crawler migrated to Groq in v1.0 but never cleaned up the old Gemini code. gemini.ts is unreferenced dead code, @google/generative-ai is an unused 5 MB+ dependency inflating install time and attack surface. The TechProfile type mismatch (required in crawler, optional in app) causes TypeScript friction and potential runtime surprises.

Output:
- crawler/src/lib/gemini.ts deleted
- @google/generative-ai removed from crawler/package.json
- crawler/src/lib/types.ts TechProfile.database, .auth, .payments, .services marked optional
</objective>

<execution_context>
@C:\Users\STL-LT-068\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\STL-LT-068\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-tech-debt-and-foundation/05-CONTEXT.md

<interfaces>
<!-- Current TechProfile in crawler (before edit) — from crawler/src/lib/types.ts lines 130-151 -->
<!-- Fields to add ? to: database, auth, payments, services -->
Current (required):
  database: string | null
  auth: string | null
  payments: string | null
  services: string[]

Target (optional, matching src/types/tech.ts):
  database?: string | null
  auth?: string | null
  payments?: string | null
  services?: string[]

<!-- App TechProfile already correct — from src/types/tech.ts lines 8-24 -->
  database?: string | null
  auth?: string | null
  payments?: string | null
  services?: string[]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete gemini.ts and remove @google/generative-ai dependency (per D-01, D-02, D-03)</name>
  <files>crawler/src/lib/gemini.ts, crawler/package.json</files>
  <read_first>
    - crawler/src/lib/gemini.ts — confirm exact file contents before deletion
    - crawler/package.json — confirm @google/generative-ai version entry before removal
  </read_first>
  <action>
    1. Delete the file crawler/src/lib/gemini.ts entirely. The file exports only getGeminiClient() and imports only from @google/generative-ai. Confirm via grep that no other file in crawler/src/ imports from './lib/gemini' or '@google/generative-ai' before deleting.

    2. In crawler/package.json, remove the entry "@google/generative-ai": "^0.24.1" from the "dependencies" object. Do not touch any other dependency. The remaining dependencies after removal are: @hono/node-server, @neondatabase/serverless, @prisma/adapter-neon, @prisma/client, @upstash/qstash, @vercel/blob, dotenv, groq-sdk, hono, p-queue, playwright-core, zod.

    3. Run `npm install` inside the crawler/ directory to update package-lock.json and remove the package from node_modules. Use `sfw npm install` per project security policy (CLAUDE.md).
  </action>
  <verify>
    <automated>
      cd crawler && node -e "const fs=require('fs'); if(fs.existsSync('src/lib/gemini.ts')) process.exit(1); const pkg=JSON.parse(fs.readFileSync('package.json','utf8')); if(pkg.dependencies['@google/generative-ai']) process.exit(1); console.log('PASS');"
    </automated>
  </verify>
  <acceptance_criteria>
    - crawler/src/lib/gemini.ts does not exist on disk
    - crawler/package.json dependencies object contains no key named "@google/generative-ai"
    - `grep -r "@google/generative-ai" crawler/src/` returns no matches
    - `grep -r "gemini" crawler/src/` returns no matches (file and all import references gone)
  </acceptance_criteria>
  <done>gemini.ts deleted, @google/generative-ai absent from crawler/package.json, crawler install succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Add ? to backend fields in crawler TechProfile interface (per D-08, D-09)</name>
  <files>crawler/src/lib/types.ts</files>
  <read_first>
    - crawler/src/lib/types.ts — read the full TechProfile interface (lines 130-151) before editing
    - src/types/tech.ts — read the app TechProfile interface to confirm the target optionality pattern
  </read_first>
  <action>
    In crawler/src/lib/types.ts, locate the TechProfile interface (lines 130-151). Change exactly four field declarations:

    - `database: string | null` → `database?: string | null`
    - `auth: string | null` → `auth?: string | null`
    - `payments: string | null` → `payments?: string | null`
    - `services: string[]` → `services?: string[]`

    Do not change any other field (framework, rendering, cdn, hosting, buildTool, cssFramework, analytics, totalJsBundleKb, totalPageWeightKb, totalRequests, renderBlockingCount, thirdPartyScriptCount, unusedJsPercent all remain unchanged). Do not change any other interface in the file (DOMSignals, CSSSignals, JSSignals, HAREntry, NetworkSignals, CrawlPass, BrowserFingerprint stay unchanged).

    This is a TypeScript-only change — no runtime behavior changes. The crawler's tech-detector.ts still always writes these fields; making them optional is a type-level defensive measure (per D-09).
  </action>
  <verify>
    <automated>
      cd crawler && npx tsc --noEmit 2>&1 | head -20
    </automated>
  </verify>
  <acceptance_criteria>
    - crawler/src/lib/types.ts contains `database?: string | null` (with `?`)
    - crawler/src/lib/types.ts contains `auth?: string | null` (with `?`)
    - crawler/src/lib/types.ts contains `payments?: string | null` (with `?`)
    - crawler/src/lib/types.ts contains `services?: string[]` (with `?`)
    - `cd crawler && npx tsc --noEmit` exits with code 0 (no TypeScript errors)
    - No other interfaces in crawler/src/lib/types.ts are modified
  </acceptance_criteria>
  <done>TechProfile backend fields optional in crawler, tsc --noEmit passes with zero errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry | Package removal touches package.json and lock file — no new packages introduced, only removal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | crawler/package.json removal | mitigate | Use `sfw npm install` per CLAUDE.md to block malicious packages; this task only removes, does not add |
| T-05-SC | Tampering | npm install (lock file update) | mitigate | slopcheck via `sfw` prefix; no new packages introduced |
</threat_model>

<verification>
After both tasks complete:
1. `ls crawler/src/lib/gemini.ts` → "No such file"
2. `node -e "const p=require('./crawler/package.json'); console.log('@google/generative-ai' in p.dependencies)"` → false
3. `grep -r "gemini\|@google" crawler/src/` → no matches
4. `grep "database?\|auth?\|payments?\|services?" crawler/src/lib/types.ts` → 4 matches (with ?)
5. `cd crawler && npx tsc --noEmit` → exit 0
</verification>

<success_criteria>
- gemini.ts deleted, @google/generative-ai absent from package.json, crawler tsc passes
- TechProfile backend fields (database, auth, payments, services) are optional (?) in crawler/src/lib/types.ts
- Both TechProfile interfaces (app + crawler) agree on optional backend fields
</success_criteria>

<output>
Create `.planning/phases/05-tech-debt-and-foundation/05-01-SUMMARY.md` when done
</output>
