---
phase: 05-tech-debt-and-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/results/[jobId]/page.tsx
  - crawler/src/index.ts
autonomous: true
requirements:
  - DEBT-02
  - DEBT-04

must_haves:
  truths:
    - "Navigating to /results/[jobId] for a failed job shows a red error card with the failure reason (or fallback copy), not a 404 page"
    - "The error card has a Link back to / for retry"
    - "Navigating to /results/[jobId] with no job record at all still calls notFound()"
    - "Starting the crawler with RAILWAY_CRAWLER_URL missing or malformed exits with code 1 and a descriptive message"
    - "Starting the crawler with QSTASH_CURRENT_SIGNING_KEY or QSTASH_NEXT_SIGNING_KEY missing exits with code 1 and a descriptive message"
    - "Starting the crawler with all three required env vars present proceeds normally"
  artifacts:
    - path: "src/app/results/[jobId]/page.tsx"
      provides: "Failed-job branch that queries Job before calling notFound()"
      contains: "prisma.job.findUnique"
    - path: "crawler/src/index.ts"
      provides: "Env var validation before initQueue() and serve()"
      contains: "process.exit(1)"
  key_links:
    - from: "src/app/results/[jobId]/page.tsx"
      to: "prisma.job.findUnique"
      via: "!result branch before notFound()"
      pattern: "prisma\\.job\\.findUnique"
    - from: "crawler/src/index.ts start()"
      to: "process.exit(1)"
      via: "env validation block before initQueue()"
      pattern: "process\\.exit\\(1\\)"
---

<objective>
Fix the misleading "Results not found" 404 experience for failed analysis jobs, and add startup validation to the crawler so missing env vars fail fast with a clear error instead of silently crashing mid-request.

Purpose:
- DEBT-02: Users who submit a URL that the crawler fails to analyze currently see a generic 404-style "not found" page. The failure reason is lost. The fix queries the Job record when no Result exists and renders a descriptive error card matching the existing D-06 inline error pattern.
- DEBT-04: The crawler silently reaches its QStash signature verification before any env check, producing confusing TypeErrors with non-null assertions (!). Adding upfront validation catches misconfiguration at boot with a clear, actionable message.

Output:
- src/app/results/[jobId]/page.tsx: new !result branch that queries Job and renders red error card or calls notFound()
- crawler/src/index.ts: validateEnv() call at the top of start(), before initQueue()
</objective>

<execution_context>
@C:\Users\STL-LT-068\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\STL-LT-068\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-tech-debt-and-foundation/05-CONTEXT.md

<interfaces>
<!-- From src/app/results/[jobId]/page.tsx — current !result handling (line 68) -->
  if (!result) notFound()   ← REPLACE THIS with the new branch (see action below)

<!-- Existing D-06 error card pattern (page.tsx lines 73-85) — reuse exactly -->
  <div className="min-h-dvh bg-[#0f172a] flex flex-col">
    <ResultsHeader />
    <main className="flex-1 max-w-4xl mx-auto w-full px-6 py-12">
      <div className="rounded-xl bg-red-950/40 border border-red-800/50 p-6">
        <p className="text-sm font-semibold text-red-300 mb-1.5">Analysis failed</p>
        <p className="text-sm text-red-400">{result.job.error_message ?? '...'}</p>
      </div>
    </main>
  </div>

<!-- From crawler/src/index.ts — current start() function structure -->
async function start(): Promise<void> {
  await initQueue()
  const port = Number(process.env.PORT ?? 3000)
  serve({ fetch: app.fetch, port })
  console.log('[feeltrace-crawler] Server listening on port', port)
}
start().catch((err) => {
  console.error('[feeltrace-crawler] Failed to start:', err)
  process.exit(1)
})

<!-- Prisma Job model fields available in page.tsx -->
  job.status        // string — 'pending' | 'processing' | 'complete' | 'failed'
  job.error_message // string | null
  job.id            // string
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failed-job error page branch to results page (per D-04, D-05, D-06, D-07)</name>
  <files>src/app/results/[jobId]/page.tsx</files>
  <read_first>
    - src/app/results/[jobId]/page.tsx — read the full file before editing, especially lines 58-85 (result query and existing D-06 error card)
    - src/app/results/[jobId]/not-found.tsx — confirm this file stays unchanged (no edits needed)
  </read_first>
  <action>
    In src/app/results/[jobId]/page.tsx, replace the single-line `if (!result) notFound()` (currently line 68) with a two-step branch:

    Step 1: When result is null, query the Job record:
      const job = await prisma.job.findUnique({ where: { id: jobId } })

    Step 2: If no job record exists, call notFound() (preserves existing 404 behavior for unknown jobIds).

    Step 3: If the job record exists and job.status === 'failed', render the red error card. Reuse the same JSX structure as the existing D-06 error block (lines 73-85 of the current file), using the queried `job` variable:
    - Outer wrapper: `<div className="min-h-dvh bg-[#0f172a] flex flex-col">`
    - Include `<ResultsHeader />` for layout consistency
    - Inner card: `rounded-xl bg-red-950/40 border border-red-800/50 p-6`
    - Title text: `text-sm font-semibold text-red-300 mb-1.5` — "Analysis failed"
    - Body text: `text-sm text-red-400` — `{job.error_message ?? 'Analysis failed — try submitting again'}` (exact fallback copy per D-06)
    - Retry CTA: `<Link href="/">` with text "← Try again" placed below the body paragraph

    Important: The existing D-06 error block (lines 71-85) handles the case where `result` exists but result.job.status === 'failed'. That block stays unchanged. The new code only handles the `!result` path.

    Do not add a footer element to the new error branch — the existing D-06 pattern does not include one.
  </action>
  <verify>
    <automated>
      npm run typecheck 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - src/app/results/[jobId]/page.tsx contains `prisma.job.findUnique({ where: { id: jobId } })` inside the `!result` branch
    - The `!result` branch no longer calls `notFound()` unconditionally — it only calls `notFound()` when the queried job is also null
    - The new error card uses the exact CSS classes: `bg-red-950/40 border border-red-800/50 p-6`
    - Fallback copy when error_message is null: exact string `'Analysis failed — try submitting again'`
    - A `<Link href="/">` retry CTA exists in the new branch
    - `npm run typecheck` exits with code 0
    - The existing D-06 error block (result.job.status === 'failed' path) is unchanged
    - not-found.tsx is unchanged
  </acceptance_criteria>
  <done>Failed jobs show descriptive error card; unknown jobIds still call notFound(); typecheck passes</done>
</task>

<task type="auto">
  <name>Task 2: Add startup env var validation to crawler start() (per D-11, D-12, D-13, D-14)</name>
  <files>crawler/src/index.ts</files>
  <read_first>
    - crawler/src/index.ts — read the full file before editing (current start() function and start().catch pattern)
    - crawler/src/server.ts — read lines 1-55 to understand which env vars are currently used with ! non-null assertions (context for why validation matters)
  </read_first>
  <action>
    In crawler/src/index.ts, add an inline validateEnv() call at the top of the start() function, before the `await initQueue()` line. Do not extract a separate function — keep it as a local validation block in start().

    The validation block must check three env vars: RAILWAY_CRAWLER_URL, QSTASH_CURRENT_SIGNING_KEY, QSTASH_NEXT_SIGNING_KEY.

    Validation rules (per D-12):
    - RAILWAY_CRAWLER_URL: check presence (process.env.RAILWAY_CRAWLER_URL is truthy) AND valid URL format using `new URL(process.env.RAILWAY_CRAWLER_URL)` — catch the TypeError if it throws. A value like "not-a-url" must fail.
    - QSTASH_CURRENT_SIGNING_KEY: check presence only (string is truthy). URL format check is not required.
    - QSTASH_NEXT_SIGNING_KEY: check presence only (string is truthy).

    On validation failure (per D-13):
    - Log: `console.error('[feeltrace-crawler] Missing or invalid required env var: {VAR_NAME}')` where VAR_NAME is the specific failing variable.
    - Call `process.exit(1)`.

    Check each variable individually so the log message names the specific missing/malformed variable.

    Validate unconditionally — no dev/prod branching (per D-14).

    The existing `start().catch` block (which logs and calls process.exit(1) on unhandled errors) must remain unchanged.

    GROQ_API_KEY and DATABASE_URL do NOT need validation here — per Claude's discretion note in CONTEXT.md, only the three QStash-related vars need fail-fast behavior.
  </action>
  <verify>
    <automated>
      cd crawler && node -e "
        const orig = process.env.RAILWAY_CRAWLER_URL;
        delete process.env.RAILWAY_CRAWLER_URL;
        delete process.env.QSTASH_CURRENT_SIGNING_KEY;
        delete process.env.QSTASH_NEXT_SIGNING_KEY;
        const src = require('fs').readFileSync('src/index.ts','utf8');
        const hasExit = (src.match(/process\.exit\(1\)/g) || []).length >= 2;
        const hasValidation = src.includes('RAILWAY_CRAWLER_URL') && src.includes('new URL(');
        console.log(hasExit && hasValidation ? 'PASS' : 'FAIL: ' + JSON.stringify({hasExit, hasValidation}));
      "
    </automated>
  </verify>
  <acceptance_criteria>
    - crawler/src/index.ts contains `new URL(` for RAILWAY_CRAWLER_URL format validation
    - crawler/src/index.ts contains a check for QSTASH_CURRENT_SIGNING_KEY presence
    - crawler/src/index.ts contains a check for QSTASH_NEXT_SIGNING_KEY presence
    - crawler/src/index.ts contains at least two `process.exit(1)` calls (one in the new validation block, one in the existing start().catch)
    - Each validation failure logs a message containing the specific env var name and the prefix `[feeltrace-crawler]`
    - The validation block appears before `await initQueue()` in the start() function body
    - `cd crawler && npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Crawler exits immediately with descriptive error on missing/malformed RAILWAY_CRAWLER_URL, QSTASH_CURRENT_SIGNING_KEY, or QSTASH_NEXT_SIGNING_KEY; tsc passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /results/[jobId] | Public URL — anyone can navigate to a results page including for failed jobs |
| Crawler startup → env | Process env is controlled by Railway/deployment config — env validation runs at process start |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Information Disclosure | results page error card | mitigate | Error card renders job.error_message from DB (internal content) — acceptable for shareable-link model; no secrets stored in error_message |
| T-05-03 | Spoofing | results page Job query | accept | Second Prisma query only on !result path — attacker cannot inject job.status via this path; status is DB-authoritative |
| T-05-04 | Denial of Service | crawler startup exit | accept | process.exit(1) on missing env is intentional fail-fast; Railway will alert on crash loop |
| T-05-SC | Tampering | npm/pip installs | accept | No new packages installed in this plan — TypeScript and Prisma changes only |
</threat_model>

<verification>
After both tasks complete:

1. App type check: `npm run typecheck` → exit 0
2. Crawler type check: `cd crawler && npx tsc --noEmit` → exit 0
3. Results page structure: `grep -n "prisma.job.findUnique" src/app/results/\[jobId\]/page.tsx` → shows line in !result branch
4. Fallback copy: `grep "Analysis failed — try submitting again" src/app/results/\[jobId\]/page.tsx` → 1 match
5. Retry link: `grep 'href="/"' src/app/results/\[jobId\]/page.tsx` → at least 2 matches (header + retry CTA)
6. Crawler validation present: `grep "new URL(" crawler/src/index.ts` → 1 match
7. Crawler validation covers all three vars: `grep -c "RAILWAY_CRAWLER_URL\|QSTASH_CURRENT_SIGNING_KEY\|QSTASH_NEXT_SIGNING_KEY" crawler/src/index.ts` → at least 3 lines
</verification>

<success_criteria>
- Failed analysis jobs render a red error card with job.error_message (or fallback copy) and a retry Link to /
- Unknown jobIds still return 404 via notFound()
- Crawler exits at startup with code 1 and a named-variable error message when RAILWAY_CRAWLER_URL, QSTASH_CURRENT_SIGNING_KEY, or QSTASH_NEXT_SIGNING_KEY is missing or malformed
- Both packages pass tsc --noEmit with zero errors
</success_criteria>

<output>
Create `.planning/phases/05-tech-debt-and-foundation/05-02-SUMMARY.md` when done
</output>
