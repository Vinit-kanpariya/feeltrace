---
phase: 02-crawler-service
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crawler/package.json
  - crawler/tsconfig.json
  - crawler/Dockerfile
  - crawler/railway.toml
  - crawler/prisma/schema.prisma
  - crawler/src/lib/types.ts
  - crawler/src/lib/prisma.ts
autonomous: true
requirements:
  - CRAWL-02
  - CRAWL-03
  - SIG-01
  - SIG-02
  - SIG-03
  - SIG-04

must_haves:
  truths:
    - "crawler/ directory exists as a self-contained Node.js project with its own package.json"
    - "All Phase 2 npm packages are installed and audited (playwright-core, hono, @upstash/qstash, @prisma/client, prisma, p-queue, zod, typescript, @types/node)"
    - "crawler/Dockerfile uses mcr.microsoft.com/playwright/node:v1.60.0-noble as the base image"
    - "crawler/railway.toml configures /health healthcheck and prisma generate in buildCommand"
    - "crawler/prisma/schema.prisma is a copy of the root schema for Docker-time prisma generate"
    - "All signal type interfaces (DOMSignals, CSSSignals, JSSignals, NetworkSignals, CrawlPass) are exported from crawler/src/lib/types.ts"
    - "Standard PrismaClient (no adapter) is exported from crawler/src/lib/prisma.ts"
  artifacts:
    - path: "crawler/package.json"
      provides: "Project manifest with all Phase 2 deps"
    - path: "crawler/Dockerfile"
      provides: "Railway-compatible Docker image definition"
    - path: "crawler/railway.toml"
      provides: "Railway deployment config with healthcheck and build command"
    - path: "crawler/src/lib/types.ts"
      provides: "Canonical signal type contracts for all four extractors"
    - path: "crawler/src/lib/prisma.ts"
      provides: "Standard PrismaClient singleton for DB writes"
    - path: "crawler/prisma/schema.prisma"
      provides: "Schema copy for prisma generate in Docker build"
  key_links:
    - from: "crawler/src/lib/types.ts"
      to: "crawler/src/extractors/*.ts"
      via: "TypeScript import"
      pattern: "import.*types"
    - from: "crawler/Dockerfile"
      to: "crawler/railway.toml"
      via: "buildCommand alignment"
      pattern: "prisma generate"
---

<objective>
Bootstrap the `crawler/` sub-project with its complete scaffold: npm project, Docker image definition, Railway config, shared type contracts, and Prisma client. This is the foundation every subsequent plan builds on. No application logic is written here — only the skeleton that makes the rest independently buildable.

Purpose: Downstream plans (02-02 through 02-05) must be able to import from `crawler/src/lib/types.ts` and `crawler/src/lib/prisma.ts` without waiting on signal extraction code. Interface-first ordering prevents scavenger hunts.

Output: A compilable, installable `crawler/` directory with signal type interfaces, Prisma singleton, Docker configuration, and Railway deployment config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-crawler-service/02-CONTEXT.md
@.planning/phases/02-crawler-service/02-RESEARCH.md
@prisma/schema.prisma
</context>

<interfaces>
<!-- Signal type contracts — these are the canonical definitions for all downstream plans -->

From the RESEARCH.md Pattern 5 + CONTEXT.md:

The full type definitions for DOMSignals, CSSSignals, JSSignals, NetworkSignals (HAREntry), and CrawlPass are documented in `.planning/phases/02-crawler-service/02-CONTEXT.md` under "Signal Type Contracts". The executor MUST copy these verbatim into `crawler/src/lib/types.ts`.

Prisma client pattern from RESEARCH.md Pattern 6:
```typescript
// crawler/src/lib/prisma.ts
import { PrismaClient } from '../generated/prisma'
const globalForPrisma = global as unknown as { prisma: PrismaClient }
export const prisma = globalForPrisma.prisma ?? new PrismaClient()
if (process.env.NODE_ENV !== 'production') globalForPrisma.prisma = prisma
```

Note: `'../generated/prisma'` resolves to `crawler/src/generated/prisma` — created by `prisma generate` at Docker build time per the railway.toml buildCommand.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Initialize crawler/ sub-project with npm packages and TypeScript config</name>
  <files>crawler/package.json, crawler/tsconfig.json</files>
  <action>
    Create `crawler/` as a new directory containing a standalone Node.js project (NOT a workspace of the root Next.js project — it is a separate deployable service).

    Create `crawler/package.json`:
    - `"name": "feeltrace-crawler"`, `"version": "0.1.0"`, `"private": true`
    - `"type": "commonjs"` (Node.js 22 in Docker, commonjs is safer than ESM for the crawler service)
    - `"main": "dist/index.js"`
    - Scripts: `"build": "tsc"`, `"dev": "ts-node src/index.ts"`, `"start": "node dist/index.js"`
    - Dependencies (exact versions, no caret — per RESEARCH.md Open Question 2):
      - `"playwright-core": "1.60.0"`
      - `"hono": "4.12.21"` (D-20)
      - `"@upstash/qstash": "2.11.0"` (D-03)
      - `"@prisma/client": "7.8.0"` (D-22)
      - `"p-queue": "9.3.0"` (D-23)
      - `"zod": "4.4.3"`
    - devDependencies:
      - `"prisma": "7.8.0"`
      - `"typescript": "6.0.3"`
      - `"@types/node": "22.x"`

    IMPORTANT — package install: Use `sfw npm install` in the `crawler/` directory (CLAUDE.md security requirement). The Package Legitimacy Audit in RESEARCH.md confirms all packages are `[OK]` / Approved.

    Create `crawler/tsconfig.json`:
    - `"target": "ES2022"`, `"module": "commonjs"`, `"lib": ["ES2022"]`
    - `"outDir": "./dist"`, `"rootDir": "./src"`
    - `"strict": true`, `"esModuleInterop": true`, `"skipLibCheck": true`
    - `"resolveJsonModule": true`
    - `"include": ["src/**/*"]`, `"exclude": ["node_modules", "dist"]`
  </action>
  <verify>
    <automated>cd crawler && node -e "const p = require('./package.json'); const deps = Object.keys({...p.dependencies, ...p.devDependencies}); ['playwright-core','hono','@upstash/qstash','@prisma/client','p-queue','zod'].forEach(d => { if (!deps.includes(d)) throw new Error('Missing: ' + d) }); console.log('all deps present')"</automated>
  </verify>
  <done>crawler/package.json lists all 6 runtime dependencies at exact pinned versions; crawler/tsconfig.json targets ES2022 with strict mode.</done>
</task>

<task type="auto">
  <name>Task 2: Write signal type contracts (types.ts), Prisma client (prisma.ts), and copy Prisma schema</name>
  <files>crawler/src/lib/types.ts, crawler/src/lib/prisma.ts, crawler/prisma/schema.prisma</files>
  <action>
    Create `crawler/src/lib/types.ts` containing the canonical signal type interfaces. Copy VERBATIM from `.planning/phases/02-crawler-service/02-CONTEXT.md` under "Signal Type Contracts":
    - `DOMSignals` (SIG-01)
    - `CSSSignals` (SIG-02)
    - `JSSignals` (SIG-03)
    - `HAREntry` (SIG-04 entry shape)
    - `NetworkSignals` (SIG-04 aggregate)
    - `CrawlPass` (aggregate viewport result)

    All interfaces must be exported with `export interface`. This file is the single source of truth for signal shapes across all extractors and the processor.

    Create `crawler/src/lib/prisma.ts` using the standard PrismaClient singleton pattern from RESEARCH.md Pattern 6:
    - Import `PrismaClient` from `'../generated/prisma'` (relative path — generated at Docker build time)
    - Global singleton pattern to prevent multiple instances in dev hot-reload
    - No `@prisma/adapter-neon` import — standard TCP connection (D-22)

    Create `crawler/prisma/schema.prisma` as an EXACT COPY of `prisma/schema.prisma` from the project root, with TWO modifications:
    - Generator block: change `output` from `"../src/generated/prisma"` to `"../src/generated/prisma"` — this resolves to `crawler/src/generated/prisma` relative to `crawler/prisma/schema.prisma`
    - Datasource block: ADD `url = env("DATABASE_URL")` — unlike the root project (which uses prisma.config.ts), the crawler has no prisma.config.ts and must declare the URL in the schema directly so that `PrismaClient` has a connection string at runtime (D-22). The root schema omits url because Prisma 7 root project reads it via prisma.config.ts; the crawler sub-project does not use prisma.config.ts.
    - Keep all models (Job, Result, Issue, CausalEdge) and enums (JobStatus) identical to root schema

    The schema copy is required because `prisma generate` in the Docker build must run from the `crawler/` directory. The generator output path must point inside the crawler container's filesystem.
  </action>
  <verify>
    <automated>cd crawler && npx tsc --noEmit 2>&1 | grep -v "generated/prisma" | tee /tmp/tsc-out.txt; grep "error TS" /tmp/tsc-out.txt && echo "TS errors found" || echo "No TS errors"</automated>
  </verify>
  <done>crawler/src/lib/types.ts exports all 6 interfaces (DOMSignals, CSSSignals, JSSignals, HAREntry, NetworkSignals, CrawlPass). crawler/src/lib/prisma.ts exports `prisma` as standard PrismaClient. crawler/prisma/schema.prisma is a valid copy of the root schema with output pointing to crawler/src/generated/prisma.</done>
</task>

<task type="auto">
  <name>Task 3: Create Dockerfile and railway.toml</name>
  <files>crawler/Dockerfile, crawler/railway.toml</files>
  <action>
    Create `crawler/Dockerfile` using the exact template from RESEARCH.md Pattern 1:

    ```
    FROM mcr.microsoft.com/playwright/node:v1.60.0-noble

    WORKDIR /app

    COPY package*.json ./
    RUN npm ci --omit=dev

    COPY . .
    RUN npm run build

    ENV PORT=3000
    ENV NODE_ENV=production
    ENV PLAYWRIGHT_BROWSERS_PATH=/ms-playwright

    EXPOSE $PORT

    USER pwuser

    CMD ["node", "dist/index.js"]
    ```

    Key constraints (per RESEARCH.md Pattern 1 and Pitfall 1):
    - Pin to exact version tag `v1.60.0-noble` — never `:latest`
    - `USER pwuser` — non-root user pre-created by the official image; preserves Chromium sandbox
    - Do NOT add `--no-sandbox` to any launch args (Pitfall 1)
    - `PLAYWRIGHT_BROWSERS_PATH=/ms-playwright` tells playwright-core where Chromium lives in this image

    Create `crawler/railway.toml`:
    - `[build]` section: `dockerfile = "Dockerfile"` and `buildCommand = "npm ci && npx prisma generate && npm run build"` (per RESEARCH.md Pattern 6 — prisma generate must run at build time to create the Prisma client in the container)
    - `[deploy]` section: `healthcheckPath = "/health"`, `healthcheckTimeout = 60`, `restartPolicyType = "ON_FAILURE"`, `restartPolicyMaxRetries = 3`

    Also create a minimal `crawler/.dockerignore` to keep the image lean:
    - Ignore: `node_modules`, `dist`, `.git`, `*.md` (docs not needed at runtime), `**/*.test.ts`

    Create a placeholder `crawler/src/index.ts` that just logs a startup message so the TypeScript build succeeds and the container can start:
    ```typescript
    // Entry point — server initialization added in 02-02-PLAN.md
    console.log('[feeltrace-crawler] index loaded')
    export {}
    ```

    This placeholder allows the Docker build to succeed in this wave without requiring the server code (built in Wave 2).
  </action>
  <verify>
    <automated>grep -n "mcr.microsoft.com/playwright/node:v1.60.0-noble" crawler/Dockerfile && grep -n "USER pwuser" crawler/Dockerfile && grep -n "healthcheckPath" crawler/railway.toml && grep -n "prisma generate" crawler/railway.toml && echo "Dockerfile and railway.toml validated"</automated>
  </verify>
  <done>crawler/Dockerfile uses the pinned v1.60.0-noble base image and runs as pwuser. crawler/railway.toml sets healthcheckPath=/health and includes `npx prisma generate` in buildCommand. crawler/src/index.ts placeholder allows TypeScript compilation to succeed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker build → Railway runtime | Build-time secrets (DATABASE_URL) must not be baked into the image — only set as Railway env vars at runtime |
| crawler/ ↔ root Next.js project | These are separate deployable services; no shared `node_modules`, no cross-imports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | crawler/Dockerfile — base image tag | mitigate | Pin to `v1.60.0-noble` (not `:latest`); update as a deliberate dependency bump with changelog review |
| T-02-02 | Spoofing | Chromium sandbox disabled if `--no-sandbox` is added | mitigate | `USER pwuser` in Dockerfile preserves Chromium sandbox; Pitfall 1 documented in RESEARCH.md — reject any PR adding `--no-sandbox` |
| T-02-03 | Information Disclosure | DATABASE_URL baked into Docker image layers | mitigate | DATABASE_URL is never in Dockerfile; set only as Railway env var at runtime; `.dockerignore` excludes `.env*` files |
| T-02-SC | Tampering | npm install of Phase 2 packages | mitigate | `sfw npm install` (CLAUDE.md); Package Legitimacy Audit in RESEARCH.md confirms all packages [OK]; no [ASSUMED] or [SUS] packages present |
</threat_model>

<verification>
After completing all three tasks:
1. `crawler/package.json` exists and contains all 6 runtime deps at pinned versions
2. `crawler/Dockerfile` contains `mcr.microsoft.com/playwright/node:v1.60.0-noble` and `USER pwuser`
3. `crawler/railway.toml` contains `healthcheckPath = "/health"` and `prisma generate`
4. `crawler/src/lib/types.ts` exports DOMSignals, CSSSignals, JSSignals, HAREntry, NetworkSignals, CrawlPass
5. `crawler/src/lib/prisma.ts` exports standard PrismaClient (no adapter import)
6. `crawler/prisma/schema.prisma` matches root schema with output pointing to crawler/src/generated/prisma
</verification>

<success_criteria>
- All 6 signal interfaces are exported from `crawler/src/lib/types.ts` matching the contracts in 02-CONTEXT.md
- `crawler/Dockerfile` uses the exact pinned Playwright base image and runs as non-root `pwuser`
- `crawler/railway.toml` includes `npx prisma generate` in buildCommand and sets `/health` as healthcheckPath
- `crawler/src/lib/prisma.ts` uses standard `new PrismaClient()` with no `@prisma/adapter-neon`
- TypeScript compilation (`npx tsc --noEmit`) succeeds within the `crawler/` directory (ignoring generated Prisma client which doesn't exist until Docker build)
</success_criteria>

<output>
Create `.planning/phases/02-crawler-service/02-01-SUMMARY.md` when done
</output>
