---
phase: 02-crawler-service
plan: 06
type: execute
wave: 5
depends_on:
  - 02-05
files_modified: []
autonomous: false
requirements:
  - CRAWL-02
  - CRAWL-03
  - SIG-01
  - SIG-02
  - SIG-03
  - SIG-04

user_setup:
  - service: railway
    why: "Deploy the crawler Docker container"
    env_vars:
      - name: QSTASH_CURRENT_SIGNING_KEY
        source: "Upstash Console → QStash → Signing Keys"
      - name: QSTASH_NEXT_SIGNING_KEY
        source: "Upstash Console → QStash → Signing Keys"
      - name: RAILWAY_PUBLIC_URL
        source: "Railway Console → your crawler service → Settings → Domains (the generated URL)"
      - name: DATABASE_URL
        source: "Neon Console → your project → Connection Details → Pooled Connection (port 6543)"
    dashboard_config:
      - task: "Create a new Railway service from the crawler/ directory"
        location: "Railway Console → New Project → Deploy from GitHub Repo OR railway up from crawler/ directory"
      - task: "Set minimum 1 GB RAM for the service"
        location: "Railway Console → crawler service → Settings → Resources → Memory"
      - task: "Update .env.local RAILWAY_CRAWLER_URL to the new Railway service URL + /crawl"
        location: "Root project .env.local file (set to https://<railway-domain>/crawl)"
      - task: "Update QStash publish URL in Upstash Console to match the Railway /crawl URL"
        location: "Upstash Console → QStash → No manual config needed — URL is set dynamically via RAILWAY_CRAWLER_URL env var in the Next.js app"

must_haves:
  truths:
    - "Railway service is live and Railway healthcheck passes at /health"
    - "Submitting a Next.js SPA URL (e.g. vercel.com) transitions job through all status states and reaches 'complete'"
    - "Job.status in Neon DB shows 'complete' after crawl finishes"
    - "Crawler server logs show both mobile and desktop passes completing"
    - "Submitting a React SPA URL produces signal data reflecting the hydrated DOM (not pre-JS HTML)"
    - "Crawler completes within 60-second SLA"
  artifacts:
    - path: "crawler/"
      provides: "Deployed Railway service accessible at public URL"
    - path: ".env.local"
      provides: "Updated RAILWAY_CRAWLER_URL pointing to deployed service"
  key_links:
    - from: "Next.js POST /api/analyze"
      to: "Railway /crawl endpoint"
      via: "QStash publish → HTTP POST"
      pattern: "RAILWAY_CRAWLER_URL"
    - from: "Railway crawler"
      to: "Neon PostgreSQL"
      via: "standard PrismaClient TCP"
      pattern: "prisma.job.update"
---

<objective>
Deploy the crawler service to Railway and verify end-to-end operation. This checkpoint confirms all five Phase 2 success criteria through real-world execution: SPA hydration, dual viewports, all four signal types, DB status transitions, and 60-second SLA compliance.

Purpose: Local TypeScript compilation proves correctness within the crawler code, but the integration checkpoint confirms Docker builds succeed, Railway environment variables are set, Playwright/Chromium runs as expected in the container, Neon accepts TCP connections from Railway's network, and QStash delivery reaches the endpoint.

Output: A live Railway service + human confirmation that a Next.js SPA URL produces signal data in the crawler logs and 'complete' status in the DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-crawler-service/02-CONTEXT.md
@.planning/phases/02-crawler-service/02-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated pre-deployment validation</name>
  <files>vitest.config.mts, crawler/Dockerfile, crawler/src/browser.ts, crawler/src/server.ts, crawler/src/processor.ts</files>
  <action>
    Run the full automated validation suite before attempting deployment:

    1. TypeScript compilation — crawler sub-project:
       ```
       cd crawler && npx tsc --noEmit
       ```
       Fix any type errors before proceeding. Common sources: missing imports after wiring in 02-05, generated Prisma client not available (acceptable error — it's generated at Docker build time; exclude with `grep -v generated/prisma`).

    2. Unit test suite — all crawler tests:
       ```
       npm test -- --run crawler/src/extractors/dom.test.ts
       npm test -- --run crawler/src/extractors/js.test.ts
       npm test -- --run crawler/src/extractors/network.test.ts
       ```
       All three must pass. Fix any failures before proceeding to deployment.

    3. Root project still compiles (crawler changes should not affect root):
       ```
       npm run typecheck
       npm run lint
       ```

    4. Verify key safety constraints are in place:
       - `grep -n "no-sandbox" crawler/Dockerfile crawler/src/browser.ts` — must return NO matches (Pitfall 1)
       - `grep -n "networkidle" crawler/src/browser.ts` — must return NO matches (Pitfall 2)
       - `grep -n "c\.req\.text()" crawler/src/server.ts` — must return at least one match (raw body for QStash)
       - `grep -n "USER pwuser" crawler/Dockerfile` — must return one match

    Report all results. If any automated check fails, fix it before calling the checkpoint.
  </action>
  <verify>
    <automated>cd crawler && npx tsc --noEmit 2>&1 | grep -v "generated/prisma" | grep "error TS"; npm test -- --run crawler/src/extractors/; echo "Pre-deployment checks done"</automated>
  </verify>
  <done>TypeScript compiles clean. All three extractor unit test files pass. Root typecheck and lint pass. Safety grep checks show no --no-sandbox, no networkidle, raw body read present, USER pwuser present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The complete crawler service is built and validated locally. Claude has:
    - Scaffolded the crawler/ sub-project with all dependencies
    - Implemented the Hono server with QStash signature verification
    - Implemented all four signal extractors (DOM, CSS, JS, Network/HAR)
    - Implemented the job processor with status transitions, SLA timeout, and idempotency
    - Passed all automated pre-deployment checks
  </what-built>
  <how-to-verify>
    Deploy to Railway and verify end-to-end:

    **Step 1 — Deploy to Railway:**
    ```bash
    # Option A: From the crawler/ directory (Railway CLI)
    cd crawler
    railway up

    # Option B: Push to GitHub and connect in Railway console
    # Railway detects the Dockerfile automatically
    ```

    **Step 2 — Set Railway environment variables** (all required before first request):
    - `QSTASH_CURRENT_SIGNING_KEY` → from Upstash Console → QStash → Signing Keys
    - `QSTASH_NEXT_SIGNING_KEY` → from Upstash Console → QStash → Signing Keys
    - `DATABASE_URL` → Neon Console → your project → Pooled connection string (port 6543, includes `?sslmode=require`)
    - `RAILWAY_PUBLIC_URL` → Railway Console → crawler service → Settings → Domains (e.g. `https://feeltrace-crawler.up.railway.app`)
    - `NODE_ENV` → `production`

    **Step 3 — Verify Railway healthcheck passes:**
    ```bash
    curl https://<your-railway-domain>/health
    # Expected: {"status":"ok"}
    ```

    **Step 4 — Update .env.local in the root project:**
    ```
    RAILWAY_CRAWLER_URL=https://<your-railway-domain>/crawl
    ```
    Restart the Next.js dev server after updating.

    **Step 5 — Submit a test URL via the FeelTrace dashboard:**
    Visit http://localhost:3000, submit `https://nextjs.org` (a Next.js SPA)

    **Step 6 — Watch Railway logs:**
    In Railway console → your service → Logs, you should see:
    ```
    [processor] Job <id> crawling...
    [hydration] ... (or: waitForFunction resolved)
    [processor] Job <id> signals collected: mobile=mobile, desktop=desktop
    [processor] Job <id> completed in NNNNms
    ```
    The completion time must be under 60 seconds.

    **Step 7 — Verify DB status in Prisma Studio:**
    ```bash
    npm run db:studio
    ```
    Open Job table → find the submitted job → status should be `complete`.

    **Step 8 — Verify SPA hydration (CRAWL-02):**
    Submit `https://react.dev` (React SPA). In Railway logs, check that the processor reaches `complete` and the hydration wait does not time out. If it falls back with `[hydration] waitForFunction timed out`, that is acceptable (non-fatal) — the crawl still completes.

    **Step 9 — Verify dual viewport (CRAWL-03):**
    Railway logs must show both `mobile=mobile` and `desktop=desktop` in the signals collected message for each job.

    **Step 10 — Verify 60s SLA:**
    The completion time in Railway logs must be under 60,000ms for both test URLs.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 10 steps pass. Or describe any issues encountered (Railway deployment failure, DB connection error, SLA exceeded, hydration always timing out, etc.) — Claude will diagnose and fix.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public internet → Railway /crawl | Railway service is publicly accessible; only QStash-signed requests should be processed |
| Railway → Neon DB | TCP connection from Railway egress IPs to Neon pooler; requires DATABASE_URL with sslmode=require |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-19 | Information Disclosure | RAILWAY_PUBLIC_URL mismatch causes all QStash deliveries to fail with 401 | mitigate | Receiver.verify() URL must exactly match the endpoint QStash posts to; verify in Railway logs that 401s are not occurring after deployment |
| T-02-20 | Denial of Service | Railway service OOM during Playwright execution | mitigate | 1 GB minimum RAM configured in Railway Resources; p-queue concurrency:1 prevents simultaneous Playwright instances; --disable-dev-shm-usage is set |
| T-02-21 | Information Disclosure | DATABASE_URL visible in Railway logs | mitigate | Railway env vars are not logged; DATABASE_URL is only accessed via PrismaClient constructor, never logged or returned in responses |
| T-02-SC | Tampering | No new packages in this plan | accept | This is a deployment/verification plan — no new installs |
</threat_model>

<verification>
Phase 2 success criteria verified at this checkpoint:

1. **CRAWL-02 (SPA hydration):** Next.js SPA URL (`https://nextjs.org`) completes successfully — logs show hydration detection ran (or fell back gracefully) and job reaches `complete`
2. **CRAWL-03 (dual viewport):** Railway logs show `mobile=mobile` and `desktop=desktop` signal passes for each job
3. **SIG-01 through SIG-04:** Crawler logs show `signals collected` line — all four signal types populated in the CrawlPass objects
4. **DB populated:** `npm run db:studio` shows job.status = `complete` for the test job
5. **SLA:** Completion time in Railway logs < 60,000ms
</verification>

<success_criteria>
Phase 2 is complete when ALL of the following are true:
1. Railway /health returns 200 {"status":"ok"} (Railway healthcheck passes)
2. Submitting a Next.js SPA URL via the dashboard creates a job that transitions to 'complete' in Neon DB
3. Railway logs confirm dual viewport passes (mobile + desktop) for each job
4. Railway logs confirm all four signal types were collected in-memory
5. Completion time in Railway logs is under 60 seconds
6. All unit tests pass: `npm test -- --run crawler/src/extractors/`
</success_criteria>

<output>
Create `.planning/phases/02-crawler-service/02-06-SUMMARY.md` when done
</output>
