---
phase: 07-ai-pipeline-depth
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - crawler/src/pipeline/page-type-detector.ts
  - crawler/src/pipeline/page-type-detector.test.ts
  - crawler/src/pipeline/benchmark-context.ts
  - crawler/src/pipeline/benchmark-context.test.ts
  - crawler/src/pipeline/stage3-narrator.ts
  - crawler/src/pipeline/run-pipeline.ts
autonomous: true
requirements: [AI-03, AI-04]

must_haves:
  truths:
    - "detectPageType returns 'e-commerce' when techProfile.payments is a non-null string"
    - "detectPageType returns 'saas-dashboard' for apps with many interactive elements, no analytics, Next.js/React"
    - "detectPageType returns 'blog' for pages with high articleCount or high h2Count + low interactive"
    - "detectPageType returns 'landing-page' for pages with analytics present, CTAs, few forms"
    - "detectPageType returns 'unknown' as the fallback when no rule matches"
    - "buildBenchmarkContext returns empty string when cwv is null"
    - "buildBenchmarkContext includes 'GOOD', 'NEEDS IMPROVEMENT', or 'POOR' labels and a ratio multiplier for LCP when CWV data is available"
    - "Stage 3 narrative uses page-type framing when pageType is not 'unknown'"
    - "Stage 3 narrative includes benchmark comparisons when CWV data is available"
  artifacts:
    - path: "crawler/src/pipeline/page-type-detector.ts"
      provides: "PageType type export, detectPageType function"
      exports: ["PageType", "detectPageType"]
    - path: "crawler/src/pipeline/page-type-detector.test.ts"
      provides: "unit tests for all 5 PageType values"
    - path: "crawler/src/pipeline/benchmark-context.ts"
      provides: "buildBenchmarkContext function, CWV_THRESHOLDS constants"
      exports: ["buildBenchmarkContext"]
    - path: "crawler/src/pipeline/benchmark-context.test.ts"
      provides: "unit tests for null CWV, GOOD/POOR labels, ratio multiplier, origin_fallback note"
    - path: "crawler/src/pipeline/stage3-narrator.ts"
      provides: "runStage3Narration with pageType and benchmarkContext params"
      contains: "pageType: PageType"
    - path: "crawler/src/pipeline/run-pipeline.ts"
      provides: "page-type detection + benchmark context + Stage 3 call with new params wired in"
  key_links:
    - from: "crawler/src/pipeline/run-pipeline.ts"
      to: "crawler/src/pipeline/page-type-detector.ts"
      via: "detectPageType(techProfile, signals.desktop.domSignals)"
      pattern: "detectPageType"
    - from: "crawler/src/pipeline/run-pipeline.ts"
      to: "crawler/src/pipeline/benchmark-context.ts"
      via: "buildBenchmarkContext(externalSignals?.cwv ?? null, pageType)"
      pattern: "buildBenchmarkContext"
    - from: "crawler/src/pipeline/run-pipeline.ts"
      to: "crawler/src/pipeline/stage3-narrator.ts"
      via: "runStage3Narration(client, enrichedIssues, edges, pageType, benchmarkContext)"
      pattern: "runStage3Narration.*pageType.*benchmarkContext"
---

<objective>
Create the page-type detector and benchmark context builder, extend Stage 3 to accept them, and wire everything into run-pipeline.ts. This delivers context-aware narrative framing (AI-03) and CWV benchmark comparisons (AI-04).

Purpose: The narrative now opens with framing relevant to the page's business context (e-commerce = conversion focus, SaaS = INP focus, etc.) and includes threshold-relative CWV comparisons ("LCP is 1.5× the good threshold") when field data is available.
Output: page-type-detector.ts + test (new), benchmark-context.ts + test (new), stage3-narrator.ts (modified signature + prompt), run-pipeline.ts (modified — wiring after Stage 2, before Stage 3 call).
</objective>

<execution_context>
@C:/Users/STL-LT-068/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/STL-LT-068/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-ai-pipeline-depth/07-RESEARCH.md
@.planning/phases/07-ai-pipeline-depth/07-PATTERNS.md
@.planning/phases/07-ai-pipeline-depth/07-01-SUMMARY.md

<interfaces>
<!-- Extracted from crawler/src/lib/types.ts — exact field names for TechProfile + DOMSignals -->

From crawler/src/lib/types.ts (TechProfile — fields used in detectPageType):
```typescript
export interface TechProfile {
  framework: string | null        // "Next.js", "React", "Vue", etc.
  analytics: string[]             // ["Google Analytics", "Hotjar", ...]
  payments?: string | null        // "Stripe", "PayPal", "Braintree", etc.
  // ... other fields omitted
}
```

From crawler/src/lib/types.ts (DOMSignals — fields used in detectPageType):
```typescript
export interface DOMSignals {
  interactiveElementCount: number
  semanticScore: {
    h2Count: number
    articleCount: number
    // ... other fields
  }
  formCount: number
  ctaVisibility: {
    buttonCount: number
    // ... other fields
  }
}
```

From crawler/src/lib/types.ts (CWVMetrics — fields used in buildBenchmarkContext):
```typescript
export interface CWVMetrics {
  lcp_ms: number | null
  cls_raw: number | null
  inp_ms: number | null
  origin_fallback: boolean
}
```

From crawler/src/pipeline/stage3-narrator.ts (current signature — to be extended):
```typescript
export async function runStage3Narration(
  client: Groq,
  enrichedIssues: EnrichedIssue[],
  edges: CausalEdgeCandidate[],
): Promise<NarrativeResult>
```

From crawler/src/pipeline/run-pipeline.ts (Stage 3 call — line 94, to be replaced):
```typescript
const narrative = await runStage3Narration(client, enrichedIssues, edges)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create page-type-detector.ts and benchmark-context.ts with tests</name>
  <files>
    crawler/src/pipeline/page-type-detector.ts,
    crawler/src/pipeline/page-type-detector.test.ts,
    crawler/src/pipeline/benchmark-context.ts,
    crawler/src/pipeline/benchmark-context.test.ts
  </files>
  <read_first>
    - crawler/src/pipeline/stage1-external-scorer.ts — structural analog: comment header, imports (types only), static constants, pure exported functions, no side effects, no console output
    - crawler/src/pipeline/stage1-external-scorer.test.ts — test file pattern: factory helpers with Partial overrides, describe/it/expect, pure unit tests (no mocks needed)
    - crawler/src/lib/types.ts — exact field names for TechProfile (framework, analytics, payments), DOMSignals (interactiveElementCount, semanticScore.articleCount, semanticScore.h2Count, formCount, ctaVisibility.buttonCount), CWVMetrics (lcp_ms, cls_raw, inp_ms, origin_fallback)
  </read_first>
  <behavior>
    page-type-detector.test.ts behaviors:
    - Test 1: detectPageType returns 'e-commerce' when techProfile.payments is 'Stripe'
    - Test 2: detectPageType returns 'saas-dashboard' when interactiveElementCount > 20, analytics is [], framework is 'Next.js'
    - Test 3: detectPageType returns 'blog' when semanticScore.articleCount is 3 (>2)
    - Test 4: detectPageType returns 'blog' when semanticScore.h2Count is 6 (>5) and interactiveElementCount is 3 (<5)
    - Test 5: detectPageType returns 'landing-page' when analytics has 1 item, ctaVisibility.buttonCount > 0, formCount < 2
    - Test 6: detectPageType returns 'unknown' when no rule matches (payments null, analytics [], interactiveElementCount 5, articleCount 0, h2Count 2, buttonCount 0)

    benchmark-context.test.ts behaviors:
    - Test 1: buildBenchmarkContext returns '' when cwv is null (regardless of pageType)
    - Test 2: buildBenchmarkContext includes 'GOOD' when lcp_ms is 2000 (≤ 2500 threshold)
    - Test 3: buildBenchmarkContext includes 'POOR' and a ratio string like '2.0×' when lcp_ms is 5000 (> 4000 threshold)
    - Test 4: buildBenchmarkContext includes 'NEEDS IMPROVEMENT' when lcp_ms is 3000 (> 2500, ≤ 4000)
    - Test 5: buildBenchmarkContext includes the origin_fallback note when origin_fallback is true
    - Test 6: buildBenchmarkContext includes CLS status line when cls_raw is non-null
  </behavior>
  <action>
    FILE 1: crawler/src/pipeline/page-type-detector.ts
    - Comment header: `// page-type-detector.ts — deterministic page-type classifier using TechProfile + DOMSignals.`
    - Imports: `import type { TechProfile } from '../lib/types'` and `import type { DOMSignals } from '../lib/types'`
    - Export: `export type PageType = 'e-commerce' | 'saas-dashboard' | 'landing-page' | 'blog' | 'unknown'`
    - Export: `export function detectPageType(techProfile: TechProfile, domSignals: DOMSignals): PageType`
    - Implement as early-return if/else chain (no arrays or loops):
      1. If `techProfile.payments` is non-null and truthy → return 'e-commerce'
      2. If `domSignals.interactiveElementCount > 20 && techProfile.analytics.length === 0 && (techProfile.framework === 'Next.js' || techProfile.framework === 'React')` → return 'saas-dashboard'
      3. If `domSignals.semanticScore.articleCount > 2 || (domSignals.semanticScore.h2Count > 5 && domSignals.interactiveElementCount < 5)` → return 'blog'
      4. If `domSignals.formCount < 2 && domSignals.ctaVisibility.buttonCount > 0 && techProfile.analytics.length > 0` → return 'landing-page'
      5. return 'unknown'
    - No console output, no async, no side effects.

    FILE 2: crawler/src/pipeline/page-type-detector.test.ts
    - Header: `// @vitest-environment node`, `// crawler/src/pipeline/page-type-detector.test.ts`
    - Imports: `import { describe, it, expect } from 'vitest'`, imports for detectPageType and PageType
    - Factory helpers: `makeTechProfile(overrides: Partial<TechProfile> = {}): TechProfile` with safe defaults (framework:null, payments:null, analytics:[], all numeric fields 0, etc.) and `makeDOMSignals(overrides: DeepPartial<DOMSignals> = {}): DOMSignals` with safe nested defaults. For DOMSignals, define a helper `makeDOM` that allows overriding semanticScore.articleCount, semanticScore.h2Count, interactiveElementCount, formCount, ctaVisibility.buttonCount independently.
    - Implement all 6 tests above.

    FILE 3: crawler/src/pipeline/benchmark-context.ts
    - Comment header: `// benchmark-context.ts — builds a CWV benchmark comparison paragraph for Stage 3 narrative.`
    - Imports: `import type { CWVMetrics } from '../lib/types'`, `import type { PageType } from './page-type-detector'`
    - CWV_THRESHOLDS const (module-level, as const): `{ lcp: { good: 2500, poor: 4000 }, cls: { good: 10, poor: 25 }, inp: { good: 200, poor: 500 } }`
      Note: cls values are raw integers (PSI percentile × 100), matching the CWVMetrics.cls_raw field; 0.1 × 100 = 10 (good), 0.25 × 100 = 25 (poor).
    - PAGE_TYPE_CONTEXT const: Record<string, string> with entries for 'e-commerce', 'saas-dashboard', 'landing-page', 'blog', 'unknown'. Use the exact framing strings from RESEARCH.md Pattern 4.
    - Export: `export function buildBenchmarkContext(cwv: CWVMetrics | null, pageType: PageType): string`
    - Implementation: if !cwv return ''. Build lines array. Push page context intro. Push empty string for blank line. Push 'Real-user performance benchmarks (from CrUX field data):'. For each metric (lcp_ms, cls_raw, inp_ms): if non-null, compute status (GOOD/NEEDS IMPROVEMENT/POOR using thresholds), compute ratio multiplier (metric/threshold).toFixed(1), push formatted line. If cwv.origin_fallback is true, push the origin fallback note. Return lines.join('\n').

    FILE 4: crawler/src/pipeline/benchmark-context.test.ts
    - Header: `// @vitest-environment node`, `// crawler/src/pipeline/benchmark-context.test.ts`
    - Imports: `import { describe, it, expect } from 'vitest'`, imports for buildBenchmarkContext
    - Factory helper: `makeCWV(overrides: Partial<CWVMetrics> = {}): CWVMetrics` with defaults `{ lcp_ms: null, cls_raw: null, inp_ms: null, origin_fallback: false }`.
    - Implement all 6 tests above.
  </action>
  <verify>
    <automated>npm test -- crawler/src/pipeline/page-type-detector.test.ts && npm test -- crawler/src/pipeline/benchmark-context.test.ts</automated>
  </verify>
  <done>
    - page-type-detector.ts exports PageType and detectPageType
    - benchmark-context.ts exports buildBenchmarkContext
    - All 6 page-type-detector tests pass
    - All 6 benchmark-context tests pass
    - Both files are pure (no async, no side effects, no console output)
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend Stage 3 signature and wire run-pipeline.ts</name>
  <files>crawler/src/pipeline/stage3-narrator.ts, crawler/src/pipeline/run-pipeline.ts</files>
  <read_first>
    - crawler/src/pipeline/stage3-narrator.ts — read entire file: NARRATOR_SYSTEM_PROMPT const (lines 5-26), parseNarrativeOutput function (lines 28-65), runStage3Narration function signature (lines 67-71), user prompt construction (line 74)
    - crawler/src/pipeline/run-pipeline.ts — read current state after Plan 07-01 modifications: Stage 1.5 block should already be present; find Stage 2 call (runStage2Reasoning), Stage 3 call (runStage3Narration), and confirm client is already declared before Stage 2
  </read_first>
  <action>
    FILE 1: crawler/src/pipeline/stage3-narrator.ts — two targeted changes:

    a) ADD IMPORT at top of file (after existing imports):
       `import type { PageType } from './page-type-detector'`

    b) EXTEND runStage3Narration SIGNATURE — add two new params after `edges`:
       `pageType: PageType,        // AI-03 — detected page type for narrative framing`
       `benchmarkContext: string,  // AI-04 — CWV benchmark paragraph ('' when no CWV data)`

    c) CHANGE SYSTEM PROMPT TO DYNAMIC — the existing NARRATOR_SYSTEM_PROMPT is a static const. Because pageType and benchmarkContext are dynamic per-call values, build the enriched prompt INSIDE runStage3Narration (not at module scope). Change the messages array to use a dynamically-constructed system prompt string:

       Build the prompt as:
       ```
       const systemPrompt = NARRATOR_SYSTEM_PROMPT + '\n\n' +
         '[PAGE TYPE AND CONTEXT]\n' +
         (pageType !== 'unknown'
           ? `This is a ${pageType}. Tailor your narrative framing, examples, and recommendations to this specific page type.`
           : '') +
         '\n\n[BENCHMARK COMPARISONS]\n' +
         (benchmarkContext || '(No real-user CWV field data available for this URL — do not include benchmark comparisons.)') +
         '\n\nWhen CWV benchmark context is provided:\n' +
         '- Open the [TECHNICAL PERFORMANCE] section with the benchmark comparison (e.g. "Real user data shows LCP of 3.8s — 1.5× the 2.5s good threshold")\n' +
         '- Connect metric values to business impact relevant to the page type'
       ```

    Then use `systemPrompt` (not `NARRATOR_SYSTEM_PROMPT`) in the messages array: `{ role: 'system', content: systemPrompt }`.

    The `userPrompt` line (line 74) and all other code in runStage3Narration are unchanged.

    FILE 2: crawler/src/pipeline/run-pipeline.ts — three targeted changes:

    a) ADD IMPORTS to the existing import block:
       `import { detectPageType } from './page-type-detector'`
       `import { buildBenchmarkContext } from './benchmark-context'`

    b) INSERT page-type detection + benchmark context block BETWEEN Stage 2 completion and Stage 3 call. Find the line `const narrative = await runStage3Narration(client, enrichedIssues, edges)` and insert BEFORE it:
       `// Page-type detection and benchmark context (deterministic — no LLM call)`
       `const pageType = detectPageType(techProfile, signals.desktop.domSignals)`
       `const benchmarkContext = buildBenchmarkContext(externalSignals?.cwv ?? null, pageType)`
       `console.log('[pipeline] Job ' + jobId + ': pageType=' + pageType)`

    c) UPDATE Stage 3 call to pass new params:
       Change `await runStage3Narration(client, enrichedIssues, edges)` to:
       `await runStage3Narration(client, enrichedIssues, edges, pageType, benchmarkContext)`

    Also update the issues.create map in the Prisma transaction to include the two new DB fields from Plan 07-02. Find the `enrichedIssues.map((issue) => ({...})` block (currently writes category, signal_source, severity, raw_evidence, technical_description) and ADD:
       `fix_suggestion: issue.fix_suggestion,`
       `severity_justification: issue.severity_justification,`
  </action>
  <verify>
    <automated>npm run typecheck && npm test</automated>
  </verify>
  <done>
    - stage3-narrator.ts runStage3Narration accepts pageType: PageType and benchmarkContext: string params
    - stage3-narrator.ts builds systemPrompt dynamically inside the function (not at module scope)
    - run-pipeline.ts imports detectPageType and buildBenchmarkContext
    - run-pipeline.ts calls detectPageType(techProfile, signals.desktop.domSignals) after Stage 2
    - run-pipeline.ts passes pageType and benchmarkContext to runStage3Narration
    - run-pipeline.ts issues.create map includes fix_suggestion and severity_justification
    - npm run typecheck exits 0
    - npm test passes (full suite, all 112+ tests)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TechProfile.payments → detectPageType | payments field comes from browser fingerprint; non-null means payment SDK detected |
| Benchmark context string → Stage 3 LLM | Static template string injected into LLM system prompt — no user-controlled data; safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-07 | Information Disclosure | PAGE_TYPE_CONTEXT benchmark strings | accept | Benchmark strings are static compile-time constants sourced from Google public docs (web.dev/articles/vitals) and Web Almanac; no user PII involved |
| T-7-08 | Tampering | benchmarkContext injected into Stage 3 system prompt | accept | benchmarkContext is built entirely from CWVMetrics (server-side PSI data) + static strings; contains no user-controlled input; not a prompt injection vector |
| T-7-09 | DoS | detectPageType heuristic always returning 'unknown' | accept | 'unknown' falls back to current narrative behavior gracefully; no user-visible error; detection is best-effort enhancement |
| T-7-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; all dependencies already installed |
</threat_model>

<verification>
Run after both tasks complete:

1. `npm test -- crawler/src/pipeline/page-type-detector.test.ts` — all 6 tests pass
2. `npm test -- crawler/src/pipeline/benchmark-context.test.ts` — all 6 tests pass
3. `npm run typecheck` — exits 0, no errors in stage3-narrator.ts or run-pipeline.ts
4. `npm test` — full suite passes
5. Grep: `grep -n "pageType" crawler/src/pipeline/run-pipeline.ts` — appears at detectPageType call and runStage3Narration call
6. Grep: `grep -n "fix_suggestion" crawler/src/pipeline/run-pipeline.ts` — appears in issues.create map
</verification>

<success_criteria>
- page-type-detector.ts and benchmark-context.ts exist as pure utility modules with no side effects
- All 12 new unit tests (6 + 6) pass
- runStage3Narration signature has 5 params: client, enrichedIssues, edges, pageType, benchmarkContext
- Stage 3 system prompt is built dynamically with page-type and benchmark context injected
- run-pipeline.ts DB write includes fix_suggestion and severity_justification per issue
- Full test suite green, typecheck clean
</success_criteria>

<output>
Create `.planning/phases/07-ai-pipeline-depth/07-03-SUMMARY.md` when done
</output>
