---
phase: 07-ai-pipeline-depth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crawler/src/pipeline/types.ts
  - crawler/src/pipeline/stage2-reasoner.ts
  - crawler/src/pipeline/stage2-reasoner.test.ts
  - prisma/schema.prisma
autonomous: true
requirements: [AI-01, AI-02]

must_haves:
  truths:
    - "Every EnrichedIssue has a fix_suggestion field containing a specific imperative action (not advisory language)"
    - "Every EnrichedIssue has a severity_justification field estimating user impact in business terms"
    - "Stage 2 Zod schema validates both new fields as required, max 300 chars each"
    - "fix_suggestion values starting with 'Consider', 'You might', 'You could', 'Try to', or 'It is recommended' are rejected by a Zod .refine() check"
    - "Prisma Issue model has fix_suggestion and severity_justification as non-nullable String columns with @default('')"
    - "Stage 2 max_tokens increased to 4096 to accommodate two extra fields per issue"
  artifacts:
    - path: "crawler/src/pipeline/types.ts"
      provides: "EnrichedIssue with fix_suggestion and severity_justification string fields"
      contains: "fix_suggestion: string"
    - path: "crawler/src/pipeline/stage2-reasoner.ts"
      provides: "Extended EMIT_ANALYSIS_TOOL, extended Stage2OutputSchema with fix_suggestion+severity_justification, max_tokens 4096"
    - path: "crawler/src/pipeline/stage2-reasoner.test.ts"
      provides: "Tests for both new fields: required presence, max 300 chars, refine rejection"
    - path: "prisma/schema.prisma"
      provides: "Issue model with fix_suggestion String @default('') and severity_justification String @default('')"
      contains: "fix_suggestion"
  key_links:
    - from: "crawler/src/pipeline/stage2-reasoner.ts"
      to: "crawler/src/pipeline/types.ts"
      via: "import EnrichedIssue — must include fix_suggestion, severity_justification"
      pattern: "fix_suggestion"
    - from: "crawler/src/pipeline/run-pipeline.ts"
      to: "prisma Issue.create"
      via: "enrichedIssues.map — must include fix_suggestion, severity_justification in DB write"
      pattern: "fix_suggestion.*issue\\.fix_suggestion"
---

<objective>
Extend Stage 2 to produce per-issue fix suggestions (AI-01) and severity justifications (AI-02). This adds two fields to the EMIT_ANALYSIS_TOOL definition, Stage2OutputSchema Zod validation, EnrichedIssue interface, and the Prisma Issue model. Includes the [BLOCKING] database migration required before verification.

Purpose: Makes each issue directly actionable — the fix_suggestion tells developers exactly what to do; the severity_justification tells product managers why it matters.
Output: types.ts (modified), stage2-reasoner.ts (modified), stage2-reasoner.test.ts (modified), schema.prisma (modified + migration run).
</objective>

<execution_context>
@C:/Users/STL-LT-068/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/STL-LT-068/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-ai-pipeline-depth/07-RESEARCH.md
@.planning/phases/07-ai-pipeline-depth/07-PATTERNS.md
@.planning/phases/06-signal-expansion/06-03-SUMMARY.md

<interfaces>
<!-- Extracted from current source files — executor reads these before touching anything -->

From crawler/src/pipeline/types.ts (current EnrichedIssue — to be extended):
```typescript
export interface EnrichedIssue extends ScoredIssue {
  technical_description: string  // 1-3 sentence plain-English explanation from Stage 2 LLM
}
```

From crawler/src/pipeline/stage2-reasoner.ts (current enriched_issues tool properties — add after technical_description):
```typescript
// Inside EMIT_ANALYSIS_TOOL.function.parameters.properties.enriched_issues.items.properties:
index: { type: 'number', description: '...' },
technical_description: { type: 'string', description: '...' },
// ADD HERE: fix_suggestion and severity_justification

// Current required array (line 64):
required: ['index', 'technical_description'],
// CHANGE TO:
required: ['index', 'technical_description', 'fix_suggestion', 'severity_justification'],
```

From crawler/src/pipeline/stage2-reasoner.ts (current Stage2OutputSchema — add after technical_description):
```typescript
export const Stage2OutputSchema = z.object({
  enriched_issues: z.array(z.object({
    index: z.number().int().min(0),
    technical_description: z.string().min(1).max(500),
    // ADD HERE: fix_suggestion and severity_justification
  })),
  causal_edges: z.array(z.object({ ... })).max(5),
})
```

From crawler/src/pipeline/stage2-reasoner.ts (current parseStage2Output merge — add after technical_description):
```typescript
const enrichedIssues: EnrichedIssue[] = validEnriched.map((item) => ({
  ...scoredIssues[item.index],
  technical_description: item.technical_description,
  // ADD HERE: fix_suggestion: item.fix_suggestion, severity_justification: item.severity_justification
}))
```

From crawler/src/pipeline/stage2-reasoner.ts (current max_tokens — line 175):
```typescript
max_tokens: 2048,  // CHANGE TO: 4096
```

From prisma/schema.prisma (current Issue model — add two fields before causedBy relation):
```prisma
model Issue {
  // ...existing fields...
  technical_description String
  // ADD HERE: fix_suggestion and severity_justification with @default("")
  causedBy CausalEdge[] @relation("ToIssue")
  causes   CausalEdge[] @relation("FromIssue")
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend EnrichedIssue, Stage2OutputSchema, and stage2-reasoner.ts</name>
  <files>crawler/src/pipeline/types.ts, crawler/src/pipeline/stage2-reasoner.ts, crawler/src/pipeline/stage2-reasoner.test.ts</files>
  <read_first>
    - crawler/src/pipeline/types.ts — read current EnrichedIssue interface (line 14-16); add fix_suggestion and severity_justification fields
    - crawler/src/pipeline/stage2-reasoner.ts — read entire file; identify EMIT_ANALYSIS_TOOL properties (lines 59-65), Stage2OutputSchema (lines 91-107), parseStage2Output merge (lines 131-134), runStage2Reasoning max_tokens (line 175), SYSTEM_PROMPT (lines 29-41)
    - crawler/src/pipeline/stage2-reasoner.test.ts — read entire file; understand existing test structure to add new tests after existing ones
  </read_first>
  <behavior>
    Tests to add in stage2-reasoner.test.ts (append to existing describe blocks or add new describe):
    - Test A: Stage2OutputSchema.parse() succeeds when fix_suggestion and severity_justification are present and non-empty
    - Test B: Stage2OutputSchema.parse() throws when fix_suggestion is missing from an enriched_issues item
    - Test C: Stage2OutputSchema.parse() throws when fix_suggestion.length > 300
    - Test D: Stage2OutputSchema.parse() throws (or is rejected by refine) when fix_suggestion starts with 'Consider '
    - Test E: Stage2OutputSchema.parse() throws (or is rejected by refine) when fix_suggestion starts with 'You might'
    - Test F: parseStage2Output result includes fix_suggestion and severity_justification on each enriched issue
  </behavior>
  <action>
    Make four targeted changes across three files:

    FILE 1: crawler/src/pipeline/types.ts
    - Extend EnrichedIssue to add two new required string fields after technical_description:
      `fix_suggestion: string          // NEW — AI-01: specific implementation action (not advisory)`
      `severity_justification: string  // NEW — AI-02: estimated user impact in business terms`
    - Use the same inline comment style as the existing `technical_description` field.

    FILE 2: crawler/src/pipeline/stage2-reasoner.ts — four targeted changes:
    a) TOOL DEFINITION: Inside EMIT_ANALYSIS_TOOL.function.parameters.properties.enriched_issues.items.properties, add AFTER the existing `technical_description` property:
       `fix_suggestion: { type: 'string', description: 'Specific implementation action to fix this issue (e.g. "Add loading=lazy to all below-fold <img> elements", NOT "Consider optimizing images")' }`
       `severity_justification: { type: 'string', description: 'Estimated user impact in business terms (e.g. "Users on mobile connections will see a blank screen for 3+ seconds during LCP, increasing bounce rate by an estimated 20-30%")' }`
    b) TOOL REQUIRED ARRAY: Replace `required: ['index', 'technical_description']` with `required: ['index', 'technical_description', 'fix_suggestion', 'severity_justification']`
    c) ZOD SCHEMA: In Stage2OutputSchema.enriched_issues z.array z.object(), add AFTER technical_description:
       `fix_suggestion: z.string().min(1).max(300).refine((v) => !['Consider ', 'You might', 'You could', 'Try to', 'It is recommended'].some((p) => v.startsWith(p)), { message: 'fix_suggestion must use imperative framing, not advisory language' }),`
       `severity_justification: z.string().min(1).max(300),`
    d) PARSE FUNCTION: In parseStage2Output's enrichedIssues map, add AFTER `technical_description: item.technical_description`:
       `fix_suggestion: item.fix_suggestion,`
       `severity_justification: item.severity_justification,`
    e) SYSTEM PROMPT: After the last existing rule ("5. Cap causal edges at 5 total."), append the following two rule blocks (inside the SYSTEM_PROMPT template literal):

       RULES FOR fix_suggestion:
       - Write a specific, imperative action: "Add loading=lazy to all <img> elements below the fold"
       - NOT advisory: never write "Consider...", "You might want to...", "You could...", "Try to...", "It is recommended..."
       - Reference the specific DOM element, CSS property, or metric involved
       - If the fix is a code change, name the specific attribute, property, or technique

       RULES FOR severity_justification:
       - Estimate concrete user impact: bounce rate, task completion, conversion
       - Use evidence-based framing: "Pages with LCP > 4s see 24% higher bounce rate (Google CrUX research)"
       - Connect the metric to a user action: "Users waiting 3+ seconds before seeing content are 3x more likely to abandon"
       - Do not just restate the threshold — explain the felt consequence
    f) MAX_TOKENS: Change `max_tokens: 2048` to `max_tokens: 4096` in the runStage2Reasoning LLM call.

    FILE 3: crawler/src/pipeline/stage2-reasoner.test.ts
    - Add the 6 new tests (A-F) after the existing test blocks. Tests must import Stage2OutputSchema and parseStage2Output (already imported). Use the existing TWO_ISSUES fixture or declare new fixtures inline for the new tests.
    - Test D + E: these test the .refine() check. Use `.parse()` wrapped in expect(() => ...).toThrow() or check that Zod throws a ZodError.
    - Test F: construct a valid raw object with fix_suggestion and severity_justification set, call parseStage2Output, assert result.enrichedIssues[0].fix_suggestion equals the input value.
  </action>
  <verify>
    <automated>npm test -- crawler/src/pipeline/stage2-reasoner.test.ts</automated>
  </verify>
  <done>
    - types.ts EnrichedIssue has fix_suggestion: string and severity_justification: string
    - Stage2OutputSchema validates fix_suggestion (required, max 300, refine rejects advisory language)
    - Stage2OutputSchema validates severity_justification (required, max 300)
    - parseStage2Output spreads fix_suggestion and severity_justification onto enrichedIssues
    - stage2-reasoner.ts max_tokens is 4096
    - All stage2-reasoner.test.ts tests pass (existing + 6 new)
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend Prisma schema and run [BLOCKING] migration</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma — read current Issue model (lines 50-61) and CausalEdge model (lines 63-75) to understand the non-nullable String field pattern with @default()
    - crawler/src/pipeline/run-pipeline.ts — read the issues.create map (lines 112-120) to confirm fix_suggestion and severity_justification must be added to the DB write payload (this is a note for Plan 07-03 which wires the final DB write; confirm what fields are currently written)
  </read_first>
  <action>
    FILE: prisma/schema.prisma
    In the Issue model, add two new fields BEFORE the `causedBy` relation field (after `technical_description`):
      `fix_suggestion         String  @default("") // AI-01 — concrete implementation action`
      `severity_justification String  @default("") // AI-02 — user impact justification`

    Pattern to follow: `mechanism String` and `explanation String` on CausalEdge (lines 73-74) — same non-nullable String field style. Use `@default("")` on both new Issue fields because the table has existing rows (RESEARCH.md Pitfall 7: non-nullable columns on existing tables require a Postgres DEFAULT for migration compatibility).

    [BLOCKING] DATABASE MIGRATION — MUST run after schema file is saved:
    Run: `npm run db:push`
    This pushes the schema changes to the Neon development database. The migration must succeed before verification can pass — Prisma client type generation depends on the live schema.

    After db:push succeeds, verify the Prisma client was regenerated:
    Run: `npm run typecheck`
    The typecheck will confirm that the Prisma-generated types now include fix_suggestion and severity_justification on the Issue model.

    NOTE: If db:push fails with "column contains null values", check that @default("") is present on both new fields in schema.prisma before retrying.
  </action>
  <verify>
    <automated>npm run db:push && npm run typecheck</automated>
  </verify>
  <done>
    - prisma/schema.prisma Issue model contains fix_suggestion String @default("") and severity_justification String @default("")
    - npm run db:push exits 0 (migration applied to Neon dev database)
    - npm run typecheck exits 0 (Prisma generated types include new Issue fields)
    - grep for fix_suggestion in prisma/schema.prisma finds the field declaration
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stage 2 LLM output → EnrichedIssue[] | LLM-generated fix_suggestion and severity_justification cross the untrusted LLM output boundary into typed pipeline data |
| EnrichedIssue[] → Prisma Issue.create | Structured data written to database — fix_suggestion/severity_justification stored as plain text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-04 | Tampering | Stage2OutputSchema fix_suggestion .refine() | mitigate | Zod .refine() check rejects fix_suggestion values starting with advisory language ("Consider...", "You might...", "You could...", "Try to...", "It is recommended..."); if refine fails, Zod throws and the job fails cleanly rather than silently storing advisory text |
| T-7-05 | Tampering | fix_suggestion/severity_justification stored as plain text | accept | Fields rendered as JSX text children in IssueCard — not executed or parsed as HTML; max 300 chars Zod limit constrains surface area |
| T-7-06 | DoS | Stage 2 token budget expansion (max_tokens 4096) | accept | 4096 is well within Groq llama-3.3-70b-versatile context window; cost is within acceptable Groq free-tier RPD envelope |
| T-7-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; all dependencies already in crawler/package.json |
</threat_model>

<verification>
Run after both tasks complete:

1. `npm test -- crawler/src/pipeline/stage2-reasoner.test.ts` — all tests pass (existing + 6 new)
2. `npm run db:push` — exits 0 (if not already run in Task 2)
3. `npm run typecheck` — exits 0, no errors relating to EnrichedIssue or Issue model
4. `npm test` — full suite passes
5. Source check: `grep -n "fix_suggestion" prisma/schema.prisma` — finds field declaration with @default("")
6. Source check: `grep -n "max_tokens" crawler/src/pipeline/stage2-reasoner.ts` — shows 4096 not 2048
</verification>

<success_criteria>
- EnrichedIssue interface has fix_suggestion: string and severity_justification: string (non-optional)
- Stage2OutputSchema validates both fields as required, max 300 chars, fix_suggestion refine rejects advisory language
- parseStage2Output spreads both new fields onto enrichedIssues
- Stage 2 max_tokens is 4096
- Prisma Issue model has fix_suggestion and severity_justification columns with @default("")
- Database migration applied (db:push exits 0)
- All tests pass, typecheck clean
</success_criteria>

<output>
Create `.planning/phases/07-ai-pipeline-depth/07-02-SUMMARY.md` when done
</output>
