---
phase: 07-ai-pipeline-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crawler/src/pipeline/stage1-5-vision-scanner.ts
  - crawler/src/pipeline/stage1-5-vision-scanner.test.ts
  - crawler/src/pipeline/run-pipeline.ts
autonomous: true
requirements: [SIGNAL-04]

must_haves:
  truths:
    - "Visual UX issues (contrast, layout, hierarchy, CTA visibility, spacing) appear as ScoredIssue[] entries after a job run"
    - "Stage 1.5 is non-blocking: a screenshot > 2.5MB returns [] without crashing the job"
    - "Stage 1.5 is non-blocking: a Groq API error returns [] without crashing the job"
    - "Visual issues enter the scoredIssues array BEFORE Stage 2 so Stage 2 can reason about them"
  artifacts:
    - path: "crawler/src/pipeline/stage1-5-vision-scanner.ts"
      provides: "runVisualScanner, parseVisualIssues, VisualIssuesSchema exports"
      exports: ["runVisualScanner", "parseVisualIssues", "VisualIssuesSchema"]
    - path: "crawler/src/pipeline/stage1-5-vision-scanner.test.ts"
      provides: "unit tests covering parse, cap, size-guard, API-error"
    - path: "crawler/src/pipeline/run-pipeline.ts"
      provides: "Stage 1.5 wired between scoreAxeViolations and Stage 2 call"
  key_links:
    - from: "crawler/src/pipeline/run-pipeline.ts"
      to: "crawler/src/pipeline/stage1-5-vision-scanner.ts"
      via: "import { runVisualScanner } then await before Stage 2"
      pattern: "runVisualScanner"
    - from: "stage1-5-vision-scanner.ts"
      to: "groq-sdk"
      via: "client.chat.completions.create with meta-llama/llama-4-scout-17b-16e-instruct"
      pattern: "llama-4-scout"
---

<objective>
Create Stage 1.5 — a vision scanner that feeds the page screenshot to Groq's multimodal model and returns visual UX issues as ScoredIssue[] — then wire it into run-pipeline.ts so visual issues enter Stage 2 reasoning alongside DOM/network/axe issues.

Purpose: Surfaces visual, layout, and contrast issues that DOM analysis cannot detect (SIGNAL-04). Extends the signal coverage of the AI pipeline without adding new infrastructure.
Output: stage1-5-vision-scanner.ts (new), stage1-5-vision-scanner.test.ts (new), run-pipeline.ts (modified — Stage 1.5 block inserted).
</objective>

<execution_context>
@C:/Users/STL-LT-068/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/STL-LT-068/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-ai-pipeline-depth/07-RESEARCH.md
@.planning/phases/07-ai-pipeline-depth/07-PATTERNS.md
@.planning/phases/06-signal-expansion/06-03-SUMMARY.md

<interfaces>
<!-- Extracted from crawler/src/pipeline/types.ts — all fields the vision scanner produces -->

From crawler/src/pipeline/types.ts:
```typescript
export interface ScoredIssue {
  category: 'perceived-perf' | 'technical-perf' | 'accessibility'
  signal_source: string       // e.g. "visual.contrast", "visual.layout"
  severity: 1 | 2 | 3 | 4
  raw_evidence: string
  viewport: 'mobile' | 'desktop' | 'both'
}
```

From crawler/src/pipeline/stage2-reasoner.ts (structural analog — apply same pattern):
```typescript
// Tool definition type:
const EMIT_ANALYSIS_TOOL: Groq.Chat.Completions.ChatCompletionTool = { ... }
// Zod schema export:
export const Stage2OutputSchema = z.object({ ... })
// Parse function:
export function parseStage2Output(raw: Record<string, unknown>, scoredIssues: ScoredIssue[])
// LLM call function with try/catch returning [] on missing tool call:
export async function runStage2Reasoning(client: Groq, scoredIssues: ScoredIssue[])
```

From crawler/src/pipeline/run-pipeline.ts (insertion points):
```typescript
// Line 53: scoredIssues.push(...scoreAxeViolations(signals.desktop.axeViolations ?? []))
// Line 54: console.log(`[pipeline] Job ${jobId}: ${scoredIssues.length} issues scored`)
// Line 57: const screenshotUrl = screenshot ? await uploadScreenshot(jobId, screenshot) : null
// Line 89: const client = getGroqClient()
// Line 90: const { enrichedIssues, edges } = await runStage2Reasoning(client, scoredIssues)
```
Stage 1.5 block goes AFTER line 53 and BEFORE line 57. The `getGroqClient()` call on line 89 must be moved up to before Stage 1.5.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create stage1-5-vision-scanner.ts and its test file</name>
  <files>crawler/src/pipeline/stage1-5-vision-scanner.ts, crawler/src/pipeline/stage1-5-vision-scanner.test.ts</files>
  <read_first>
    - crawler/src/pipeline/stage2-reasoner.ts — structural analog: tool definition, Zod schema, parse function, async run function with try/catch
    - crawler/src/pipeline/stage1-external-scorer.ts — deterministic pure function file structure pattern
    - crawler/src/pipeline/stage2-reasoner.test.ts — test file pattern: @vitest-environment node, fixture objects, Schema.parse tests, vi.fn() mock for Groq client
    - crawler/src/pipeline/types.ts — ScoredIssue interface fields
  </read_first>
  <behavior>
    - Test 1: VisualIssuesSchema.parse() succeeds for a valid tool output with 1 visual issue (contrast category, severity 3)
    - Test 2: VisualIssuesSchema.parse() throws when visual_category is not in the allowed enum
    - Test 3: parseVisualIssues maps a VALID_TOOL_OUTPUT with 1 entry to a ScoredIssue[] where signal_source starts with "visual.", category is "perceived-perf", viewport is "desktop"
    - Test 4: parseVisualIssues caps output at 5 issues even when tool output contains 6 entries
    - Test 5: runVisualScanner returns [] when screenshotBuffer.length > 2_500_000 (no Groq call made)
    - Test 6: runVisualScanner returns [] when Groq client.chat.completions.create throws (non-blocking)
  </behavior>
  <action>
    Create crawler/src/pipeline/stage1-5-vision-scanner.ts following the EXACT structural discipline of stage2-reasoner.ts:

    1. Imports: `import Groq from 'groq-sdk'`, `import { z } from 'zod/v4'`, `import type { ScoredIssue } from './types'`

    2. VISION_SYSTEM_PROMPT: static module-level const. The prompt instructs the model to focus ONLY on visually apparent issues: color contrast failures, crowded/cluttered layout sections, inconsistent visual hierarchy, above-the-fold CTA/value-prop failures, broken/misaligned layout sections, missing visual breathing room. It explicitly says "Do NOT repeat issues already detectable via DOM/ARIA" and "Do NOT invent issues — only report what is visually apparent in the screenshot." Cap at 5 issues maximum.

    3. EMIT_VISUAL_ISSUES_TOOL: module-level const typed as `Groq.Chat.Completions.ChatCompletionTool`. Function name is `emit_visual_issues`. The `visual_issues` array items have required fields: `description` (string), `location` (string — e.g. "hero section"), `severity` (number, 1=Low, 2=Medium, 3=High, 4=Critical), `visual_category` (enum: contrast | layout | hierarchy | cta-visibility | spacing | other).

    4. VisualIssuesSchema: exported Zod schema. visual_issues array with `.max(5)`. Each item: description z.string().min(1).max(300), location z.string().min(1).max(100), severity z.number().int().min(1).max(4), visual_category z.enum(['contrast', 'layout', 'hierarchy', 'cta-visibility', 'spacing', 'other']).

    5. parseVisualIssues: exported function `parseVisualIssues(raw: Record<string, unknown>): ScoredIssue[]`. Calls `VisualIssuesSchema.parse(raw)` first line. Maps each visual_issue to ScoredIssue: category='perceived-perf', signal_source=`visual.${issue.visual_category}`, severity=issue.severity as 1|2|3|4, raw_evidence=`${issue.description} (location: ${issue.location})`, viewport='desktop'.

    6. runVisualScanner: exported async function `runVisualScanner(client: Groq, screenshotBuffer: Buffer): Promise<ScoredIssue[]>`. PRE-ENCODE SIZE GUARD: if `screenshotBuffer.length > 2_500_000`, log `console.warn('[pipeline] Stage 1.5: screenshot buffer exceeds 2.5MB pre-encode limit — skipping vision scan')` and return []. Encode: `const base64Image = screenshotBuffer.toString('base64')`. Wrap entire API call in try/catch that returns [] on any error. Messages: system prompt as {role:'system'}, user message with content array [{type:'text', text:'Analyze this screenshot for visual UX issues.'}, {type:'image_url', image_url:{url:`data:image/jpeg;base64,${base64Image}`}}]. model: 'meta-llama/llama-4-scout-17b-16e-instruct'. tools: [EMIT_VISUAL_ISSUES_TOOL]. tool_choice: {type:'function', function:{name:'emit_visual_issues'}}. max_tokens: 1024. On missing/wrong tool call: console.warn('[pipeline] Stage 1.5: no tool call returned — treating as 0 visual issues') and return []. On success: JSON.parse toolCall.function.arguments and call parseVisualIssues(raw). The entire call is wrapped in try/catch returning [].

    Create crawler/src/pipeline/stage1-5-vision-scanner.test.ts following stage2-reasoner.test.ts pattern:
    - Header: `// @vitest-environment node`
    - Imports: `import { describe, it, expect, vi } from 'vitest'`, `import Groq from 'groq-sdk'`, `import { VisualIssuesSchema, parseVisualIssues, runVisualScanner } from './stage1-5-vision-scanner'`
    - VALID_TOOL_OUTPUT fixture with visual_issues array containing 1 item (description, location, severity:3, visual_category:'contrast')
    - Implement all 6 behaviors above (Tests 1-6).
    - For Test 5 (buffer size guard): pass `Buffer.alloc(2_600_000)` and an empty object cast as Groq — no API call should be made.
    - For Test 6 (API error): use `vi.fn()` to create a mock client where `chat.completions.create` throws `new Error('Groq 429')`.
  </action>
  <verify>
    <automated>npm test -- crawler/src/pipeline/stage1-5-vision-scanner.test.ts</automated>
  </verify>
  <done>
    - stage1-5-vision-scanner.ts exports runVisualScanner, parseVisualIssues, VisualIssuesSchema
    - All 6 test cases pass
    - model string is 'meta-llama/llama-4-scout-17b-16e-instruct' (not any deprecated llama-3.2 model)
    - Pre-encode size guard at 2_500_000 bytes is present
    - try/catch wraps the entire LLM call block
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Stage 1.5 into run-pipeline.ts</name>
  <files>crawler/src/pipeline/run-pipeline.ts</files>
  <read_first>
    - crawler/src/pipeline/run-pipeline.ts — read current full content: identify exact line where getGroqClient() is called (line 89), where scoreAxeViolations push is (line 53), where the log statement is (line 54), and where uploadScreenshot is called (line 57)
    - crawler/src/pipeline/stage1-5-vision-scanner.ts — verify export name is runVisualScanner
  </read_first>
  <action>
    Make three targeted changes to run-pipeline.ts:

    1. ADD IMPORT: At the bottom of the existing import block (after line 11), add:
       `import { runVisualScanner } from './stage1-5-vision-scanner'`

    2. MOVE getGroqClient() UP + INSERT Stage 1.5 block: The current call `const client = getGroqClient()` is at line 89 (just before Stage 2). Move it up to just before the Stage 1.5 block. Insert the following block AFTER the `scoredIssues.push(...scoreAxeViolations(...))` line and BEFORE the `const screenshotUrl = screenshot ? await uploadScreenshot(...)` line:

       `const client = getGroqClient()`
       `// Stage 1.5: vision scanner — insert visual issues into scoredIssues BEFORE Stage 2`
       `// so Stage 2 can reason about them and form causal edges with visual signals`
       `if (screenshot) {`
       `  const visualIssues = await runVisualScanner(client, screenshot)`
       `  scoredIssues.push(...visualIssues)`
       `  console.log('[pipeline] Job ' + jobId + ': Stage 1.5 complete — ' + visualIssues.length + ' visual issues')`
       `}`

       Update the existing console.log on line 54 to:
       `console.log('[pipeline] Job ' + jobId + ': ' + scoredIssues.length + ' total issues scored')`

    3. REMOVE DUPLICATE getGroqClient() call: Delete the now-duplicate `const client = getGroqClient()` line that was at the original line 89 position (just before Stage 2). The `client` variable is already declared in the Stage 1.5 block above.

    The zero-issues path (lines 70-86) must remain unchanged. The screenshotUrl upload (line 57) continues after Stage 1.5.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    - run-pipeline.ts imports runVisualScanner from './stage1-5-vision-scanner'
    - runVisualScanner call is present between scoreAxeViolations push and uploadScreenshot call
    - Only ONE getGroqClient() call exists in the file (no duplicate)
    - npm run typecheck exits 0
    - npm test passes (full suite)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Screenshot buffer → Groq vision API | JPEG buffer from Playwright is base64-encoded and sent to external Groq API |
| Groq API response → ScoredIssue[] | LLM output parsed via Zod before entering the pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-01 | Tampering | stage1-5-vision-scanner.ts → Groq API | mitigate | Forced tool-call output (not free text); vision model sees JPEG bytes, not HTML — eliminates prompt injection via DOM content |
| T-7-02 | DoS | runVisualScanner: screenshotBuffer | mitigate | Pre-encode size guard: `screenshotBuffer.length > 2_500_000` check before base64 conversion; returns [] and logs warning — crawler process never allocates >3.3MB base64 string for oversized screenshots |
| T-7-03 | DoS | Groq API 429 rate limit | mitigate | try/catch in runVisualScanner catches all errors including 429 and returns []; job continues without visual issues — non-blocking by design |
| T-7-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; groq-sdk already in crawler/package.json (slopcheck [OK]) |
</threat_model>

<verification>
Run after both tasks complete:

1. `npm test -- crawler/src/pipeline/stage1-5-vision-scanner.test.ts` — all 6 tests pass
2. `npm run typecheck` — exits 0, no errors in run-pipeline.ts or stage1-5-vision-scanner.ts
3. `npm test` — full suite passes (target: 112+ tests)
4. Grep check: `grep -n "runVisualScanner" crawler/src/pipeline/run-pipeline.ts` — appears in import and call site
5. Grep check: `grep -n "getGroqClient" crawler/src/pipeline/run-pipeline.ts` — appears exactly ONCE
</verification>

<success_criteria>
- stage1-5-vision-scanner.ts exists with exports: runVisualScanner, parseVisualIssues, VisualIssuesSchema
- stage1-5-vision-scanner.test.ts exists with 6 passing tests (parse/validate/cap/size-guard/error)
- run-pipeline.ts Stage 1.5 block is between scoreAxeViolations push and uploadScreenshot call
- Zod schema rejects visual_category values not in the allowed enum
- runVisualScanner returns [] (not throws) on buffer > 2.5MB and on any Groq API error
- Full test suite green, typecheck clean
</success_criteria>

<output>
Create `.planning/phases/07-ai-pipeline-depth/07-01-SUMMARY.md` when done
</output>
